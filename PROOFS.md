# 筛选规则的数学论证（PROOFS.md）

> 动机（用户定的标准）：**筛选必须被证明有效，"经验上有效"不算。**
> REFSOL 探针（逐条消除的运行时断言）只是检测器——几百关零违例也可能是运气。
> 本文对筛选链条逐条给出论证；给不出的，明确标为缺口并降级处理。
>
> 状态标记：✅ 已证 ｜ ⚠️ 部分论证（有缺口，待补） ｜ ❌ 已证伪（不得当硬约束）

---

## 0. 模型形式化

**盘面**：网格图 G=(V,E)。V = 自由格；u,v 相邻（四邻）且都自由 ⇔ (u,v)∈E。

**解**：一个访问序列 v₀,v₁,…,v_{n−1}（n=|V|），满足：

- (S1) 覆盖且不重复：每个自由格恰好出现一次；
- (S2) 相邻性：v_i 与 v_{i+1} 在 G 中相邻（滑行逐格经过，每个经过的格都算访问）；
- (S3) **滑行规则**：把序列切成极大同方向段（滑行）。每段结束于 v_i 意味着
  v_i 沿该方向的下一格 f 满足：f 是墙，或 f ∈ {v₀,…,v_{i−1}}（更早被访问）。
  这对**每一段**成立，包括最后一段（终点处的滑行也是被挡住才停的；
  终局时所有格已访问，与此一致）。

由 (S1)(S2)：解在 G 上是一条**哈密顿路径**。记 t(v) 为 v 的访问时刻（双射 V→{0..n−1}）。

**路径边集** P ⊆ E：解中相继访问的格对。**筛选的可靠性** = 声称"任何解都满足 X"的推论，
对每一个真解确实成立。

---

## 1. 无向层（`pprocess`, `set_edge`）

### 1.1 度数约束 ✅

**命题**：非端点格在 P 中度数恰好 2；两个端点度数恰好 1。

**证明**：哈密顿路径上，内部顶点恰有一个前驱边和一个后继边；端点只有一条关联边。∎

实现（`pprocess`）：`lo/hi` 度数窗口 + 「used > hi 或 used+avail < lo ⇒ 矛盾」+
「used == hi ⇒ 余边禁用」+「used+avail == lo ⇒ 可用边全部必用」。
这是命题的直接区间推理，逐条对应。✅

### 1.2 必用边不成环 ✅（`set_edge` 的并查集）

**命题**：P 的任何子集不含圈。**证明**：路径是无圈图，子集亦然。∎

实现：必用边并查集，合并前同根 ⇒ 矛盾；`pprocess` 首段把"连上即成环"的待定边禁掉。
注意它只对**必用**边建圈检测——待定边不参与合并，无过杀。✅

### 1.3 端点颜色/奇偶 ✅

**命题**：按 (x+y) 奇偶二染色，P 的每条边异色，故访问序列颜色交替。设两色计数 c₀,c₁：
- |c₀−c₁| ≥ 2 ⇒ 无解；
- c₀ = c₁+1 ⇒ 两端点均为色 0（同理对称）；
- c₀ = c₁ ⇒ 两端点异色。

**证明**：交替序列中，两端同色 ⇔ 长度为奇 ⇔ 该色比另一色多 1；两端异色 ⇔ 长度为偶 ⇔ 两色相等。∎

实现：`filter_endpoints` 开头的计数分支 + 每起点相的 `prop_endcol = col[s] ^ ((n−1)&1)` 等价形式。✅

### 1.4 「至多挂 1 条边 ⇒ 必是端点」及其全局/每起点语义 ✅

每起点相：`used+avail == 1` 的格只能是终点 ⇒ 颜色须等于 `prop_endcol`，且终点唯一。
由 1.1、1.3 直接得。全局相（`prop_global`）：只能断言"它必须**可能**是端点"（`end_ok`，见 §2.2），
不得用颜色二选一（全局相端点色未定，两色都可能）。当前实现正是如此。✅

---

## 2. 流层（`do_flow` / `filter_endpoints`，Régin GAC）

### 2.1 度约束松弛的 GAC ✅

**命题**：把"每格度数 b(v)（非端点 2，端点 1），边取 0/1"建为二部图度约束子图问题
（网格按染色天然二部），用最大流求可行解；对可行流的**残量图**求 SCC，则：
- 正向弧两端不同 SCC 且流量为 0 ⇒ 该边**不出现在任何**可行度约束子图中 ⇒ 任何解不用它（可禁用）；
- 反向弧两端不同 SCC 且流量为 1 ⇒ 该边出现在**所有**可行子图中 ⇒ 任何解必用它。

**证明**：标准流理论/Régin (1994)：一条弧的取值可换 ⇔ 它位于残量图的某个圈上 ⇔ 两端同 SCC。
可靠性方向：任何真解在此松弛下都是可行点；"所有可行点都取 x"的边，真解也取 x；
"没有可行点取 1"的边，真解不取。∎

（松弛比真问题弱——它不含连通性与时序——但**弱松弛上的必然结论对强问题仍必然**，方向没错。）

### 2.2 哑点技巧与 `end_ok` ✅（模掉 2.4 的实现注意项）

**命题**（偶 n，两端异色）：源侧加哑点 z₁（容量 1）连所有色 1 格、汇侧加 z₂ 连所有色 0 格，
所有格容量 2。可行流中恰一个色 1 格与恰一个色 0 格各"让出" 1 单位 ⇒ 它们是端点。
格 c 可为端点 ⇔ 存在可行流使哑点单位落在 c ⇔（哑弧载流）或（哑点与 c 同 SCC）。
奇 n 同理（单哑点容量 2 挂多数色一侧）。

**证明**：容量与守恒的直接对应 + 2.1 的支撑判据。∎
`end_ok[c]=0` 的语义是"c 当**任何一个**端点都不可行"（两个哑点分别测，取或），
所以 §1.4 全局相用它收紧是合法的。✅

### 2.3 分治（块对联合假设）✅（条件：枚举穷尽）

**命题**：把格子分成 K 块，对每对 (i,j) 限制两哑点分别落在块 i、j 内跑可行性；
若某块与所有块（含自身）配对都不可行，则该块内所有格都不能当端点。

**证明**：真解的两个端点各落在某块中，构成某个可行配对。枚举含 i=j 与全部有序/无序组合
（实现按无序对 + 同块对）。若含真端点的配对可行性被正确测试，则整块淘汰只删不可行者。∎
条件是**枚举无遗漏**——实现按 `K×K` 全对跑，满足。✅

### 2.4 ⚠️ 实现注意项（曾出过三次同型 bug，现已修，列为审计点）

- 奇偶两种哑点挂法下，"哑弧载流"的判读方向相反（v32/v44/流 SCC 各栽过一次）——
  现以 `prop_endcol != col[prop_start]` 分支判读。已修，但**任何改动此处必须重审**。
- 热启动（v69–v71）的可靠性依赖"容量只降不升 + 从合法流跑到最大即最大流"，
  论证见 notes（v69 节），成立。✅

---

## 3. 搜索内剪枝

### 3.1 `cheap_ok` ✅

设当前在 p，剩余格集 R（未访问自由格），剩余路径 = 从 p 出发、恰覆盖 R 的路径（p∉R，共 |R|+1 个顶点）。

- **奇偶**：剩余路径顶点序列颜色交替、首色 col(p)、长度 |R|+1 ⇒
  R 中与 p 异色的格数必等于 ⌈|R|/2⌉。实现 `cnt[col[p]^1] != (remaining+1)/2 ⇒ 0`。✅
- **孤格**：R 中度数（对 R∪{p} 诱导子图）为 0 且不与 p 相邻的格无法被进入 ⇒ 矛盾。
  实现 `zero_cnt − adj_zero > 0 ⇒ 0`（相邻 p 的 0 度格可作第一步进入，故扣除）。✅
- **死端计数**：剩余路径中除终点外每个格需要 ≥2 条关联边（进+出）；度 ≤1 的格只能当终点，
  终点只有一个。实现 `low_cnt − adj_low + adj_zero <= 1`：与 p 相邻的低度格被扣除
  （p→它 的边可补足其度），但 0 度且邻 p 的格仍占死端名额（`+adj_zero` 回加）——
  它只能作为下一步立刻进入并终结，仍是"只能当终点"。✅
- **死端必须是终点候选**（`tc_on` 分支）：终点 ∈ tcand（流层算出的候选集，§2.2 语义），
  死端只能当终点 ⇒ 非候选的死端 ⇒ 矛盾。依赖 tcand 可靠（§2.2）与 `tcand_for == s` 防串台。✅

### 3.2 `reach_ok`（精确连通性）✅

**命题**：若 R 中存在从 p 的邻居出发（四邻 BFS，只走 R）不可达的格 ⇒ 无解。
**证明**：由 (S2)，访问序列是 G 中路径；剩余路径的每一格都要从 p 经 R 内的格逐格走到。∎
（滑行只会**减少**可停的方式，不增加可达性——BFS 是可达性的上近似，用于否定是可靠方向。）✅

### 3.3 `estate_ok`（滑行与已定边的一致性）✅

一次滑行 p→…→c（方向 d，长 len）蕴含：线上 len 条边全用；中间格直穿 ⇒ 其垂直边不用；
落点 c 不再前行 ⇒ 边 (c,c+dd) 不用。
最后一条的完整论证：c 首次被访问于此刻，其关联边只能在进入/离开 c 时被用；
进入边是 (c−dd,c)；离开时 dd 方向被挡（墙或已访问）、−dd 已访问，故离开边垂直；
所以 (c,c+dd) 永不被用。∎ 与已定 estate 冲突 ⇒ 剪。✅

### 3.4 `first_move_dir_ok` / `first_run_ok` ✅

**命题**：第一次滑行必从 s 直冲到墙（t=0 时无已访问格可挡），
故线上有禁用边、或中间格有垂直必用边 ⇒ 该方向不可为首步。
**证明**：滑行规则 (S3) 在空盘面上的特例 + §3.3 的中间格论证。∎ ✅

### 3.5 `chain_dir_ok` / `first_run_ok`（链定向）✅（G1 已闭合）

**预备**：必用边的极大链 seq[0..k−1]。内部格有 2 条必用边 ⇒ 度数已满（1.1）⇒ 无其他 P 边
⇒ 链是 P 的**连续段**，访问序即位置序（正向或反向二选一）。链不成环由 1.2 保证；
起点 s（度 1）不可能是链内部格（那需要 2 条必用边，`pprocess` 在此之前已判矛盾）。

**内部拐点规则**：以给定方向穿链时，位置 i 处若拐弯（next−seq[i] ≠ seq[i]−prev），
由 (S3)：前方格 f = seq[i]+(seq[i]−prev) 是墙，或 t(f) < t(seq[i])。
- f 是墙 ⇒ 合法，跳过 ✅；
- f 在**同链** ⇒ 链的访问序即位置序，t 比较化为位置比较（rev 时取 k−1−pos）：
  `tf > ti ⇒ return 0`。✅
- f 在**链外**：一般情况下不知 t(f)，不下结论（非 strict 跳过，可靠）✅；
  **strict 情形**（链头/尾 == s，且访问方向使 s 最先）：s 是全路径起点（t=0）且度数 1，
  它唯一的 P 边就是链边 ⇒ **链占据路径的前 k 个位置** ⇒ 链外任何格 t ≥ k > ti
  ⇒ 拐点前方在链外必然"更晚" ⇒ `return 0`。✅

**调用方逻辑**：`head_s ⇒ ok1=0`（反向穿链要求 s 在链内最后被访问，与 t(s)=0 矛盾）✅；
`tail_s` 对称 ✅；两方向都不可行 ⇒ 矛盾（真路径必取其一）✅。

**`first_run_ok`** ✅：首滑必直冲到墙（§3.4）。含 s 的链，头部 min(limit, k−1) 步必须沿首方向直线
（limit = 到墙距离）。链短于滑行 ⇒ 无约束（路径在链外继续直行，合法）；
链长于滑行 ⇒ 位置 limit 处允许拐（那里滑行已被墙终止）。实现逐条对应。∎

### 3.6 树内镜像 `pprocess2` / `set_edge2`（G3 已闭合）✅

**子问题定义**：前缀走到 p，剩余格集 R（g=1），剩余路径 = 从 p 出发恰覆盖 R 的路径
（p 记 1 条出边；终点度 1；其余度 2）。

**关键引理**：**未访问格的任何 P 边都属于剩余路径。**
证明：c 未访问 ⇒ c 的每条 P 边在进入/离开 c 时被走，都发生在将来。∎
推论 1：`estate2` 从 estate（每起点相的结论）memcpy 而来是可靠的——estate 只假设"s 是起点"，
该假设沿路径不变；两端都在 R 里的必用边必属于剩余路径。
推论 2：一端已访问的必用边**不可能**被剩余路径使用（不能重访）；`pprocess2` 的度数计数
只数 g=1 的邻居，等于把这类边跳过——**少剪不误剪**（这里其实是矛盾，可作为将来的加强点，
但跳过是保守方向，可靠）。✅

逐条对照：环检测（dsu2）= §1.2；度数窗口（p2_start 1 / p2_end 1 / 其余 2）= §1.1 在子问题上的实例；
`p2_endcol = col[p] ^ (remaining & 1)` = §1.3 的交替计数（剩余路径长 remaining+1）；
"至多 1 边 ⇒ 必是终点" = §1.4。全部与已证命题一致。∎

---

## 4. 有向层（v73，`dprocess` / `dir_probe` / `ord_reach`）

变量 dirv[c][e] ∈ {未定,是,否}：路径是否从 c 走向 c+e。每条 P 中的边恰被一个方向使用。

### 4.1 与无向层互喂 ✅
禁边 ⇒ 两向否；必用边 ⇒ 恰一向是（一向否 ⇒ 另一向是）；两向否 ⇒ 边不用。
均为"边被使用 ⇔ 恰一个方向被走"的直接推论。∎

### 4.2 出入度 ✅
出度：非终点恰 1、终点 0；入度：非起点恰 1、起点 0。序列相继性的直接推论。
全局相只用"≤1"与 `end_ok=0 ⇒ 恰好 1"（§2.2 语义匹配）。∎

### 4.3 滑行时序（T 关系）✅
T(c,e)（从 e 进入 c 且不直行）⇒ c+e 是墙或 t(c+e) < t(c)。即 (S3)。∎
成对反对称 ¬(T(c,e) ∧ T(c+e,−e))：两式给出 t 互小，矛盾。∎

### 4.4 时序找环（bounded brute force）✅
"更早于"关系边集：dirv=是 给 t(a)<t(a+d)（相继访问）；T 给 t(c+e)<t(c)。
真解的 t 是全序 ⇒ 该关系无环。有界搜索**找到环才剪**，找不到不下结论 ⇒ 任意预算下可靠。∎

### 4.5 有向 probing（failed literal）✅（结构条件）
假设 dirv[c][e]=v 传播出矛盾 ⇒ 任何解都不取 v（传播规则均为已证可靠规则的复合）⇒ 取反。
双腿交集：A ⊢ X 且 ¬A ⊢ X ⇒ X。∎
**结构条件**：试探内所调用的每条规则必须使用与当时语境一致的语义
（全局相须 `prop_global=1`；试探期不得写非 trail 状态）。当前实现满足；
此处历史上出过 4 个同型 bug（详见 notes），**任何改动必须重过 REFSOL + 1~200 全量**。

---

## 5. 已证伪 / 已知不可靠（不得当硬约束）

- ❌ **SEAMR**（终点候选限制在起点 L1 球内）：306 个合法解实测 L1 中位 14、最大 141，
  R=20 会剪掉约 1/3 关卡的真解。只可作软先验（先小半径试，失败必须放开重试）。
- ❌ 树内有向层曾误写外层 estate（分发宏漏用）——已修；教训：D_mode 下所有写必须走分发宏。

## 6. 缺口清单（按风险排序）

- ~~G1~~ **已闭合**（§3.5）：`chain_dir_ok`/`first_run_ok` 均已证。
- **G2** 「障碍接触一致性检查」若未来从 ericgopak 引入——引入前必须先证（notes 已标 ⚠ 未验证）。
- ~~G3~~ **已闭合**（§3.6）：镜像实现对照审计完成。

## 7. 证明与探针的关系

- 本文的证明保证**规则**正确；REFSOL 探针检测**实现**与规则的偏离（编码错、语境错、同步漏）。
- 两者缺一不可：今天探针抓到的 4 个 bug 全是实现层的（方向编码、语境切换、分发宏、越界判断），
  规则本身都是可证的。反过来，SEAMR 是规则层错误——探针在样本内抓不到它，靠的是全量统计证伪。
