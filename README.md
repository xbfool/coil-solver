# coil-solver

给 [adum/coilbench](https://github.com/adum/coilbench) 写的 Coil 解题程序。**上游仓库里没有任何答案**
（关卡文件只有棋盘，校验器 `check.c` 是纯模拟器，走一遍数还剩几格没走到），所以这里是从零写的。

## 题目

网格里选一个空格出发，每次选一个方向就**一直滑到撞墙/撞边界/撞已访问格子**才停，然后重新选方向。
要求每个空格恰好走一次。本质是空格集合上的哈密顿路径，只是拐弯时机被滑动规则限死。

评测是**一个程序打全部关卡**：评测脚本按关号依次把棋盘喂给 `./run_solver`（stdin 进、stdout 出，
每关 600 秒），失败即停，成绩 = 通过的最高关号。程序看不到关号，只看到棋盘，所以没法按关特判。
难度只有一个维度——棋盘大小：L1 是 3×3，L1207 是 1990×1990。

## 当前进展（详见 notes.md）

**两条线，含义不同，别混着说：**

| 口径 | 分数 | 卡在哪 |
| --- | --- | --- |
| **官方 600 秒/关**（`evaluate_full.py`，和 results.md 同口径） | **694** | L664 合规（590s，v66，台式机）；下一堵是 L695 |
| **打榜线**（不限时，用缓存解） | **694** | L695 离线 2400 秒也没解出 |

对标上游 results.md（各家模型 47～264，最高 kimi 2.6 的 264）时应报 **663**。
参考：上千人试过这道题，**47 人过了 300 关**，4 人通关（上游 background.md）。

- **L664 已攻陷**：2432s → **590s 合规**（v66）。
  ⚠ **机器依赖**：590s 是在台式机上测的；笔记本（CPU 弱）上同二进制同配置 800s 未解出。
  所以**跨机器的绝对秒数不可比**，做 A/B 必须同机。
��的是两个参数/调度错误，不是算法：
  SWEEP 第一轮预算从 80.4M 改成 **400k**（按赢家树 291k 节点定标，原值是它的 276 倍），
  漏斗从静态分片改成**动态取号**（原来 27 个核在 124 秒后白闲 570 秒）。
  真正的下一堵墙是 **L695**（离线 2400 秒未解出）——建议先用 PINSTART 量它的赢家树，
  很可能是同一个调度病而非「更难」。
- **解的银行不在本仓库**，见 `SOLUTIONS.md`（走 private 仓库，理由是防止解流进 LLM 训练集）。
- 生产版本 **v66**（v58 + SWEEP=400000 + 漏斗动态取号）。

## 走到今天的几个转折

1. **v30 流 GAC**：网格是二部图，「每格恰好用 2 条边」就是度约束子图；一次 max-flow + 残量图 SCC
   同时产必用和禁用。判定率 39.7% → 50.1%。
2. **v32 全局端点过滤**：起点必然是路径两个端点之一，而流模型里那个哑点正在挑端点 ——
   我们本来就在算 SCC，只是从没看过哑点的弧。起点候选砍 30~500 倍。
3. **CP-SAT 二进制加法器编码**：滑动规则首次进带学习的求解器，L73 7.8 秒、L22 0.1 秒。
4. **自适应配置转正**：L501 当年打了 19 击，现在裸默认 564 秒自动破。
5. **四堵墙**：L501（针）→ L570（预算调度）→ L603（厚墙/密室）→ L664（漏斗税爆炸）。

**统领性的一条规律**：这批盘面的信息在**度**里，不在**连通性**里 ——
桥、割点、互斥、等价推理、subtour 全部 0 条推论，而度约束流一路见效。

## 当前的算法边界

L664 那类「深 doom」（~96 层决策深死）抗一切廉价剪枝：传播能把节点剪到 170 万**地板**，
其下是真 NP 硬核，只能实打实搜。一晚上试遍的思路全部判死且各有量化死因
（PD128 成本抵消 / 增量传播撞「传播是节点相对的」根本障碍 / 割点 0.14% 触发慢 16 倍 /
SAC 全局层新禁边 0 / trail 撤销提速≈0）。**要突破得换范式，不是调参能成。**

## ⚠️ 在线战线：停火中

曾把 hacker.org 打伤（爬虫卡 442 每秒重试 = 无意 DoS，真凶是长路径 GET 撞 414 上限）。
**重启必须带三件套：POST 提交、指数退避、限速。** 那是别人的服务器。

## 换台机器（一条命令）

```bash
wsl -e bash -lc "cd /mnt/d/works/coil-solver && ./setup.sh"
```

拉 coilbench、修 CRLF、编译校验器、装全部 **1207 关**（奇数关明文 + 偶数关归档 + 解出的 `levels_all/`）。

关卡不进 git：奇 148MB + 偶 150MB ≈ 300MB，而上游 release 有 47MB 压缩包，就地重建更划算。
（上游把偶数关加密是**防 LLM 训练集污染**；我们是从零写求解器、从不看答案，这条顾虑对我们不适用，
所以脚本直接把偶数关也解出来用。）

## 两种评测口径 —— 别混

| 命令 | 跑什么 | 用途 |
| --- | --- | --- |
| `./bench <版本> --timeout 600` | **只跑奇数关**（evaluate.py） | 日常迭代 |
| `COIL_FULL_PASSWORD=local ./bench-full <版本>` | **奇+偶全量**（evaluate_full.py） | 对标官方榜 |

奇数关口径会**偏乐观**：中间任何一关偶数关卡住都测不出来。要跟 results.md 对标得用全量。

## 用法

需要 WSL（Windows 侧没有 C 编译器，且上游脚本是 POSIX sh）：

```bash
wsl -e bash -lc "cd /mnt/d/works/coil-solver && ./bench --start 1 --timeout 60"
```

`bench` 会编译版本源码 → 拷进 coilbench 当 `./solver`（`run_solver` 会优先跑它）→ 调官方
`evaluate.py`。coilbench 默认找**同级目录**，可用 `COILBENCH_DIR` 覆盖。

**关卡包不用口令**：`download_full_levels.sh` 里的口令只是用来把偶数关加密存档的，关卡本体在
公开 release 直接下载 —— `releases/download/data-v1.0/boards.zip`（47MB，1207 关）。
解开后奇数关放 `levels_public/`（`evaluate.py` 用），全部 1207 关放 `levels_all/`（单关调试用）。

两个环境坑：编译要 `-std=gnu99`（gcc 15 下 `-std=c99` 会把 POSIX 声明藏起来，`kill()` 变成
隐式声明，而隐式声明在新版是错误不是警告）；clone coilbench 要 `core.autocrlf false`，
否则 CRLF 会让 WSL 里的 `#!/usr/bin/env python3` 起不来。

## 版本

每个版本单独一个文件，`./bench <版本名>` 直接对比。当前最好的是 `v48-branchprop.c`。实验脚本索引见 `experiments/README.md`。

`versions/v1-dfs.c` — 滑动 DFS：

- **滑动 DFS**：状态 = 位置 + 已访问集合，每步枚举 4 个方向滑到底
- **连通性剪枝**：每个节点从当前位置洪水填充，剩余空格必须全部可达，否则剪掉
- **死端剪枝**：统计只剩 ≤1 个出口的未访问格子，超过 1 个必然无解（路径只有一个终点）
- **Warnsdorff 排序**：优先走向出口最少的落点
- **起点排序**：度数小的格子优先（度 1 的格子必然是路径端点）
- **迭代放宽**：先给每个起点浅搜（30 万节点），全失败再把上限 ×8 重来，避免卡死在坏起点上

每个版本单独一个文件，`./bench <版本名>` 直接对比。当前最好的是 `v48-branchprop.c`。实验脚本索引见 `experiments/README.md`。
不用翻 git 历史。成绩表和迭代观察见 `notes.md`。
