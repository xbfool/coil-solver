# 迭代记录

每个版本一个文件放在 `versions/`，跑 `./bench <版本名>` 对比。

## 成绩表

日常迭代用 **30 秒超时**当快速代理指标（跑一轮几分钟），里程碑才用官方 600 秒复测。
关号越高盘越大：L101=36×36，L135=48×47，L301=104×102，L901=304×302，L1207=1990×1990（终点）。

| 版本 | 30s 超时 | 600s 超时 | 卡在哪 | 说明 |
| --- | --- | --- | --- | --- |
| （上游自带 coil_solver.py） | 31 | 47（上游记录） | — | 纯暴力回溯基线 |
| v1-dfs | **135** (48×47) | 未测 | L137 (48×48) 超时 | 滑动 DFS + 连通性剪枝 + 死端剪枝 + Warnsdorff |

参考：上游 results.md 里各家模型迭代出来的成绩多在 47～117，最高是 kimi 2.6 的 264。

## v1-dfs

**做法**：状态 = 位置 + 已访问集合，每步枚举 4 个方向滑到底。剪枝两条——
①从当前位置洪水填充，剩余空格必须全部可达；②只允许 1 个死端（只剩 ≤1 出口的未访问格子），
因为路径只有一个终点。落点按出口数升序试（Warnsdorff），起点按度数升序试（度 1 的格子必是端点）。
每个起点先浅搜 30 万节点，全失败再把上限 ×8 重来。

**观察**：
- 时间跳变剧烈，不随盘面单调：L131 (46×46) 只要 0.04s，L129 (46×45) 要 9s，L135 (48×47) 要 5.66s。
  说明成败很大程度取决于**第一个起点的头几步猜得对不对**，而不是盘面本身多难。
- 这暗示下一步该往「随机重启 / 打散搜索顺序」方向走，而不是继续加剪枝：
  确定性 DFS 一旦扎进坏子树，加大节点上限只会把同一片坏区域再犁一遍。

## 待试清单

- [ ] **随机重启**：Warnsdorff 打平时随机选，节点上限到了就换随机种子重来（v2 先试这个）
- [ ] 增量连通性（并查集带回滚）替掉每节点 O(剩余) 的洪水填充
- [ ] 度 2 格子的强制边传播（哈密顿路径的经典约束传播）
- [ ] 关节点检测：走完一步若把剩余区域切成两块即剪
- [ ] 大盘（几百×几百以上）回溯必死，需要构造性解法 / 分块拼接
