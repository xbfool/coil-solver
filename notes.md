# 迭代记录

每个版本一个文件放在 `versions/`，跑 `./bench <版本名>` 对比。

## 成绩表

日常迭代用 **30 秒超时**当快速代理指标，里程碑才用官方 600 秒复测。
关号→盘面：L101=36×36，L137=48×48，L201=70×69，L301=104×102，L901=304×302，L1207=1990×1990（终点）。

| 版本 | 30s 超时 | 卡在哪 | 说明 |
| --- | --- | --- | --- |
| 上游 coil_solver.py | 31 | L35 | 纯暴力回溯基线（官方 600s 记录是 47） |
| v1-dfs | 135 | L137 | 滑动 DFS + 连通性剪枝 + 死端剪枝 + Warnsdorff |
| v2-restart | ~135 | L137 勉强 30s | 加随机重启（Luby） |
| v3-parity | ~137 | — | 加奇偶染色剪枝，中盘快 2 倍（L137 15→4.5s） |
| v4-incdeg | **137** | L139 | 增量度数把剪枝降到 O(1)，洪水填充只留残局 |

参考：上游 results.md 各家模型 47～117，最高 kimi 2.6 的 264。

**关键观察：v1→v4 四个版本，分数从 135 只挪到 137。** 单关耗时确实降了 2～3 倍，
但关号是**指数级**难度轴——常数倍的提速换不来几关。继续在「搜索 + 剪枝」这条路上加东西是没有前途的，
必须换问题的表示方式。

## 结构分析：这题到底是什么

### 精确刻画

设自由格上的哈密顿路径 P = (c₁,…,cₙ)。**P 是合法 Coil 解，当且仅当**：
在每个转向格 cᵢ 处，沿进入方向的前方格子要么是墙，要么是 cⱼ（j < i，即已经走过）。

也就是说 Coil = 哈密顿路径 + **一个纯时序的前缀阻挡条件**。终局看是看不出这个约束的
（终局所有格子都被走过），它只在「走到这一步时」才有意义。这正是 Coil 比一般哈密顿路径难的地方，
也是为什么现成算法套不上去。

### 空棋盘上解是螺旋（已实测验证）

12×12 全空棋盘，v4 给出的解转向序列是 `RDLURDLURDLU…`——**严格的螺旋**。
这不是偶然：没有墙时，一旦撞到边界就被迫沿边界走，绕完一圈只能向内缩，唯一出路就是螺旋。
所以在大片空白区域里，解的形状是被**强制**的，自由度极低。33% 的墙只是在螺旋上打补丁。

这条观察指向：**大盘不该搜，该构造**。L1207 是 1990×1990、396 万格，任何回溯都没戏；
但如果解在大尺度上就是「螺旋 + 绕墙修补」，那构造 + 局部修复是可行的。

### 可能的「换个数学空间」

按可行性排序，我认为值得试的：

1. **度约束子图 → 完美匹配（Tutte 归约 / Edmonds blossom）**。
   哈密顿路径 = 每点度 2（两端点度 1）的生成子图 + 连通。度约束部分是**多项式可解**的
   （Tutte 的小工具展开成完美匹配，用 blossom 算法），难的只剩「结果是若干不相交圈，要合并成一条」。
   好处是起手就拿到一个结构上合法的对象，而不是从空盘瞎搜。
   代价：这套只管哈密顿路径，Coil 的时序阻挡条件不在里面，得当骨架再修。

2. **插头 DP / 连通性轮廓 DP（非交叉分划 ≅ Temperley–Lieb 代数）**。
   逐列推进，状态是「前沿的连通配对方式」，状态数是 Catalan 数级——对宽度 ≲ 20~25 完全实用，
   且**关于另一维是线性的**。单独用它打不了 1990 宽，但可以：把大盘切成窄条各自精确求解再缝合。
   这是我目前认为最有前途的一条（精确 + 可分治）。

3. **Kasteleyn / Pfaffian（二聚体模型）**。平面图完美匹配数 = 带符号邻接矩阵的 Pfaffian，
   多项式时间。诱人，但**连通性不是局部约束**，落不进 Pfaffian 的框架——这也正是带洞网格的
   哈密顿圈仍是 NP 完全（Itai–Papadimitriou–Szwarcfiter 1982）的原因。可以用来做上界/计数直觉，
   别指望直接出解。

4. **螺旋构造 + 局部修复**。承接上面的实测观察：先按空盘规律铺螺旋骨架，遇到墙做局部绕行补丁，
   再用时序条件检查/修复。土，但很可能是唯一能碰 1990×1990 的路子。

### 复杂度现实

带洞网格图的哈密顿路径是 **NP 完全**，所以不存在「换个空间就多项式」的银弹。
但这些关卡是**随机生成且保证有解**的——这个 regime 恰恰是构造性/结构性方法擅长、
而通用搜索最吃亏的地方。所以方向不是找更强的搜索，是找这批实例的**生成结构**。

## 下一步

- [ ] 先做 2：窄条上的插头 DP，验证在宽度 15~20 的长条上能否秒解 → 再想分治缝合
- [ ] 顺带验证「随机 33% 墙 + 保证有解」的关卡是不是有可利用的生成规律（看看墙的分布是否均匀）
- [ ] 4 作为大盘兜底
- [ ] 1 可以先不做，时序条件让它只能当骨架，性价比不如 2
