# 迭代记录

每个版本一个文件放在 `versions/`，跑 `./bench <版本名>` 对比。实验脚本在 `experiments/`。

## 成绩表

日常迭代用 **30 秒超时**当快速代理指标，里程碑才用官方 600 秒复测。
关号→盘面：L101=36×36，L139=50×48，L201=70×69，L301=104×102，L901=304×302，L1207=1990×1990（终点）。

| 版本 | 30s 分数 | 说明 |
| --- | --- | --- |
| 上游 coil_solver.py | 31 | 纯暴力回溯基线（官方 600s 记录 47） |
| v1-dfs | 135 | 滑动 DFS + 连通性剪枝 + 死端剪枝 + Warnsdorff |
| v2-restart | ~135 | 加 Luby 随机重启 |
| v3-parity | ~137 | 加奇偶染色剪枝（中盘快 2 倍） |
| v4-incdeg | 137 | 增量度数把剪枝降到 O(1)，洪水填充只留残局 |
| v5-exhaust | 待测 | **砍掉重启**，系统穷举（见下：重启是帮倒忙） |

参考：上游 results.md 各家模型 47～117，最高 kimi 2.6 的 264。

## 实测结论（都有脚本可复现）

### 1. 「换个数学空间」这条路被数据否掉了 —— `experiments/ham_vs_coil.py`

| 盘面 | 哈密顿路径 | 合法 Coil 解 | 占比 |
| --- | --- | --- | --- |
| 3×3 空 | 40 | 16 | 40% |
| 4×4 空 | 552 | 32 | 5.8% |
| 5×5 空 | 8,648 | **32** | 0.37% |

**Coil 解数是常数 32，哈密顿路径指数爆炸。** 插头 DP / 完美匹配 这类办法构造的是**几何对象**
（哈密顿路径），而 Coil 多一条**纯时序**约束——转向时前方格子必须已经走过——DP 的前沿状态里
根本没有「谁先谁后」这个信息。所以「构造哈密顿路径再过滤」的过滤比例趋于 0，路是死的。

附带一个反直觉的事实：Coil 解**不是时间对称的**，同一条路径反着走通常不合法。

### 2. 关卡是「照着有解」构造出来的 —— `experiments/board_stats.py`

- **没有任何度 0 或度 1 的自由格**（度2=30%，度3=57%，度4=14%，共 100%）。随机撒墙绝无可能。
- **墙在两种染色上的数量完全相等**：L201 786/786，L501 4749/4749，L1101 144585/144585。
  这正是哈密顿路径需要的奇偶平衡条件。生成器在刻意维持它。
- 墙密度稳定 33%，永远单连通，2×2 全空块约占自由格 30%（是开阔场地，不是走廊迷宫）。

### 3. 真实关卡的解极少，常常唯一 —— `experiments/count_solutions.c`

| 关 | 自由格 | 有向解数 | 全树节点 |
| --- | --- | --- | --- |
| L51 | 294 | 2 | 4.5 万 |
| L71 | 493 | 3 | 10 万 |
| L99 | 859 | **1** | 3.2 万 |
| L139 | 1,604 | **1** | 1,685 万 |
| L171 | 2,352 | 10 | 1,042 万 |

约束几乎把答案锁死了。**这直接推翻了 v2~v4 的随机重启**：树本来就不大，重启到 4 万节点
就换起点，等于把搜过的反复扔掉。同一关 v4 三十秒超时，穷举器几秒解完 → v5 砍掉重启。

### 4. 约束传播能把一半路径直接定死 —— `experiments/forced_edges.py`

把解看成边的选择（每格恰好用 2 条邻边），三条规则迭代到不动点：
度2格的两条边必选 / 某格定满 2 条则排除其余 / 两端已连通的边会成环故排除。

| 关 | 自由格 | 强制边 | 占比 | 骨架片段 | 最长片段 |
| --- | --- | --- | --- | --- | --- |
| L139 | 1,604 | 868 | 54% | 186 段 | 38 格 |
| L301 | 7,161 | 3,862 | 54% | 829 段 | 41 格 |
| L501 | 19,232 | 10,095 | 53% | 2,206 段 | 39 格 |

**零搜索、O(n) 时间，路径的一半就定了。** 这就是「拆成子问题」的正确形态：
拆点不是猜出来的，是**推**出来的。L501 从「给 19,232 格排一条路」降成
「把 2,206 段骨架和 6,931 个散格串起来」——问题规模缩了近 9 倍，而且这一步能线性扩展到 1990×1990。

## 下一步：v6 = 传播 + 缩点 + 在缩点图上搜

1. 先跑强制边传播，把每段骨架**缩成一个超级节点**（两个端头 + 内部固定走法）
2. 在缩点图上搜索，节点数少一个数量级
3. 时序（Coil 转向）条件在展开成实际路径时再校验/修复
4. 更强的传播规则还没上：连通性/奇偶推回边层面、按段做可达性 —— 54% 是下界不是上限
