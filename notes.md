# 迭代记录

每个版本一个文件放在 `versions/`，跑 `./bench <版本名>` 对比。实验脚本在 `experiments/`。

## 成绩表（30 秒超时作为快速代理指标）

关号→盘面：L101=36×36，L139=50×48，L201=70×69，L301=104×102，L901=304×302，L1207=1990×1990（终点，共 1207 关）。

| 版本 | 手法 | 30s 分数 | 结论 |
| --- | --- | --- | --- |
| 上游 coil_solver.py | 暴力回溯 | 31 | 基线（官方 600s 记录 47） |
| v1-dfs | 滑动 DFS + 连通/死端剪枝 + Warnsdorff | 135 | |
| v2-restart | + Luby 随机重启 | ~135 | 后来证明是负作用 |
| v3-parity | + 奇偶染色剪枝 | ~137 | 单关快 2 倍 |
| v4-incdeg | 剪枝降到 O(1)，洪水填充只留残局 | 137 | |
| v5-exhaust | 砍掉重启，系统穷举 | 137~139 | 目前最好，但不稳定 |
| v6-beam | 束搜索（宽搜） | 失败 | beam 走空，见下 |
| v7-levelbeam | 按进度分层的束搜索 | 失败 | 同上 |
| v8-artic | 割点剪枝（Tarjan） | **比 v5 慢 3~4 倍** | 负结果 |
| v9-forced | 边层面约束传播当剪枝 | **解不出来** | 负结果，模型本身错 |
| v10-probe | 探针证伪起点 + 按探深排序 | ~137 | 单关有快有慢，见下 |
| v11-parallel | v10 + 起点分片 fork 并行（32 核） | 165 | |
| v12-nodeprobe | v11 + 分支点探针证伪 | **151（比 v11 差）** | 有的关快有的关慢，净效果为负 |
| v13-emitfix | 修 v11 的并行失效 bug | **193** | 见下，一个 bug 值 18 关 |
| v14-parity-start | v13 + 奇偶排除起点 | 193 | 规则正确但奇数关口径下用不上，见下 |
| v15-iddeep | v13 + 起点迭代加深 | **明显更差** | 负结果，L193 7.1s→174s |
| v16-localreach | 全盘洪水填充 → 等价的局部检查 | **213** | 常数 4~5 倍，剪枝强度不变 |
| v17-propcut | v16 + 约束传播（起点+终点色当种子） | **215** | v9 重做，这次起爆了 |
| v18-propbranch | v17 + 传播搬进搜索当分支剪枝 | **215** | 只在很浅（D=2）划算，深了重演 v12 |
| v19-chainorient | v18 + 有向层（链定向 + 首步滑行） | 215 | 规则 sound 且有效，但只涨偶数关号 |
| v20-probing | v19 + probing（failed literal） | **221** | 必用边 25%→35%，难关数量级改善 |
| v20 + PROBE=8000 | 只改探针预算这一个数字 | **237** | 老默认值 2000 是 v13 时代的结论，过期了 |
| v21-tiered | 分层探针（小预算→大预算→无限搜） | **221（更差）** | 只赚尾部赔中盘，见下 |
| v22-portfolio | 28 核分 4 档预算，每档覆盖全部起点 | **明显更差** | 起点要多等 4 倍，见下 |
| v23-adaptive | 只在第一层存活率高时才上第二层 | **237** | 同分但更稳：最慢关 22.1s，L239 333s→42.5s |
| v24-bridge | v23 + 动态桥规则（桥必被使用） | 237（无变化） | 负结果，但量出了病根，见下 |
| v25-chainpin | 链定向的**进入方向**接进 DFS 当剪枝 | — | 有 pins 过期 bug，被 v26 取代 |
| v26-pinout | v25 + **出方向** + 修 pins 过期 bug | **与 v23 打平** | 有信息换不来时间，见下 |
| v27-pinprop | v26 + 在 in 层跑互斥约束的不动点 | 无增益 | 负结果，原因见下 |
| v28-pairprobe | 同格**配对** probing，专门产出「禁用」 | 判定 +1.7% | 小幅正向，见下 |
| v29-equiv | 等价推理（边间 EQUAL/OPPOSITE，带奇偶并查集） | 0 条推论 | 负结果，原因见下 |
| **v30-flow** | **二部度约束流 + 残量图 SCC（GAC）** | **判定率 +5~12 点** | **正向，难关明显变快** |
| v31-subtour | v30 + 连通性加强（连通块跨边 >= 2） | 0 条推论 | 负结果，见下 |
| **v32-endfilter** | **全局端点过滤（用流砍起点候选）** | **起点砍 30~500 倍** | **本轮最大提速** |
| v33-globalfix | 全局阶段滚不动点 + 全局边推论当基线 | 同 v32 | 迭代无增益，但量出了瓶颈所在 |
| v34-pairblock | 分治：块对联合假设 | 淘汰 0 格 | 负结果，但改掉了下一步方向 |
| **v35-costtier** | **按规则代价分层：传播 → 流 → probing** | **瓶颈关快 2 倍** | 纯工程优化，立竿见影 |
| v37-sccdiag | 残量图 SCC 分布 + 打碎实验（诊断用） | — | 查清过滤失效的一半机制，见下 |
| v38-profile | 分层计时（PROF=1） | — | 量出 probing 占 89% 时间证伪 0 |
| **v39-chainfirst** | **把链定向提到 probing 前面** | **L223 快 3.6 倍** | 排序原则改成「单位代价的证伪量」 |
| **v40-noprobe** | **去掉 probing + PROBE 回调 2000** | **255**（v35 是 235） | 同口径 +20 关，见下 |
| v41-chainstat | 链规模统计（诊断用） | — | 证伪了「链太短」的猜想 |
| v42-crosschain | 跨链偏序+环检测（新底座重做） | 打平 | 负结果：前提「链已定向」在瓶颈关不成立 |
| v43-endprune | 终点候选剪枝（候选仅 ~40 格） | 略慢 | 负结果：轮不到它触发；候选信息本身有价值 |
| **v44-tcdead** | **死端必须是终点候选** | **L287 5.8×** | tcand 的正确用法：看身份不是数数量 |
| **v45-swarm** | **swarm：候选极少时全员同攻 + 策略梯队** | **L319 271s→45s** | 起点彩票赢了之后的新瓶颈：单树深搜 |
| **v45.1 (BFS)** | **reach_local 洪水改 BFS 序** | **L319 41s→10s** | 一行改动 4 倍；gprof 实测它占 95.7% 时间 |

### ⭐⭐ 全量口径（奇+偶）600 秒：**318**（2026-08-21，v32）

    L314 PASS 15.9s  L315 PASS 0.42s  L316 PASS 5.0s  L317 PASS 5.8s  L318 PASS 6.2s
    L319 (110x108) TIMEOUT

**从 v23 的 288 涨到 318，+30 关。** 而且 L308~L318 大多只要 **0.3~6 秒** ——
全局端点过滤把绝大多数关变成瞬秒，卡住的 L319 正是过滤不动的那一类。

对比：上游 results.md 最高 264（kimi 2.6）；公开代码最高 ericgopak ~332（但它跑 L165 要 2 小时）。
**我们现在 318，已经逼近公开代码的最高水平，且中盘快两个数量级。**

### 全量口径 600 秒：288（v23，作为对照保留）

`evaluate.py` 只跑奇数关，中间任何一关偶数关卡住都测不出来，所以那个数字偏乐观。
用 `evaluate_full.py`（奇+偶）正式复跑：

    L286 (99x97) PASS 0.72s   L287 (98x98) PASS 269.4s   L288 (99x98) PASS 206.3s
    L289 (100x98) TIMEOUT (567.2s)

**288，比奇数关口径的 287 还高一关** —— 因为 L288 也过了。
**L272~L288 连续 17 关全过，没有任何一关偶数关挡路**，说明奇数关口径这次没有虚高。

**288 > 上游 results.md 里的最高分 264（kimi 2.6）**，也高于表内所有模型（47～264）。

### 600 秒奇数关口径：287（同一版本，作为对照保留）

一直用 30 秒当快速代理指标，但官方榜和 results.md 全是 **600 秒/关**。补跑之后：

    L287 (98x98) PASS 220.70s     L289 (100x98) TIMEOUT (572.79s)

**287 > 上游 results.md 里的最高分 264（kimi 2.6）**，也高于表内所有模型（47～264）。
路上最慢的几关：L287 220.7s、L273 110.9s、L271 86.0s、L257 73.6s——
说明 600 秒的余量还没吃满，瓶颈仍是个别关的方差而不是整体速度。

参考：上游 results.md 各家模型 47～117，最高 kimi 2.6 的 264。

## 已证实的结构（脚本可复现）

### 1. 换数学空间的路是死的 —— `ham_vs_coil.py`

空盘上：3×3 有 40 条哈密顿路径 / 16 个 Coil 解；4×4 是 552 / 32；5×5 是 8,648 / **32**。
**Coil 解数恒为 32，哈密顿路径指数爆炸。** 因为 Coil = 哈密顿路径 + **纯时序**的前缀阻挡条件
（转向时前方必须已经走过），插头 DP / 完美匹配这些方法的状态里没有「谁先谁后」，
构造完再过滤的比例趋于 0。另：Coil 解**不是时间对称**的，同一条路反着走通常不合法。

### 2. 关卡是照着有解构造的 —— `board_stats.py`

无任何度 0/度 1 的自由格；**墙在两种染色上的数量完全相等**（L1101 是 144585/144585），
正是哈密顿路径需要的奇偶平衡；墙密度稳定 33%，永远单连通。

### 3. 解极少，常常唯一 —— `count_solutions.c`

L99（859 格）**1 个**有向解；L139（1604 格）**1 个**；L171（2352 格）10 个。
全树节点也不大（L139 全枚举 1685 万）。据此砍掉 v2~v4 的随机重启 → v5。

### 4. 87% 的决策点只有一个候选 —— `branching.py`

沿真解走，L99 是 401/462 步只有 1 个候选能活过剪枝，L139 是 726/840。三选一几乎不存在。
路径几乎被逼死，偶尔一个二选一。

## 三条走不通的路（都花了实现代价，记下来别再踩）

### 束搜索（v6/v7）：beam 走空

「87% 是强制」是**沿真解**的统计，不是整棵树的。整棵剪枝后的树每层约有 2 万个活状态
（L139：1685 万节点 / 840 层），K=12288 都留不住正确的那个，启发式（Warnsdorff）区分度太低。
v7 修掉了 v6 的两个真 bug（播种只扫左上角、拿不同进度的状态互相排序），L171 从 91s→7s，
但 L139/L201 仍然走空。**结论：状态数不是瓶颈在宽度，是启发式没有信息量。**

### 割点剪枝（v8）：理论强，实测慢 3~4 倍

规则本身是对的：路径过一个格子只用 2 条边 → 割点切出 ≥3 块必然无解；当前位置是端点只用 1 条边
→ 它是割点就无解。用一次 Tarjan 就能拿到，和洪水填充同阶。但这些盘面最小度为 2、连通性极好，
**这种割点几乎不出现**，白付了 Tarjan 的常数（L137 12.9s→43s，L139 44s→超时）。

### 边层面约束传播（v9）：~~模型本身就错~~ → **是我的建模错了，方法本身是通关者用的**

> **2026-08-20 订正**：调研发现第一个通关全部 1206 关的人（Tron）用的**正是边约束传播**，
> 而且是"先把搜索几乎消灭掉"——预处理后只剩 0.3% 的格子不确定用哪两条边，最后才跑朴素搜索，
> 2000×2000 用时 37 分钟。我错在两处：(1) 建成了**环模型**（每格 2 条边），
> 而 Coil 是**路径**，2 个端点只用 1 条边，所以必然自相矛盾；
> (2) 他消除的对象是**有向的「从某侧进入某格」**，比无向边细，正好能装下 Coil 的时序约束。
> **这条路要重做，见 research-notes.md 第一节。** 下面是当时的失败记录，保留备查。

传播规则要靠「每格恰好用 2 条边」才能启动（度 2 的格子锁死两条边）。但真解是**路径**，
两个端点只用 1 条边。抽查 10 关，**有 6 关（L11/51/71/99/201/251）的环模型直接自相矛盾**
——它们有哈密顿路径但没有哈密顿圈。所以之前测出的「54% 边被定死」只在恰好没矛盾的关上成立，
拿去当剪枝会把真解剪掉（v9 在 L11/L51/L99 上直接无解）。
用虚拟 ∞ 点把路径转成圈是正确建模，但那样每格多一条候选边，度 2 规则再也启动不了，传播退化成空转。

## v10/v11：终于打动了瓶颈

### v10：用「证明无解」来筛起点

给每个起点一个小预算搜一下，**子树被搜穷仍无解 = 这个起点被严格证伪**，可以永久剔除
（不是启发式，是证明）。实测证伪率：L137 砍掉 1183/1529 = **77%**，L99 更极端——859 个起点
只剩真起点 1 个。幸存者再按探针探到的最深处排序。

单关效果有快有慢（L137 14.3s→4.1s，L139 43.7s→24.0s，但 L171 11.1s→26.3s），
因为探针本身要花 n×预算个节点，而排序质量不稳定。对照实验：关掉排序更慢
（L137 4.1s→23.5s），说明排序确实有信息量。

### v11：起点分片并行 —— 32 核直接打在彩票上

「解唯一 ⇒ 起点唯一，但要试 n 个起点」这件事天然并行，起点之间完全独立。
用 fork 而不是线程：所有工作数组都是全局的，fork 之后每个子进程自带一份副本，零重构。
子进程 i 只处理下标 ≡ i (mod P) 的起点，谁先解出就把答案写进管道退出，父进程收第一个成功的。

| 关 | v10（单核） | v11（32 核） |
| --- | --- | --- |
| L139 | 48.0s | 11.3s |
| L153 | 超时 | 36.4s |
| L157 | 28.2s | 3.8s |
| L165 | 42.3s | 3.8s |
| L171 | 25.7s | 5.5s |

**30 秒超时下的成绩：137 → 165**（L167 超时）。

## 关于「能不能证明有解 / 无解」

- **证明无解 = 剪枝**，我所有剪枝都是这个（奇偶、连通、死端、割点）。哈密顿路径 NP 完全，
  所以不存在又便宜又完备的判定，只能不断叠必要条件，且每条都要算代价（v8 就是规则对但不划算）。
- **证明有解而不构造**：理论上可行——没有洞的实心网格区域上哈密顿路径是多项式的
  （Umans–Lenhart 1997），而 Coil 在空矩形上解就是螺旋（已实测）。所以剩余区域一旦变成实心矩形
  就能直接构造完、整棵子树免搜。但这批盘面墙占 33% 且均匀散布，剩余区域里永远夹着墙，
  **几乎不会触发**，只有最后几格才可能。
- 真正见效的是「证明无解」的实用形态：v10 的探针证伪。

## 规模天花板：搜索路线到不了 1000 关（算得出来）

用全枚举节点数拟合：L51(294格)=4.5万，L139(1604格)=1685万，L171(2352格)=1042万
→ 节点数 ≈ e^(0.0027 × 自由格)。代进去：

| 关 | 自由格 | 估计节点数 | 可行性 |
| --- | --- | --- | --- |
| L301 | 7,161 | 2×10⁸ | 还能算 |
| L401 | ~12,600 | 4×10¹⁴ | 几个月 |
| L1001 | ~82,000 | e²¹⁹ | 荒谬 |

**加机器几乎买不到关**：算力翻 1000 倍 = 多 2,580 个格子 ≈ **多 40 关**。
32 核那次买到 +28 关，已经是这条路的红利上限。搜索路线天花板 ≈ **L350~450**。

## 贪心/构造路线也被否掉了 —— `greedy_depth.c`

想法是：既然搜索到不了大盘，就贪心走 + 局部修补。实测贪心（Warnsdorff，不回溯）能覆盖多少：

| 关 | 自由格 | 贪心最好覆盖 |
| --- | --- | --- |
| L99 | 859 | 201 (23%) |
| L501 | 19,232 | 362 (1.9%) |
| L1101 | 575,730 | **444 (0.08%)** |

关键在绝对值：**不管盘子多大，贪心都在 200~450 格处卡死**。L1101 要修补 1400 次，每次都可能连锁。
「贪心 + 局部修补」不成立。路径是根针，走不出来，只能搜出来。

## v12：把探针证伪用到分支点上（有效）

v10 的探针只用在起点。推广到搜索内部：**在浅层分支点上，给每个候选跑一次限预算探针，
被搜穷仍无解的候选当场证伪剔除** —— 二选一直接塌成强制走。
只在前 14 个决策点做，因为早期一步走错的代价是整棵子树，花小钱买大保险。

**踩了个大坑，值得记下来**：第一版 v12 有 bug——`-1`（预算用尽）这条返回路径**不撤销已走的格子**。
v5 里这没事，因为每换一个起点都会 `memcpy` 重置盘面；但 v12 把 `-1` 当探针结果用在搜索**内部**，
返回后还要接着搜，盘面就脏了。更坑的是我当时只用 `/usr/bin/time` 量时间没看输出，
**把「失败用了 12 秒」读成了「12 秒解出」**，得出一张全错的对比表。
教训：任何性能对比都必须先确认输出非空。

修好之后的真实数据（都确认有输出）：

| 关 | v11 | v12（已修） |
| --- | --- | --- |
| L139 | 20.5s | 20.4s |
| L153 | 超时 | 56.8s |
| L167 | 超时 | 56.9s |
| L171 | 17.8s | **4.5s** |
| L201 | 29.0s | 46.4s |

正式评测结果：**v12 = 151，反而不如 v11 的 165**。分支点探针在个别关上很值（L171 从 17.8s 到 4.5s），
但在它帮不上忙的关上白花钱（L201 从 29s 涨到 46s），30 秒这个阈值下净效果是负的。

**所以目前最好的仍是 v11（165）。** 分支点探针的想法本身是对的（证伪是可靠的），
问题在于「什么时候值得花这个钱」没有判据——下次该试的是**自适应**：
只在真正卡住（某个候选反复失败）时才启动探针，而不是所有浅层分支点无差别地做。

## 网上调研（完整版见 research-notes.md）

**头号发现：我把 v9 判死是错的。** 第一个通关 1206 关的 Tron 用的正是边约束传播，
2000×2000 用时 37 分钟、4500 行 C。我错在建成环模型（Coil 是路径，2 个端点只用 1 条边），
且他消除的是**有向的「从某侧进入某格」**，比无向边细，能装下时序约束。这条路要重做。

第二条有硬证据的路：**瓶颈分区**（ericgopak/Mortal-Coil，仓库里 committed 了到第 332 关的解）。
切割判据只有 5 行，每个区域的穿越方式带 must-be-blocked/must-be-free 位掩码，主搜索只在区域层面走。
**但要注意它只改善尾部**：它跑我们 30 秒能过的 L165 要 7322 秒（2 小时）。

**零穿越解的区域必含起点或终点** —— 这个比"度 1 端点"实用得多（我们的盘面根本没有度 1 格），
直接打我们最大的瓶颈（起点彩票）。

独立验证了我们三个测量：撞墙后 80%+ 没有选择（我们 87%）、大盘通常唯一解、盘面是构造出来的
（生成器源码可查：先随机走一条 coil，再在线段层面 tweak，没走到的格子变墙）。

别人踩过的坑：置换表不划算（重复模式太罕见）、PSO 4 小时只到 32 关、Hierholzer 被否。

另：公开的 16 个 GitHub 仓库最高只到 332 关，绝大多数在 100 关左右——
**本仓库 v11 的 165 已超过其中除一个之外的全部**。

## 现在的判断

分数卡在 137~139，四个方向（更强剪枝、随机重启、束搜索、约束传播）都试过了。
剩下的真瓶颈是**起点彩票**：解通常唯一，起点也就唯一，而候选起点有 n 个，
系统搜索平均要浪费 n/2 个起点的功夫。v5 在 L251 只要 15s 却在 L201 要 64s，
这种非单调正是起点顺序的运气造成的，跟盘面难度无关。

下一步应该打这个点，而不是继续加剪枝：
- [ ] 给每个起点跑一个极廉价的贪心（不回溯，走到卡住为止），按「走了多远」给起点排序
- [ ] 或者反过来找**终点**：终点是路径唯一能停下的地方，约束比起点强
- [ ] 起点/终点定位对了，理论上能省掉 n/2 的常数——这是目前唯一还没吃到的数量级


---

# 2026-08-20 续：先修 bug，再谈算法

## 环境重建（换机器后）

原来的 `/mnt/d/works/coilbench` 没了，重新拉了一份：

- `git clone https://github.com/adum/coilbench.git` 到 coil-solver 的同级目录，`bench` 改成默认找同级。
- **关卡包不需要口令**：`download_full_levels.sh` 里的口令只用于把偶数关加密存档，
  关卡本体在公开 release 里直接下载 —— `releases/download/data-v1.0/boards.zip`（47MB，1207 关）。
  解开后奇数关放 `levels_public/`（`evaluate.py` 用），全部 1207 关放 `levels_all/`（单关调试用）。
- `bench` 的编译参数从 `-std=c99` 改成 `-std=gnu99`：gcc 15 下 c99 会藏掉 POSIX 声明，
  `kill()` 变成隐式声明而隐式声明在新版是**错误**不是警告，v11/v12 直接编不过。
- coilbench 要 `git config core.autocrlf false` 重新 checkout，否则 CRLF 让 WSL 里
  `#!/usr/bin/env python3` 变成 `python3
`，evaluate.py 起不来。

换机器后 CPU 是 28 核（原来 32）。**v11 在这台机器上的基线是 175**（原记录 165，L177 超时）。

## v13：一个 bug 值 18 关 —— v11 的并行其实一直是废的

v11 写了 `emit()`，注释写明「找到解就写进管道（子进程）或直接打印（单进程模式）」，
父进程也照着这个契约写了「收到第一个成功的就打印并杀掉其余」。**但 emit 一次都没被调用** ——
两处解出的地方走的都是 `printf` + `return 0`。编译警告 `'emit' defined but not used` 一直在，
被淹没在另外两条警告里没人看。

后果不是「答案错」而是「并行白做」：

- 子进程的 stdout 是从父进程继承来的，`printf` 的答案确实进了最终 stdout，所以**校验一直是过的**；
- 父进程从管道读到 0 字节，于是走「这个孩子没答案」的分支，**不 kill 其余 27 个子进程**；
- `evaluate.py` 用 `subprocess.run(capture_output=True)`，它要等**所有持有 stdout 写端的进程**
  退出才返回。

所以 v11 的实测耗时从来不是「最快的核找到解的时间」，而是**「最慢的那个 shard 把自己那片起点
全部搜穷的时间」**。28 核并行的收益基本被这一个 bug 吃光了。

修法三处：(a) 两处解出改走 `emit()`；(b) 子进程 fork 后关掉**所有**不属于自己的管道端——
管道是 fork 之前全建好的，0 号孩子攥着其他所有管道的写端，父进程永远等不到那些管道的 EOF；
(c) 父进程从 `wait()` 改成 `poll()` —— 大盘的解可能撑爆管道 64KB 缓冲，孩子会阻塞在 `write`
上等人来读，而 `wait()` 里的父进程要等它退出才读，那就互相等死了。

| 关 | v11 | v13 |
| --- | --- | --- |
| L139 | 5.13s | 1.31s |
| L153 | 14.43s | 5.89s |
| L167 | 28.71s | 1.25s |
| L173 | 8.21s | 0.92s |
| L177 | 172.4s | 8.34s |

**30 秒成绩：175 → 193**（L195 超时）。

顺带发现 v11 的输出本身也是脏的：L167 的 stdout 有 13612 字节 —— 多个子进程各自把自己找到的
答案打进了同一个 stdout。`check` 只看第一行所以一直没暴露。v13 每次只有一份。

**教训**：这个仓库前面记过一次「任何性能对比都必须先确认输出非空」。这次要补一条更强的：
**并行程序的性能对比，必须确认「赢家退出后其余进程真的死了」**。
`wc -c` 输出字节数、`ps` 看残留进程，两条都该是例行动作。

## v14：奇偶排除起点 —— 规则是对的，但这个评测口径下用不上

路径在棋盘两色间严格交替。自由格共 n 个时，n 为奇数 ⇒ 路径位置 0..n-1 里偶数位同色，
两个端点（位置 0 和 n-1，都是偶数位）**必然同色，且是多数色**。少数色的格子当场全部排除。
在 11 个已知真解上验证通过，L99 的起点候选 859 → 430。

但是：**1207 关里只有 335 关是奇面积，而 150 关之后这些全是偶数关号**
（尺寸序列是 (a,a) → (a+2,a) → (a+2,a+1) → (a+2,a+2)，w·h 到了大盘基本恒为偶数）。
`evaluate.py` 只跑奇数关，所以这条规则在快速评测口径下一分不涨。
跑 `evaluate_full.py`（奇+偶全量）时它对约一半的关有效，留着。

## v15：起点迭代加深 —— 负结果

想法来自上一轮的「下次该试自适应」：把 v13 的两段式（固定 2000 节点探针 → 幸存者逐个无限搜）
换成轮次预算 B, 2B, 4B…，每轮给每个存活起点 B 个节点，搜穷就永久剔除。
几何增长保证重复劳动 ≤ 2 倍，且没有任何单个起点能把本片焊死。

实测全面更差：

| 关 | v13 | v15 |
| --- | --- | --- |
| L139 | 1.32s | 5.44s |
| L177 | 8.22s | 21.22s |
| L193 | 7.13s | **174.25s** |
| L195 | 52.44s | 72.70s |
| L197 | 99.24s | 201.71s |

**原因**：v13 的「无限搜」对错误起点不是浪费而是**投资** —— 一个错起点被搜穷就永久剔除，
代价是它自己那棵（因为无解所以被剪得很小的）子树。迭代加深把这个一次性投资拆成了
「每轮都花预算但都不够搜穷」，同一棵子树被反复重搜十几遍才终于被剔除。
几何增长的 2 倍上界只在「最后一轮就是决定性的那轮」时成立，这里不成立。

**结论**：起点这一层上，「一次性搜穷 + 永久剔除」优于「逐步加深」。
notes 里那条「下次该试自适应」到此证伪。

## v16：把每节点的全盘洪水填充换成等价的局部检查 —— 193 → 213

v13 的每个搜索节点，对每个候选方向都做一次 `reach_ok`：从落点洪水填充整个剩余区域，
数够不够 `remaining` 个。这是最强的一条剪枝，但也是 O(剩余格数)——L195 开局就是三千格，
一个节点最多四个候选，光这一项就是每节点上万次访存。**它可以严格等价地做成局部的。**

进入某节点时剩余区域 R 已知连通（上一层验过），本次滑行吃掉的只是一条直线 S，
新剩余 R' = R \ S。R' 要被切开，切口必然紧贴 S；又因为 R 连通，R' 里任何一点都能在 R' 内
走到某个「S 的自由邻居」（它到 S-邻居的 R-路径若碰到 S，进入 S 之前的最后一点就是个 S-邻居）。
于是

    R' 连通  ⟺  S 的自由邻居两两连通

这些邻居一旦连上就能立刻收工，不用把剩余区域数完。滑行终点 c 也在 S 里，它的自由邻居同属
这个集合，所以「从 c 出发能到全部剩余格」一并被保证。**剪枝强度一点没变，只是不再付全价。**

归纳的底座要单独打：起点那一层的剩余区域是「全盘去掉起点」，起点若是割点就不连通了，
所以每个起点开搜前照旧做一次完整 `reach_ok`（顺带白捡一条起点剪枝）。

| 关 | v13 | v16 |
| --- | --- | --- |
| L139 | 0.95s | 0.23s |
| L177 | 6.11s | 1.27s |
| L193 | 5.83s | 1.41s |
| L195 | 52.4s | 9.32s |
| L197 | 99.2s | 17.66s |

**30 秒成绩：193 → 213**（L215 超时）。约 4~5 倍的常数，按 e^(0.0027×自由格) 换算
大致就是 20 关的量级，和实测吻合。

## v17：约束传播终于起爆了 —— 找到了 v9 缺的那个种子

**v9 当年空转的真正原因不是「环模型 vs 路径模型」**（上一轮订正时我以为是），
而是**传播根本没有起爆点**：度 2 规则要「这一格恰好用 2 条边」才能锁死两条边，
可路径有两个端点只用 1 条边，端点是谁又不知道，于是每个格子都得留「我可能是端点」的活口，
谁也锁不死谁。换成虚拟 ∞ 点的圈模型同样卡在这里——每格多一条候选边，度 2 规则更启动不了。

**种子在奇偶上**：路径在棋盘两色间严格交替，所以**起点一旦假定，终点的颜色就被完全确定**

- 自由格数 n 为偶数 ⇒ 起点终点异色 ⇒ 终点色 = 1 − col(起点)
- n 为奇数 ⇒ 起点终点同色 ⇒ 终点色 = col(起点)

于是**一半的格子当场被证明「绝无可能是端点」**，它们里面的度 2 格必须用满两条边，
传播就有了起爆点，再沿强制边连锁下去。而求解器本来就在逐个枚举起点，这个假定是免费的。

`experiments/propagate.c` 先做的可行性验证（无向边 + 度约束 + 不成环，还没上有向层）：

| 关 | 自由格 | 真起点下定死的边 | 全起点证伪率 | 传播速度 |
| --- | --- | --- | --- | --- |
| L99 | 859（奇） | **48.7%**，还直接定出了终点 | **44.2%** | 6.4 万起点/秒 |
| L139 | 1604（偶） | 24.8% | **0%** | 5.0 万起点/秒 |
| L165 | 2252（偶） | 24.6% | **0%** | 3.5 万起点/秒 |
| L175 | 2495（奇） | 26.1% | **31.1%** | 3.5 万起点/秒 |
| L195 | 3028（偶） | — | **0%** | 2.3 万起点/秒 |
| L213 | 3602（偶） | — | **0%** | 1.9 万起点/秒 |

soundness 在 11 个已知真解上验过：真解用到的边一条也没被禁用，被判必用的边真解也都用了。

**读数**：n 为奇数的关证伪率 31~44%，n 为偶数的关**证伪率是 0**——纯结构层推不出任何矛盾。
所以传播**替代不了 DFS 探针**。但它便宜得离谱（每起点 40 微秒，比探针便宜两三个数量级），
而且定死的 25%~49% 的边可以直接拿去剪枝。

v17 就这么用：每个起点开搜前跑一次传播（矛盾就免费证伪掉这个起点），把 `estate` 留着，
在滑行候选生成时卡三条：

- run 上那 len 条边**全都要用**，撞上任何一条禁用边就否掉这个方向；
- run 的中间格是直穿的（两条边都沿滑行方向），所以它的两条**垂直边一条都不用**，
  那里若有必用边就矛盾；
- 落点 c 处路径要么转弯要么收尾，反正不会再往前一格，所以边 (c, c+dd) 不用，它若是必用边就矛盾。
  （c+dd 此刻要么是墙，要么是更早访问过的格子；后者也不可能与 c 在路径上相邻，
  因为 c 的前驱已经是 c−dd 了。）

传播出来的东西**整局有效**——它说的是最终解的性质，跟搜索走到哪一步无关，所以只跑一次。

| 关 | v16 | v17 |
| --- | --- | --- |
| L139 | 0.20s | 0.21s |
| L177 | 1.22s | **0.19s** |
| L193 | 1.28s | 1.76s |
| L195 | 9.10s | 6.62s |
| L197 | 16.82s | **1.62s** |
| L213 | 1.57s | 1.50s |

**30 秒成绩：213 → 215**（L215 从 v16 的超时变成 15.8s 通过，L217 超时）。
个别关是数量级的改善，但不均匀——涨的关（L193）也有。

## 现在的判断（更新）

- 175 → 215，四步：**修一个并行 bug（+18）→ 局部连通性（+20）→ 传播剪枝（+2）**。
  前两步都不是算法创新，是把已有的东西做对；这条经验值得记住：
  在上新算法之前，先确认旧算法真的在按设计跑。
- 传播这条路**方向确认是对的**（起爆点找到了，soundness 干净），但目前只有纯结构层，
  偶数关证伪率为 0。要拿到 Tron 那种「预处理后只剩 0.3% 不确定」的效果，
  必须上**有向层**：把「从某侧进入某格」当变量，才装得下 Coil 的时序约束。
  纯无向的结构层已经证明推不出滑行规则——转弯的合法性取决于「前方那格是不是更早访问」，
  这是纯时序的，无向边模型里没有这个信息。
- 有向层最小可用的一条规则（已推导，未实现）：
  记 T(c,e) = 「沿方向 e 进入 c 之后没有直行」，它蕴含 t(c+e) < t(c)。
  于是相邻的两格**不能互相要求对方更早**：¬(T(c,e) ∧ T(c+e, −e))。
  这条要 in(c) 收敛成单值才能触发，所以得先让无向层把边定死到一定程度——两层是互相喂的。

## v18：把传播搬进搜索内部当分支剪枝 —— 只在很浅的地方划算

v12 试过在浅层分支点上给每个候选跑一次限预算 DFS 探针来证伪，想法是对的（证伪是证明不是启发式），
但败在太贵：一次探针 2000 个节点。传播只要 O(剩余格数)，大约相当于 40 个节点——**便宜五十倍**。
规则跟起点那次传播完全一样，只是作用在剩余区域上：起点 = 当前位置（它已经走过，但在路径上
还欠一条出边，正好当起点用），终点色 = col(当前位置) ^ (剩余格数 & 1)。

先做了 soundness 验证（这次学乖了，动手调性能之前先验）：拿已知真解沿路走一遍，
每个滑行落点上跑一次传播，**真解可行所以传播永远不许报矛盾**，静态那层的 `estate_ok`
也永远不许否掉真解实际走的方向。L99/L139/L165/L175/L193/L195/L209/L215 八关全部零报警
（L209 走了 1856 步）。所以两层传播都是 sound 的。

深度扫描（`PROPDEPTH` = 在前几层做）：

| 关 | v17（不做） | D=1 | D=2 | D=4 | D=12 | D=20 |
| --- | --- | --- | --- | --- | --- | --- |
| L209 | 6.55s | 5.77s | **5.28s** | 85.1s | 70.9s | 56.1s |
| L215 | 16.3s | 14.6s | 12.0s | 13.1s | **1.27s** | 1.18s |
| L217 | 59.0s | 55.5s | 54.7s | 64.1s | 57.7s | 38.7s |

**D=2 是甜点**，三关都不比 v17 差。往深了做就重演 v12 的老毛病：有的关数量级地赢
（L215 16.3s → 1.27s），有的关数量级地输（L209 6.55s → 85s）。
注意 L209 那次变慢**不是 bug**（soundness 验过了），是并行下「谁先解出」的竞态被改变了——
剪掉候选本身只会让单个 shard 更快，但它改变了各 shard 的推进节奏，赢家换了人。
**这类非单调性是起点彩票的固有噪声，不是可以靠调参消掉的东西。**

## 有向层：链定向 —— sound、有效，但正好卡在评测口径的盲区

无向层只能说「这条边一定被用」，说不出方向。但必用边天然拼成一条条**链**（每格最多挂 2 条），
而且**每条链都是路径的一个连续段**，所以：

1. **链一旦定向，链内所有格子的先后顺序就完全确定了**。于是 Coil 的滑行规则第一次能被判定：
   链上某格拐弯 ⇒ 前方那格必须是墙、或者更早被访问。**前方那格若也在本链上而且位置更晚 ⇒
   这个定向不可能**。两个方向都被排除 ⇒ 起点矛盾。
2. **含起点的那条链是全序里的第一段**，占据路径位置 0..k−1，所以任何不在它上面的格子位置都 ≥ k，
   绝不可能更早。于是这条链上拐弯点的前方格只剩两种合法可能：是墙，或就在本链上且位置更早。
   落在别的链上、或落在未定区域 —— 一律矛盾。（严格版，威力比第 1 条大得多）
3. **起点的第一次滑行是在空盘面上做的**，撞不到任何已访问格，所以必然一路滑到墙。
   那整条线因此就是路径的开头：线上每条边都要被用（撞上禁用边 ⇒ 这个方向不可能），
   线上的中间格是直穿的（有垂直的必用边 ⇒ 矛盾）。四个方向全不可行 ⇒ 起点证伪；
   只剩一个方向 ⇒ 整条线当场定死，回灌传播再滚一轮。

三条都在 5 个已知真解上验过 soundness（真起点一次都没被误杀）。证伪率：

| 关 | 自由格 | 只有无向层 | + 链定向 + 首步 |
| --- | --- | --- | --- |
| L99 | 859（奇） | 44.2% | **58.4%** |
| L175 | 2495（奇） | 31.1% | **40.8%** |
| L153/L161/L199（奇） | — | 34~37% | 类似量级 |
| L139 | 1604（偶） | 0% | 0.2% |
| L165 | 2252（偶） | 0% | 0.8% |
| L195 | 3028（偶） | 0% | 0.1% |
| L213 | 3602（偶） | 0% | 0.4% |

## 为什么偶数关这么顽固 —— 本轮最有价值的一条诊断

一开始以为是规模效应，不是。L51（294 格）证伪 97.6%，L199（3247 格，奇）还有 36.1%，
而 L101（894 格，偶）就是 0%。**决定性的变量是自由格数 n 的奇偶**，原因是：

- **n 为奇数**：两色数量差 1，**起点和终点同色**（都在多数色上）。少数色的格子全部 `[2,2]`，
  多数色的格子 `[1,2]`（可能是终点）。假定起点 s ⇒ s 从 `[1,2]` 变成 `[1,1]` —— 这是**收紧**。
- **n 为偶数**：两色数量相等，**起点和终点异色**。起点色的格子本该全部 `[2,2]`，
  假定起点 s ⇒ s 从 `[2,2]` 变成 `[1,1]` —— 这是**放松**。

**放松一个约束永远推不出矛盾。** 所以偶数关里，「假定起点 s」这件事对传播几乎不提供任何信息：
对同色的所有 s，约束集只差那一格，基础约束不矛盾则全都不矛盾——证伪率必然趋近 0。
奇数关反过来，每个起点假设都是一次真正的收紧，所以能证伪三四成。

试过两条补救，都记下来：

- **终点候选的基数传播**（终点只有一个；挂满 2 条边的格子不可能是终点，候选缩到 1 就定死终点，
  终点色那半边立刻塌成 `[2,2]`）：写了，**一次都没触发**。终点候选有 n/2 ≈ 1500 个，
  而传播只定死 25% 的边，离把候选逼到 1 差得太远。
- **首步滑行推理**（上面第 3 条，是唯一让「起点的位置」真正参与收紧的入口）：
  把偶数关从 0% 抬到 0.1%~0.8%。方向唯一的情况几乎不出现，所以定死的边数一点没变。

**而且这条路正好卡在评测口径的盲区上**：`evaluate.py` 只跑奇数**关号**，而 150 关之后
奇数关号的盘面尺寸序列是 (a,a)→(a+2,a)→(a+2,a+1)→(a+2,a+2)，w·h 恒为偶数，
自由格数也就恒为偶数。**所以有向层这三条规则涨的全是评测跑不到的关。**
跑 `evaluate_full.py`（奇+偶全量）时它们对一半的关有效，留着。

## 下一步该往哪走（更新）

有向层的**静态**版本到此为止：三条规则都 sound，但在偶数 n 的大盘上威力有限，
根子是上面那条奇偶不对称。要真正复刻 Tron 的效果（预处理后只剩 0.3% 不确定），
缺的不是更多规则，而是**换一种用法**：

- **Tron 应该不是逐个枚举起点的**。他说的 recursive patterns + bounded brute force
  本质上是 **probing / failed-literal**：假设「从某侧进入某格」，传播，矛盾就把这个假设消掉，
  然后拿消掉的结果再 probing 一轮，直到不动点。这不需要起点当种子——**probing 自己造种子**。
- 代价 O(4n) 个假设 × O(n) 传播 = O(n²)。n=3000 大约 10⁷ 次格子处理，
  一次性 0.1~1 秒，**完全付得起**（我们现在整关才几秒）。关键是它只跑一次，
  而不是每个起点跑一次（那就是 O(n³)，付不起）。
- 但不假定起点的基础模型太松（每格 `[1,2]`，端点是谁都不知道），probing 会推不出东西。
  补法是加**端点数 ≤ 2 的基数约束**：只用 1 条边的格子最多 2 个，超了就矛盾。
  这条在无起点模型下是有牙齿的，而且是 probing 能咬住的东西。
- 所以下一步的具体形态是：**不假定起点的全局传播（端点数 ≤ 2）+ 边/进入方向的 probing +
  迭代到不动点**，跑一次，把结果当成所有起点共享的先验，再进现在的枚举-搜索框架。

## v20：probing（failed literal）—— 215 → 221

Tron 说的 recursive patterns + bounded brute force，本质就是 probing：对一条还没定的边试着假设
「它必用」，传播一遍；矛盾的话这个假设就被证伪，那条边只能是禁用。反过来同理。
矛盾是**证明**不是启发式，所以 sound。反复滚到不动点。

先在 `experiments/probe.c` 里量了三件事，结论一正两负：

**（负一）不假定起点的全局 probing 是死的。** 本来最想要的是「probing 自己造种子，
不需要起点当锚，只跑一次，结果对所有起点共享」——那样代价就从 O(n³) 降到 O(n²)。
实测：不假定起点的基础模型（每色恰好一个端点、端点数基数约束）在偶数关**定死 0 条边**，
probing 无从下嘴（每格都还可能是端点、都是 `[1,2]`，任何单边假设都能被「那我是端点」消化掉）。
奇数关倒是可以：L99 全局传播 28.8% → probing 后 **41.5%**，因为奇数关少数色的格子天生就是 `[2,2]`。

**（负二）probing 一个起点都证伪不了。** 在「假定起点」的基础上 probing，
L139/L165 的全部候选起点跑下来，**传播 0、probing 0、链定向 0**，三层叠加合计 0.0%。
这就把上一节那个奇偶不对称的结论钉死了：**偶数关的起点没法用约束传播证伪**。
根子是纯哈密顿路径约束太弱——哈密顿路径的数量是指数级的（`ham_vs_coil.py` 早就测过：
5×5 空盘 8648 条哈密顿路径 vs 32 个 Coil 解），每个起点在度约束下都真的可行。
Coil 的滑行约束只能经由「定向」进场，而定向要靠必用边，即使 probing 把必用边推到 35%，
链定向还是抓不到矛盾。**起点证伪只能靠搜索（DFS 探针），传播干不了这活。**

**（正）但 probing 当剪枝很值。** 在假定起点的基础上 probing 一轮：

| 关 | 传播 | + probing | 单起点耗时 |
| --- | --- | --- | --- |
| L139 | 24.8% | **35.0%** | 0.02s |
| L165 | 24.6% | **33.1%** | 0.03s |
| L195 | 26.9% | **37.8%** | 0.05s |

soundness 用真解的边对照过，必用/禁用没有一条跟真解冲突。

接进求解器时成本是个真问题：L195 一个 shard 有 108 个起点，每个 0.05 秒就是 5.4 秒，
比整关的时间还长。所以分两级：

- **正式搜索阶段**的起点一律做 probing —— 探针那一段已经筛掉大部分，正式搜索通常头几个起点
  就出解，多花的 0.05 秒相对于动辄几秒的搜索是划算的。
- **探针阶段**做不做由 `PROBEALL` 控制。实测这是个漂亮的权衡：

| 关 | 探针阶段不做 | 探针阶段也做 |
| --- | --- | --- |
| L195 | 6.25s | 6.74s |
| L209 | **1.04s** | 5.20s |
| L215 | **1.11s** | 2.05s |
| L217 | 42.9s | **19.7s** |
| L219 | **3.63s** | 3.91s |

易关小输、难关大赢。而评测只看「每关有没有在 30 秒内做完」，把最难的关拉进门槛比让易关
从 1 秒变 0.3 秒值钱得多，所以默认打开。

**30 秒成绩：215 → 221**（L223 超时）。111 关全过校验器，零 FAIL，最慢的 L217 用 19.7 秒。

顺带记一个对比：v19 → v20 在几个难关上是数量级的改善（L209 5.5s → 1.0s，L215 11.6s → 1.1s，
在 PROBEALL 关掉时；打开后分别是 5.3s / 2.1s，但换来 L217 能过）。

## 招数清单（到 v20 为止）

**搜索层**：滑动 DFS ｜ Warnsdorff 排序 ｜ 奇偶染色剪枝（O(1) 增量）｜ 死端/孤格剪枝（O(1) 增量）
｜ 连通性剪枝（v16 起是局部等价版）｜ 起点按度数排序 ｜ 起点探针证伪（搜穷=证明）
｜ 起点分片 fork 并行 + 赢家写管道 + kill 其余

**传播层**：无向边度约束 + 不成环 ｜ 起点+两色交替⇒终点色确定（起爆种子）｜ 端点数基数约束
｜ 定死的边剪滑行候选 ｜ 传播进搜索当浅层分支剪枝（D=2）｜ 必用边链的定向 ｜ 含起点链的严格版
｜ 首步滑行必须撞墙 ｜ probing / failed literal

**已否决**：Luby 随机重启 ｜ 束搜索 / 分层束搜索 ｜ 割点剪枝（Tarjan）｜ 环模型边传播
｜ 分支点 DFS 探针（太贵）｜ 起点迭代加深 ｜ 探针预算调大 ｜ 贪心 + 局部修补
｜ 终点候选基数传播（没触发）｜ 不假定起点的全局 probing（0 条边）

## 还没用的招数（按我判断的性价比排）

1. **跨链的时序偏序 + 环检测**（本轮推导出来但没实现，最便宜）。链是路径的连续段，所以链之间
   存在全序。链 Li 上某个拐弯点的前方格若落在链 Lj 上 ⇒ **Lj 整体早于 Li**。把这些偏序边收集
   起来，**有环就是矛盾**。现在的实现只在同一条链内部比较位置，跨链的信息完全扔掉了。
   这是链定向的自然推广，不需要新机制，只要一个偏序图 + 环检测。
2. **把 probing 的对象从「边」换成「有向的进入方向」**。Tron 消除的正是「不会从某侧进入某格」，
   比无向边细一倍，而且**装得下时序**。现在 probing 只试边的用/不用，时序信息进不来。
3. **瓶颈分区 + 每区域解目录**（Mortal-Coil，有 332 关的 committed 实证）。切割判据 5 行，
   每条穿越方式带 must-be-blocked / must-be-free 位掩码。**零穿越解的区域必含端点** ——
   这是唯一一条有实证的、能直接打「起点彩票」的招，而起点证伪已经被证明传播干不了。
   警告：它只改善尾部，中盘反而更慢（同一个仓库跑我们 2 秒能过的 L215 要两小时）。
4. **度 2 链收缩**（Hippo 说这是过 160 关的关键）：搜索单元从「格子」变成「段」。
   配 conditional shortcuts（「假设起点不在本区域」前提下才成立的捷径），他自己警告 bug 多到
   调了一个月，回归测试用**第 88 关**。
5. **障碍接触一致性检查**（Mortal-Coil）：近 O(1)，可当 flood fill 的廉价替身。要先小盘对拍
   验证 soundness——我们现在的局部连通性已经很便宜了，收益可能不大。
6. **Euler-Tour Tree / Top Tree 维护动态连通性**（Hippo 提过，O(log n)）。
7. **按违反约束条数迭代加深**（Hippo，别处没见过任何实现）。
8. **600 秒官方口径**——我们一直用 30 秒当快速代理指标。官方榜和各家模型成绩都是 600 秒，
   L217 只要 20 秒，600 秒口径下现在这版能走得远得多。这不是算法进步，但它是唯一能跟
   官方榜（kimi 2.6 的 264）直接对标的数字。
9. 工作窃取 / 共享起点队列——实测 28 核全程满载，预计收益不大。
10. 2-level probing（同时假设两条边）——O(m²)，多半太贵。

## 跨链时序偏序 —— 负结果，而且原因很本质

链是路径的连续段，所以链之间存在全序；链 Li 上某个拐弯点的前方格若落在链 Lj 上，
就意味着 **Lj 整体早于 Li**。把这些偏序边攒起来，有环就是矛盾。还配了不动点迭代：
某个定向会让图成环，那个定向就被排除，只剩一个定向的链就此定死、它的边永久加入，
又可能让别的链的某个定向成环 —— 滚到不动点。

**实测推不动，一条跨链偏序边都加不进去。** 加了统计才看清原因：

| 关 | 拐弯点总数 | 前方是墙 | 前方在同链 | 前方在别的链 | 前方不在任何链上 |
| --- | --- | --- | --- | --- | --- |
| L99 | 504 | 434 (86%) | 30 | 37 | 3 |
| L139 | 497 | 447 (90%) | 16 | 28 | 6 |
| L165 | 684 | 609 (89%) | 18 | 38 | 19 |
| L195 | 969 | **855 (88%)** | 29 | 69 (7%) | 16 |

**88~90% 的拐弯点前方是墙**，拐弯天然合法，时序约束平凡满足，一点信息都不产生。
这其实是这道题的本质：滑行本来就是撞墙才停的，33% 墙密度下绝大多数拐弯当然贴着墙。
剩下 7% 的跨链引用又全部落在**还没定向**的链上，而定向要靠加边、加边要靠定向 —— 死结。

**结论：有向层的时序推理在这批盘面上信息量本身就不够，不是实现问题。**
连同上一节的「偶数关起点证伪率 0%」，可以合起来给这条路盖棺：
Coil 的时序约束虽然是它区别于纯哈密顿路径的全部，但它在**静态预处理**里榨不出东西 ——
时序信息只在搜索过程中（知道谁已经走过了）才真正有力量，而那正是 DFS 本来就在做的事。

## 瓶颈分区的可行性检查 —— 结论非常正面

用 ericgopak/Mortal-Coil 的切割判据（`src/Simulator.cpp:8`，5 行）：站在格子 c 朝方向 d，
`(左边是墙 或 左前是墙) 且 (右边是墙 或 右前是墙)` 就是个宽度为 1 的瓶颈，当割边。
砍掉全部割边后求连通分量（`experiments/partition_stats.py`）：

| 关 | 自由格 | 割边 | 区域数 | 最大区域 | 占比 | <=20 格的区域数 |
| --- | --- | --- | --- | --- | --- | --- |
| L99 | 859 | 403 | 318 | 30 | 3.5% | 315 |
| L139 | 1604 | 687 | 540 | 46 | 2.9% | 530 |
| L195 | 3028 | 1403 | 1092 | 51 | 1.7% | 1081 |
| L215 | 3702 | 1675 | 1294 | **57** | **1.5%** | 1270 |
| L301 | 7161 | 3145 | 2416 | 50 | 0.7% | 2361 |
| L401 | 12543 | 5389 | 4100 | 128 | 1.0% | 4010 |

**盘面被切得极碎**：最大区域只占 1%~3%，绝大多数区域不到 20 格，而且这个性质随盘面变大
不但没退化反而更好（L401 的最大区域也才 128 格 / 1.0%）。

这意味着这套方法的本钱是付得起的 —— **离线穷举每个区域的全部穿越方式**（区域才几十格），
每条穿越方式带 must-be-blocked / must-be-free 位掩码，主搜索只在区域层面走、两次位运算判兼容。
也解释了 ericgopak 那个仓库为什么能 committed 到第 332 关。

而且它带着我们**最想要的那件东西**：**穿越解数为 0 的区域必然含起点或终点**。
起点证伪已经被证明传播干不了（偶数关 0%），而这是唯一有实证、能直接打「起点彩票」的招。

**下一步就做这个。** 两个已知的坑要提前记着：
1. 它**只改善尾部**。同一个仓库跑我们 2 秒能过的 L215 要两小时 —— 分区法是改变尾部形状，
   不是全面加速，所以要做成「和现在的搜索并存」而不是替换。
2. `floodInnerSubcomponents()`（环形 / donut 区域，包住内部障碍的情况）在原仓库里是**空 stub**，
   作者自己没做出来。我们得自己处理，或者先只处理非环形区域。

## 瓶颈分区实做 —— 结构可行，但便宜的那一半判据不产生信息

分区本身切得很漂亮（上一节的表），于是直接上最想要的那条判据：
**穿越解数为 0 的区域必然含起点或终点**。

先挑最容易 sound 的形态做：**只判 2 个口的区域**（占 70~80%）。推理是干净的 ——
割边是「边」，路径走过一次就不能再走，2 口区域只有两条割边，所以路径进出它最多各一次，
于是只剩两种可能：恰好穿越一次（e1 进 e2 出或反之，且必须把区域内格子全覆盖），
或者端点就在这个区域里。两个方向都无穿越解 ⇒ 必含端点。全盘只有 2 个端点，
这种区域最多 2 个 —— 一旦找到，起点候选就从几千格塌到几十格，正好补上传播干不了的那件事。

穿越判定写成保守版才 sound（宁可判有解）。唯一要小心的是滑行会撞到区域外，而外面的状态未知：
撞到**进来那条割边**的外侧格 —— 那格是我们进来前走过的，必然已访问，必须停；
撞到**出去那条割边**的外侧格 —— 不知道走没走过，两种都试（停下 / 滑出去），
「滑出去」只有在区域已被全覆盖时才算穿越解；撞到区域内的格子 —— 自己算得出来，精确判定。
区域最大 57 格，一个 uint64 位掩码就能精确穷举（`experiments/transit.py`）。

**实测：零穿越解区域 0 个。** L99 / L139 / L165 / L195 / L215 全是 0。
所有 2 口区域都能穿越 —— 因为切得太细了，L215 的 1294 个区域里绝大多数是走廊里的单格，
单格或走廊从一头进另一头出当然有穿越解。

那就让区域「变胖」再判 —— 也就是先做**度 2 链收缩**（Hippo 说这是过 160 关的关键，
其实和这里是同一件事）。可行性一量，另一头也不成：

| 关 | 自由格 | 度 2 格 | 收缩后的块数 | 最大块 | 占比 |
| --- | --- | --- | --- | --- | --- |
| L139 | 1604 | 504 (31%) | 37 | **477** | 29.7% |
| L195 | 3028 | 979 (32%) | 76 | 327 | 10.8% |
| L215 | 3702 | 1153 (31%) | 74 | **666** | 18.0% |
| L301 | 7161 | 2158 (30%) | 138 | 541 | 7.6% |

**两个极端，中间没有甜点**：按瓶颈切 —— 区域小到判据失效；按度 2 收缩 —— 区域大到几百格，
穿越方式根本穷举不了。要拿到中间地带只能上 Mortal-Coil 的完整机制：多口区域的穿越目录、
每条方式带 must-be-blocked / must-be-free 位掩码、再按「出入口序列」等价归并
（那一步才是让目录不爆炸的关键）。那是几千行的工程。

**结论：分区法在我们现在这个关卡区间（L215~250）性价比是负的。** 两条理由：
便宜的那一半判据实测不产生任何信息；完整版的实证摆在那里 —— 同一个仓库跑我们
2 秒能过的 L215 要 7322 秒。它是**改变尾部形状**的工具，要等搜索路线真的顶到天花板
（估算在 L350~450）之后才轮得到，而不是现在。

好消息是可行性数据已经量清楚了，真要做的时候不用再摸一遍：切割判据、区域尺寸分布、
2 口占比、度 2 链占比、收缩后的块尺寸，都在 `experiments/partition_stats.py` 和
`experiments/transit.py` 里，跑一下就有。

## 完整的分区机制做出来了 —— 四种用法全部证伪

上一轮只测了最便宜的判据（2 口区域零穿越），这轮把 Mortal-Coil 的**完整机制**实现了：
对每个区域枚举**全部**可行走法（`experiments/catalog.py`），三件事都考虑到位才 sound ——
路径可能**多次**进出同一区域（实测 13% 的区域会，最多 7 次）、路径可能在区域内**开始或结束**、
滑行撞到区域外时**外面的状态未知**（撞到已用过的口：外侧格必然已访问所以必须停；
撞到没用过的口：可停可出去，两种都试）。枚举量超预算就放弃该区域（放弃 = 不提取信息，仍 sound）。

### 地基测量先说明了「别人为什么慢」

`experiments/visit_pattern.py` 沿真解量每个区域被进出几次：

| 关 | 只被进出 1 次的区域 | 段总数 | 格子级滑行步数 |
| --- | --- | --- | --- |
| L139 | 469/540 = 87% | 623 | 840 |
| L165 | 661/768 = 86% | 896 | 1196 |
| L195 | 945/1092 = 87% | 1268 | 1601 |
| L215 | 1126/1294 = 87% | 1495 | 1986 |

87% 只穿越一次 —— 目录规模完全可控，这是好消息。但**段总数只比滑行步数少 25%**。
也就是说「主搜索只在 component 层面走」这件事**根本没把搜索深度降下来**，
却把格子级的剪枝（O(1) 奇偶、O(1) 死端、局部连通性、传播定死的边）全丢了。
这就是 ericgopak 那个仓库跑我们 2 秒能过的 L215 要 7322 秒的原因，
也是它耗时**极不单调**（L290 只要 0.9 秒、L321 要 10.6 小时）的原因 ——
它把分区当主搜索框架，一旦起点选错就整个卡死，而它没有我们的起点探针证伪和 28 核并行。

所以这里没照抄它的出口，改成把目录当**信息源**：走法交集 => 必用边，走法并集的补 => 禁用边，
喂给 v20 已有的 estate 剪枝。榨不出信息的区域直接跳过，只赚不亏。结果是四连负：

### 负一：含端点模式时，交集必然被打成空集

枚举**全部**走法（含端点模式），必用边 **0**、禁用边 **0**、零走法区域 **0**。
走法数分布看一眼就明白了：最少的档是 **6 种**，没有任何区域是 1~2 种。
一个单格 2 口区域的 6 种走法是「左进右出 / 右进左出 / 左进就结束 / 右进就结束 /
起点在这从左出 / 起点在这从右出」—— **后四种各少用一条割边，交集必然为空**。

### 负二：pure 很强（58~67%），但正好错在那 2 个区域上

排掉端点模式只枚举**纯穿越**：必用边 L139 **57.9%** / L165 **63.8%** / L195 **66.6%**，
远超 v20 传播+probing 的 35%，而且 415/530 个区域的走法数只有 1 或 2 种。
但它只对「本区域不含端点」成立，soundness 在 L139 上如期失败（必用边错 2 条、禁用边错 1 条）。

### 负三：拿它筛「(起点区域, 终点区域)」对 —— 一对都筛不掉

既然全盘只有 2 个区域是错的，就反过来枚举那 2 个：假设端点区域是 (S,T)，
把**除 S、T 之外**所有区域的 pure 必用边全部应用上（这一步完全 sound），再传播，
矛盾就排除这对。真解那一对确实活下来了（soundness 对），但**随机 60 对全部活下来**。
原因就是负二里那个数字：pure 在 L139 上总共才错 **3 条边**，错得太少，传播吸收得掉，
根本产生不了矛盾。同一个数字反过来也说明 pure 的 67% 几乎全是对的（错误率 0.2%）——
可惜「几乎」在剪枝这件事上等于零，那 3 条错边会把真解剪掉。

### 负四（决定性的）：sound 的那部分，现有传播早就推出来了

终点的颜色是已知的（两色交替，起点一定终点色就定死），所以**不含终点色格子的区域
绝不可能含终点**，加上「本区域不是起点区域」，pure 对它就完全成立。这是 sound 的用法。
可惜能这么用的区域只有 **38%**，必用边掉到 24~28%，已经低于现有的 35%。
最后一测（`experiments/region_pairs.py` 的 synergy）把它和现有传播喂到一起：

| 关 | A 只有传播 | B 传播 + 区域 pure | 净增 |
| --- | --- | --- | --- |
| L139 | 24.8% | 24.8% | **0 条** |
| L165 | 24.6% | 24.6% | **0 条** |
| L195 | 26.9% | 26.9% | **0 条** |

**一条新边都没有。** 区域方法 sound 的那部分推出的东西，「度约束 + 不成环」的传播已经
全部推出来了 —— 想想也对，小区域的穿越约束本质上就是度约束的局部体现，
真正超出度约束的那部分（滑行的时序）恰恰是不 sound 的那部分。

### 分区法到此彻底关闭

四种用法（零穿越定端点 / 全走法交集 / pure + 区域对筛选 / 颜色过滤后的 sound 子集）
全部测死，而且每一条都有解释而不只是一个数字。加上更早的那条结论
（拐弯点 88~90% 前方是墙，Coil 的时序约束在静态预处理里榨不出东西），
可以合起来给整个「预处理路线」盖棺：

**这批盘面上，能在搜索之前算出来的东西，「度约束 + 不成环 + 两色交替 + probing」已经榨干了。
再往上只能靠搜索过程中的信息（谁已经走过了），而那正是 DFS 本来就在做的事。**

剩下的路只有两条：把搜索本身做得更快（常数、剪枝顺序、并行），
或者接受 e^(0.0027×自由格) 这条天花板（估算 L350~450）。

## 订正：上一节的「预处理路线盖棺」是错的

那个结论建立在一个**逻辑错误**上。我说「拐弯点 88~90% 前方是墙，所以滑行的时序约束
榨不出东西」—— 但那个 88% 是**沿真解**统计的。它只说明约束在**真解上**平凡满足，
**完全不能说明它剪不掉错误走法**。剪枝的价值恰恰在于杀掉错的分支，而我从没测过这件事。

而且事实摆在那儿：Tron 用预处理把「不确定用哪两条边的格子」压到 **0.3%**，然后才跑朴素搜索。
先把口径对齐（他说的是**格子**，我一直在报**边**）：

| 关 | 我们（格子口径，传播+probing） | Tron |
| --- | --- | --- |
| L139 | 36.3% | 99.7% |
| L195 | 39.4% | 99.7% |
| L215 | 46.8% | 99.7% |

不是口径问题，是真的缺东西。

## 找到了一条无条件的强约束：相邻两格不能背对背地互相进入

`in(c)=d` 表示 c 的前驱是 c−d。若同时 `in(c+d)=−d`（右邻的前驱是它右边那格），那么
边 (c, c+d) **一定不被使用** —— 两格的入边都被别人占了。于是 c 处转弯要求 t(c+d)<t(c)、
c+d 处转弯要求 t(c)<t(c+d)，**直接矛盾**。

    ¬( in(c)=d ∧ in(c+d)=−d )     无条件成立

在 5 个真解上验过：违反 **0** 次。这正是 Tron 说的
"the field at y,x will not be entered from the left side" 那一类东西。

顺带量到另一个关键数字：真解里**只有 3.5~4.6% 的格子「前方是自由格且更早访问」**。
也就是说 **95%+ 的情况下，一旦知道进入方向 d，要么 c+d 是墙（必须转弯），
要么必须直行下去** —— 滑行约束比我之前判断的强得多，而无向边模型一点都装不下。

## 有向层单独跑是死的，联合起来才有戏

`experiments/directed.py`：只有有向层（in/out + 互斥 + 2 环排除），**几乎定不死任何东西**
（in 定死 1/1604，平均每格还剩 2.81 个候选）。原因是**没有种子**：互斥和 2 环排除都要求
in[c] 先收敛成单值，而每格的 in 候选数就是它的度数，最小是 2 —— 天然没有单值格。

`experiments/joint.py` 把两层耦合起来（u = c−d，边 (u,c)）：

    R1  in[c] 不含 d 且 in[u] 不含 −d  => 这条边没人用，判禁用
    R2  边禁用                          => in[c] 去掉 d，in[u] 去掉 −d
    R3  边必用 且 in[u] 不含 −d         => 方向只能是 u→c => in[c] 塌成 {d}
    R4  in[c]={d}                       => 边 (u,c) 判必用

再加一个真种子：**起点的第一次滑行是在空盘面上做的，必然一路滑到墙**，所以那**整条线**上的
in 方向全是同一个 d —— 一次钉死一大串，而不是一个。效果：4 个首步方向里能直接证伪 1~2 个，
(起点, 首步) 对的证伪率 **17~18%**。但**没有任何一个起点的四个方向被全部证伪**（0/60），
所以起点还是杀不掉，和之前的结论一致。

## 决定性的诊断：规则很强，缺的是种子

给传播喂真解的一小部分 in 方向，看它能推出多少：

| 喂进去 | 推出来（in 定死） | 放大 |
| --- | --- | --- |
| 2% | **11.3%** | 5.6× |
| 5% | 20.5% | 4.1× |
| 10% | 36.8% | 3.7× |
| 25% | 69.1% | 2.8× |
| 50% | **93.1%** | 1.9× |

**传播规则本身足够强**（喂一半能推出 93%），问题百分之百在种子上。这也直接指出我之前
probing 做错的地方：**我一直在 probe「边」（2 个取值），该 probe 的是「in 方向」（4 个取值）**，
而且喂一个 in 方向就能推出一片，所以拿它去撞矛盾命中率高得多。

## 迭代 probing 确实雪崩，但收敛在 12%

对每个格子的每个候选 in 方向做 failed-literal（假设它成立、传播、矛盾就消掉），滚到不动点：

| 轮次 | 消除方向数 | in 定死 | 用哪两条边已确定 |
| --- | --- | --- | --- |
| 起手（只传播） | — | 0.1% | 0.0% |
| 第 1 轮 | 182 | 3.3% | 3.2% |
| 第 2 轮 | 108 | 7.3% | 6.9% |
| 第 3 轮 | 67 | 10.0% | 9.3% |
| 第 4 轮 | 62 | **12.2%** | 11.0% |
| 第 5 轮 | 6 | 收敛 | |

从 0.1% 到 12.2%，雪崩是真的，但离 99.7% 还差得远。

**所以现在的状态是：方向找对了，路只走了一小段。** 下一步的候选（按我判断的把握）：
1. **2-level probing**：同时假设两个 in 方向再传播。单层已经收敛，双层是自然的下一档，
   而 Tron 说的 "bounded brute force" 多半就是这个量级的东西。代价是 O((4n)²) 次传播，
   要靠增量传播（trail 撤销）才付得起 —— 现在的 python 原型是全量重算，必须先 C 化。
2. **把 in 方向的约束接进 DFS 当剪枝**：即使只有 12%，这也是**有向**信息，
   v20 现在完全没有；知道 in 方向就知道路径在那一格怎么走，剪枝比无向边强。
3. 把首步滑行那个种子推广：路径的**每一次**滑行都必须滑到底，不只是第一次 ——
   在搜索里这是自动满足的，但在预处理里还没有被用成种子。

## 种子的**分布**比数量重要 —— 这条解释了「搜索中传播」为什么没用

之前那张「喂 25% 推出 69%」的表，种子是**随机散布全盘**的。而 DFS 每走一步能提供的种子
是**连续前缀**（走过的那一段）。两者差别极大：

| 喂 25% 的 in 方向 | 传播推出 | 超出种子 |
| --- | --- | --- |
| 连续前缀（DFS 实际能给的） | 25.0% | **+0.0pt** |
| 随机分布 | 70.9% | **+45.9pt** |

L195 同样：连续前缀 +0.5pt，随机分布 +51.1pt。

**连续前缀几乎推不动任何东西。** 道理也说得通：走过的那一段把信息全堆在一处，
剩余区域依旧是对称的、没有着力点；而散布的种子每一个都能在自己的局部点火。

这正好解释了 v18 的实测结果（在搜索里做传播，只有很浅的 D=2 划算，深了就赔）——
不是实现问题，是**种子分布**的问题。「每走一步剩下的又是一个新的 0 步起手」逻辑上没错，
但那个新起手拿到的种子仍然挤在边界上，推不动。

## 互斥约束的威力：只砍掉一半，不够

在空盘上枚举，看「哈密顿路径」被互斥约束砍掉多少，离真正的 Coil 解还差多远：

| 空盘 | 哈密顿路径 | 加互斥后 | 真正的 Coil 解 |
| --- | --- | --- | --- |
| 3×3 | 40 | 40 (100%) | 16 |
| 4×4 | 552 | 400 (72.5%) | 32 |
| 5×5 | 8648 | **4048 (46.8%)** | **32** |

互斥是无条件成立的强约束，但它只砍掉一半，而 Coil 解只有 4048 的 1/126。
**纯结构约束（度 + 不成环 + 两色交替 + 互斥）刻画不了 Coil，时序是本质的。**
这也是结构传播推不动的根本原因，不是弧一致性做得不够。

**所以 Tron 那 99.7% 里一定含时序推理**，而时序推理在预处理阶段（不知道路径顺序）很难做。
唯一可能的形态是他说的 **bounded brute force**：在一个有界窗口里把路径经过它的所有方式
**连同相对时序**一起枚举。我的 `catalog.py` 就是这个思路（区域内枚举含滑行模拟），
但用瓶颈分区切出来的区域太碎（大多是单格），信息量不够；换成以格子为中心的大窗口
则枚举代价指数上升。这里是明确的未完成品，不是死路。

## 一个参数值 16 关：探针预算的最优值随算法演进翻了车

v13 时代测过「加大探针预算是负作用」（L193 7.2s → 23.6s），于是 `PROBE=2000` 这个默认值
就一直没人再动过。但 v17~v20 之后 estate 变强了（传播 + probing 把边定死到 35%），
**探针里每个搜索节点的剪枝力today 完全不是当年那个量级**，于是大预算重新变得划算：

| PROBE | L193 | L195 | L209 | L215 | L217 | L223 | L225 | **各关最大值** |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 2000（老默认） | 14.4s | 6.8s | 5.4s | 2.1s | 18.6s | **60.5s** | 3.9s | 60.5s |
| **8000** | 8.3s | 7.6s | 6.0s | 2.1s | 4.0s | **19.7s** | 4.1s | **19.7s** |
| 16000 | 9.1s | 8.1s | 6.4s | 2.2s | **0.3s** | 27.2s | **0.4s** | 27.2s |
| 32000 | 10.6s | 8.8s | 7.3s | 2.2s | 0.3s | 22.8s | 0.4s | 22.8s |

评测只看「每关有没有在 30 秒内做完」，所以要挑的是**各关最大值最小**的那一档 —— PROBE=8000。

**30 秒成绩：221 → 237**（L239 超时）。改一个数字，16 关。

**教训**：调参结论会随算法演进过期。这个仓库里每一条「XX 是负作用」的记录，都该标注它是在
哪个版本上测的；换了底层之后，值得把老的负结论重新跑一遍 —— 尤其是那种「只改一个常数」的。

顺带看出参数敏感度大得吓人：L217 在 PROBE=2000 要 18.6 秒、PROBE=16000 只要 **0.28 秒**（66 倍）；
L223 反过来是 PROBE=8000 最好。**没有一个值对所有关都对**，这是起点彩票的固有噪声。

## v21：分层探针 —— propagate_strong 只算一次，白捡一层大预算筛选

既然单一预算总会在某些关上踩空，那就分层。关键是发现 v20 里 `propagate_strong`
（传播 + probing，每起点约 0.05 秒）被**算了两遍**：探针阶段一遍、正式搜之前又一遍。
把它缓存下来，中间那层大预算筛选就是纯赚的：

    第一层：propagate_strong + 小预算(8000)      便宜地干掉大部分起点
    第二层：复用缓存的 estate + 大预算(24000)    不重算 probing，纯赚
    第三层：复用缓存的 estate + 无限搜           搜穷即永久剔除

第三层的「无限搜」必须保留 —— v15 那次纯迭代加深惨败（L193 7s→174s）就是因为错起点
永远搜不穷、被反复重搜，而**搜穷一次就能永久剔除**才是真正的投资。

| 关 | v20 (PROBE=8000) | v21 分层 |
| --- | --- | --- |
| L217 | 3.9s | 3.9s |
| L223 | 19.0s | 22.8s |
| L235 | 17.1s | 15.6s |
| L237 | 23.5s | 24.1s |
| L239 | **342.7s** | **44.2s** |

L239 是 v20 的瓶颈关，分层把它从 342 秒压到 44 秒（8 倍），虽然还没进 30 秒线。
PROBE2 也有甜点：24000 最好，48000 会让 L239 炸回 316 秒。

## 桥规则：sound，但这批盘面上初始图里一条桥都没有

从 Masyu 求解器的「格子线形状」建模想到的一条 **sound 的结构规则**，把时序条件转成了连通性条件：

    转弯于 c（进入方向 d）要求 t(c+d) < t(c) —— 路径得先到 c+d，再绕回 c；
    而转弯时边 (c, c+d) 是不用的。所以这条边若是**桥**，「绕回来」根本不存在：
        边 (c, c+d) 是桥  ⟹  in(c)=d 必须直行  ⟹  **in(c)=d ⟹ in(c+d)=d**

这是**蕴含式**，不需要 in 先收敛成单值就能传播，正是一直缺的那种种子来源。可惜：

| 关 | 自由格 | 网格边 | 桥 |
| --- | --- | --- | --- |
| L139 | 1604 | 2254 | **0** |
| L195 | 3028 | 4209 | **0** |
| L223 | 3992 | 5640 | **0** |

**初始图上一条桥都没有** —— 这批盘面是 2 边连通的，和当年 v8 割点剪枝失效是同一个原因
（「这些盘面最小度为 2、连通性极好，割点几乎不出现」）。

但这条规则没死透：**边一旦被禁用，剩余图上就会冒出桥**；搜索走到一半时剩余图更是到处是桥。
所以它该做成**动态**的 —— 在传播的不动点迭代里重算桥，或者在搜索内部用。留给下一轮。

## v21 分层探针、v22 预算 portfolio —— 两个都是负结果

参数敏感度那么大（66 倍），很自然想到两条对策，都试了，都赔。

**v21 分层探针**（小预算 → 大预算 → 无限搜，`propagate_strong` 缓存复用）：
单关看很漂亮 —— L239 从 342 秒压到 44 秒（8 倍）。但完整评测 **221，比单预算的 237 还差**：

| 关 | v20 (PROBE=8000) | v21 分层 |
| --- | --- | --- |
| L213 | 1.5s | **23.9s** |
| L217 | 3.9s | 9.0s |
| L223 | 19.0s | 超时 |
| L239 | 342.7s | **44.2s** |

中间那层大预算筛选，对**大部分关**是纯浪费 —— 它让每个存活起点都多付一遍钱，
而真正需要它的关只是少数。只赚了尾部，赔了中盘。

**v22 预算 portfolio**（把 28 核分成 4 档、每档一个预算、每档都覆盖全部起点，
赌「总有一档预算对上」）：

| 关 | v20 (PROBE=8000) | v22 portfolio |
| --- | --- | --- |
| L213 | 13.4s | 49.1s |
| L217 | 3.8s | **0.7s** |
| L223 | 18.3s | 75.0s |
| L235 | 16.9s | 64.6s |
| L237 | 23.4s | 86.3s |

只有 L217 赢（预算撞对了），其余全线崩。道理很清楚：每档只剩 7 个核来分起点，
**单档的起点数变成 4 倍**，而「起点要多等 4 倍」这个代价比「预算踩空」贵得多。

**两条负结果合起来给出一条判断**：起点彩票的噪声不能靠「多花钱买保险」抹平 ——
不管是纵向多花（分层）还是横向多花（portfolio），**多出来的钱都摊在了不需要它的关上**。
唯一划算的仍然是「挑一个各关最大值最小的单一预算」，也就是 PROBE=8000。
## v23：自适应分层 —— 只在**需要**的时候才加钱

v21（无差别分层）和 v22（预算 portfolio）失败的原因是同一个：**多出来的钱摊在了不需要它的关上**。
那就别无差别地花 —— 用第一层的**存活率**当判据：

    存活率高  => PROBE 这个预算在这一关根本搜不穷子树、证伪不动 => 值得加钱上第二层
    存活率低  => 预算已经够用 => 直接进无限搜，一分钱不多花

阈值 30%（`TIER2` 可调）。效果正是想要的 —— 中盘一分不赔，尾部大赢：

| 关 | v20 (PROBE=8000) | v23 自适应 |
| --- | --- | --- |
| L213 | 14.1s | 13.3s |
| L217 | 3.7s | 3.8s |
| L223 | 18.3s | 22.4s |
| L235 | 16.7s | 15.4s |
| L237 | 23.2s | 22.5s |
| **L239** | **333.0s** | **42.5s** |

PROBE2 也有很窄的甜点：12000 → 439 秒，**24000 → 42 秒**，48000 → 316 秒。
这种「差一档就差十倍」的现象本身就是起点彩票噪声的写照，不是可以靠调参消掉的东西。

完整评测：**237**，和单预算的 v20 同分（L239 42.5 秒，还是差一口气进 30 秒线），
但**明显更稳**——最慢的关从 23.7 秒降到 **22.1 秒**，而 L239 这种极端关从 333 秒降到 42.5 秒。
soundness 拿真解逐步验过（L139/L195/L215 零报警）。

**所以现在的主版本是 v23**：同分、余量更大、尾部形状好得多。要突破 239 关，
差的是让 L239 再快 1.4 倍 —— 而它对 PROBE2 极度敏感（12000→439s，24000→42s，48000→316s），
说明这一口气得靠别的东西补，不是调参能调出来的。

## v24：动态桥规则 —— 负结果，但量出了传播的病根

昨晚记的是「初始网格图上一条桥都没有，但边被禁用后会冒出桥，所以该做成动态的」。做了。

用的是比 in 方向蕴含式更强也更好接的形式：**可用边图（estate≠2）里的桥必然被路径使用**
——去掉它图就断成两块，哈密顿路径要覆盖两边所有格子，唯一能跨过去的可用边只有它。
这条直接就是 `estate=1`，不需要 in 变量。soundness 在 L99/139/171/195/215 五关上验过，**零报警**。

结果：**几乎找不到桥**。

| 关 | 边 | 判定 | 必用 | 禁用 | 桥规则判出 |
| --- | --- | --- | --- | --- | --- |
| L139 | 2254 | 600 (26.6%) | 560 | 40 | **0** |
| L195 | 4209 | 1229 (29.2%) | 1134 | 95 | **0** |
| L215 | 5215 | 1457 (27.9%) | 1339 | 118 | **1** |

**病根在这张表里**：判定出来的边**九成是「必用」，「禁用」只占全部边的 1.8%**。
而桥是靠**删边**才冒出来的 —— 我们几乎不删边，图就一直是 2 边连通的，桥自然没有。

这条诊断比桥规则本身有用：**我们的传播是不对称的，擅长推「这条边必用」，不擅长推「这条边不能用」。**
所有基于连通性的规则（桥、割点、v8 的关节点）都要靠禁用边才能起作用，
所以它们在这批盘面上一直失效，根子是同一个。

下一轮该问的是：**什么规则能大量产出「禁用」？** 现有的两条产出禁用的规则是
「连上去成环」和「某格已定满 2 条边」，后者需要度饱和，而 560 条必用边摊到 1604 格
平均每格才 0.7 条，离饱和很远。

## v25/v26：捡回链定向白扔的有向信息

`orient_chains` 对每条链算了两个行进方向可不可行（ok0/ok1），**但只用来判「两个都不行 => 矛盾」**。
只有一个可行时，整条链的行进方向其实已经钉死了 —— 这个有向信息一直没交给 DFS，
而诊断正说「缺的是有向信息，无向边装不下时序」。

- **v25**：记下每格的强制**进入方向** `pin_in[c]`，DFS 每滑过一格 O(1) 校验。
- **v26**：再挖**出方向** `pin_out[c]`，配合滑行的两条强性质：
  run 的**中间格必须直穿**（pin_out ≠ 滑行方向就矛盾）、**落点必然转弯或收尾**（pin_out = 滑行方向就矛盾）。

覆盖率（白捡的，信息本来就算出来了）：

| 关 | 钉死进入方向 | 占比 |
| --- | --- | --- |
| L139 | 132 / 1604 | 8.2% |
| L195 | 308 / 3028 | 10.2% |
| L215 | 366 / 3702 | 9.9% |

和 python 迭代 probing 拼出来的 12% 是同一量级。

### 性能：和 v23 打平（不是赢）

干净 A/B（每关都确认输出非空，无 CPU 争抢）：

| 关 | v23 | v26 | 变化 |
| --- | --- | --- | --- |
| L195 | 30.3s | **15.8s** | −48% |
| L215 | 3.18s | 3.31s | +4% |
| L223 | 38.0s | 39.0s | +3% |
| L235 | 22.5s | 21.2s | −6% |
| L239 | 65.9s | **76.1s** | +15% |
| L249 | 49.0s | 48.4s | −1% |
| L257 | 90.0s | **126.1s** | +40% |
| L271 | 207.3s | 222.4s | +7% |
| L287 | 329.8s | **269.5s** | −18% |
| **合计** | **836.1s** | **821.7s** | **−1.7%** |

**有信息，但换不来时间。** 猜测是：被 pins 剪掉的走法，本来也会被现有的连通性/奇偶/estate
很快剪掉，省下的不多；而每个候选多付一个 O(len) 的循环。

**方法论教训**：双向波动到 ±40%，9 关的样本**根本分辨不出小效应**。
笔记里反复出现的「差一档就差十倍」就是起点彩票的方差，它同样会淹没 A/B 的信号。
以后要判断小改动，得么么控方差（固定起点重复测），要么把样本量拉到几十关，
否则「合计 −1.7%」这种数字读不出任何东西。

**所以主版本仍然是 v23。** v26 留在仓库里作为记录，PIN=0 可关。

### 踩到一个「单起点验证验不出来」的 bug

v25 在 L195 上直接 FAIL，而 v23 能解 —— 但 `--verify` 五关零报警。原因：
**第二/三层复用缓存的 estate 时没有同步重算 pins**。pins 是**起点相关**的
（orient_chains 用 head_s/tail_s 定向），于是上一个起点的钉死方向被套到别的起点的搜索上，
把真起点误剪掉。而 `--verify` 只跑一个起点，天然验不出这类**跨起点串味**的 bug。

教训：**凡是「按起点算、又被缓存复用」的东西，都要检查恢复路径**；
单起点的 soundness 验证覆盖不到这一类。v26 里在两处 memcpy(estate) 之后补了 orient_chains(s)。

## v27：互斥约束在 in 层跑不动点 —— 无增益，但原因很干净

诊断说「缺的是种子」，而 v26 的链定向恰好提供了种子（8~10% 的格子 in 单值）。
于是把昨晚那条**无条件**互斥约束当传播规则接上去：

    ¬( in(c)=d ∧ in(c+d)=−d )   =>   已知 in(c)=d 就消掉 c+d 的一个候选

指望的是雪崩：每格的 in 候选数就是它的度数，**度 2 的格子只有 2 个候选**（占 30%），
消掉一个就变单值。实测**几乎一无所获**：L139 +0 格，L195 +0 格，L215 +1 格。

**原因**：钉死的格子都在链上，而这条规则作用的对象是**被钉死格子正前方那一格**。那一格要么
就是链的下一格（本来就已经钉死，消不出新信息），要么是**墙** —— 而「95%+ 的转弯贴着墙发生」
是昨晚量过的。**种子的位置和规则的作用面几乎不重叠。**

这和昨晚「互斥约束只砍掉一半，时序是本质」是同一件事的两个侧面：
互斥是**结构**约束，而 Coil 的信息量集中在**时序**里，结构规则再叠也够不着。

（顺带白捡了一条：候选集空掉 => 起点被证伪。实测没抓到额外的起点，但它是免费的，留着。）

## 订正：「禁用只占 1.8%」这个数字是我量错口径

之前用 `--verify` 量传播产出，而那里调的是 `propagate(s)`——**不含 probing**。
真正搜索时用的是 `propagate_strong`（带 failed-literal probing），而 probing 恰恰是产出禁用的机制。
补测（`STRONG=1`）：

| 关 | 仅传播 | 传播 + probing |
| --- | --- | --- |
| L139 | 必用 560 / 禁用 40 | 必用 775 / 禁用 **103** |
| L195 | 1134 / 95 | 1585 / **225** |
| L215 | 1339 / 118 | 2186 / **342** |

禁用翻了一倍多。但**不对称的结论不变**：必用 : 禁用 ≈ **7 : 1**，
禁用仍只占全部边的 4.6~6.6%，而真解里约 **29%** 的边是不被使用的。桥依然是 0 条。

### 不对称的机制 —— 它直接指出了下一步

单边 probing 里：
- **「假设禁用」很容易矛盾** —— 最小度 2 的图禁掉一条边就造出度 1 格，连锁到成环/奇偶。
  所以它产出的是**必用**。
- **「假设必用」很少矛盾** —— 加一条必用边很少能让某格饱和到 2 条。所以禁用产出少。

要让禁用多起来，就得让「假设必用」更容易矛盾 —— **同时假设两条**就容易饱和/成环。

## v28：同格配对 probing —— 小幅正向

不必做 O(m²)：每格恰好用 2 条边，所以**只在同一格的相邻边之间配对**就够了。
若含 e 的所有同格配对都矛盾，e 就不可能在被选中的那两条里 => **禁用 e**。每条边最多 3 次探测。

| 关 | PAIR=0 必用/禁用 | PAIR=1 必用/禁用 | 配对直接判出 |
| --- | --- | --- | --- |
| L139 | 788 / 106 | 798 / 113 | 7 |
| L195 | 1590 / 225 | **1635 / 252** | 10 |
| L215 | 2180 / 337 | 2193 / 345 | 6 |

直接产出的禁用只有 6~10 条，但**级联带出的必用更多**（L195 +45）。合计判定边数 +1.7%。
开销出乎意料地小（L195/L215 只多 1.5%）—— 因为常规 probing 之后符合条件的格子（0 条必用边
且 ≥3 条待定边）已经不多了。soundness 三关零报警。

**作用面的天花板也就在这里**：配对只对「还要从 ≥3 条待定边里选 2 条」的格子有效，
而 probing 跑完之后这种格子本来就少。要再往上，得跨格配对（代价大得多）。

## ⭐ 从答案反推：局部模式确实存在，而且窗口必须 ≥5×5 —— `experiments/mine_patterns.py`

Tron 的核心是消除「the field at y,x will not be entered from the left side」，
他管这叫 local patterns，但从未展开。**我们有真解，可以直接反推**：
统计每个局部墙形下各个进入方向的实际频次，某方向在大量样本里一次都没出现 => 候选规则。

必须做留出验证（26599 种形状 × 4 方向 ≈ 10 万次检验，靠「0 次」很容易挖出噪声）。

| 窗口 | 归一化形状数 | 挖掘集候选（0 次且 30+ 支持） | 留出验证 |
| --- | --- | --- | --- |
| **3×3** | 37 | **0 条** | — |
| **5×5** | 26,599 | **44 条** | **活 38 / 证伪 6** |

**跨规模再验一次**（挖掘 L<120，留出 L≥120）：**活 20 / 证伪 1**。

两条结论：
1. **3×3 一条都挖不出来，5×5 挖得出** —— 窗口大小是硬门槛，这正好解释了 Tron 为什么
   要叫「local **patterns**」而不是「局部规则」：信息藏在 5×5 这个尺度上。
2. 规则**跨规模泛化**（小盘挖的在大盘上依然成立），不是过拟合。

最干净的一条（挖掘 93 + 留出 126 个样本，0 次）：中心格上方两行全是「墙」——
多半就是**棋盘上边界**——此时**从来不会是「向上滑进来」的**，只会横向进入。

### ⚠ 方法论上的两个已知弱点（下一步要补）

1. **每关只用了一个解**（求解器找到的第一个）。多解的关卡里这个样本是有偏的。
   要补：拿 `count_solutions.c` 在小盘上**枚举全部解**再统计，那才是真正的「所有解都不出现」。
2. **没证明它们 sound**。目前只知道「在 102 个真解里没出现过」。
   这批盘面全是同一个生成器造的，所以有可能是**生成器的规律**而非定理。
   工程上的处理是照 v9 的老办法：**当硬剪枝用 + 整轮失败就退回不带它的模式**，保完备。

### 这条路怎么接进求解器（v29 的形状）

把活下来的规则做成一张表：**归一化 5×5 模式 -> 禁止的进入方向掩码**。
开局给每个格子查一次表，得到 `forbid_in[c]`，然后：
- DFS 里当 O(1) 剪枝用（和 v26 的 pin_in 同一个位置）
- 喂进边传播层：进入方向被禁 => 对应的边有约束 => 可能产出**禁用边**（正好补我们最缺的那一侧）

这是目前唯一一条**有实证支撑、又直指 48% -> 99.7% 那个缺口**的路子。

## v29：等价推理 —— 实现了、sound、但一条都推不出来

调研指出日本人把规则分两类：**「最大数理論」产线（必用），「通過点理論」产 ×（禁用）**，
而我们手上全是前者。等价推理正是后者的核心机制：不急着把边定成 0/1，先记住**边之间的关系**。

实现（带奇偶的并查集，EQUAL=0 / OPPOSITE=1）：设某格还需 r 条边、有 m 条待定边，
- r=1,m=2 => 记两条 OPPOSITE
- r=1,m=3 且某对 EQUAL => 那对要么都用(2>1)要么都不用 => **两条都禁用**，第三条必用
- r=1,m=3 且某对 OPPOSITE => **第三条禁用**
- r=2,m=3 且某对 EQUAL => 那对必用，**第三条禁用**
- r=2,m=4 且某对 OPPOSITE => 记另两条 OPPOSITE

soundness 四关零报警。但**推论数为 0**：

| 关 | 建立关系 | 能用关系的格 (nu=3,r=2) / (nu=4,r=2) | 实际推出 |
| --- | --- | --- | --- |
| L139 | 103 | 281 / 81 | **0** |
| L215 | 422 | 378 / 162 | **0** |

**原因**：每次 union 只连接**同一格的两条边**，而要在某格做推理，需要**它自己的两条边之间有关系** ——
那要求关系图上存在**环**。422 条关系摊在 5215 条边上太稀疏，一个环都合不上。

**更深一层的原因有点反直觉：我们在「必用」方向太强了。** 能产生关系的是 (r=1,m=2) 这种
「还差一条、有两个候选」的格子，而我们的传播/probing 往往直接把它们解成必用，
于是留给关系层的原料反而少。日本人的 Slitherlink 里没有这个问题，因为那里的格子约束是
**奇偶**（0 或 2 条），本来就定不死，关系是主要产物。

**怎么救**：调研里给了答案 —— **把顶点级的关系推理提升到任意区域**（割奇偶恰好剩 2 条未定跨边时，
那两条就有 EQUAL/OPPOSITE 关系）。区域数量远多于格子，关系密度能上一个数量级。
但这需要先解决「端点在不在区域内」这个未知量 —— 见下一步。

## ⭐ v30：二部图度约束流 + Régin 残量 SCC —— 第一个实质性突破

术语先对齐（前面一直在用没解释）：把解建模成**选边**，每条边只有「必用」「禁用」两种归宿；
**判定率** = 搜索前已经判出来的边占比。L215 有 5215 条边，而路径只用 3701 条，
所以**约 29% 的边是不被使用的**。Tron 预处理后只剩 **0.3%** 未判定，我们之前是 **48%** ——
这就是「算法代差」的具体形态。

### 建模（关键在端点的未知量）

网格是二部图，「每格恰好用 2 条边、端点用 1 条」就是二部图上的**度约束子图**，整个能建成流。
起点 s 已假定，颜色记 X；另一个端点的**颜色已知、位置未知**。加一个哑点 z 吸收这个未知量：

- 终点在 ~X 侧：z 放 X 侧，源→z 容量 1，z→所有 ~X 格。恰好一个 ~X 格从 z 拿 1 单位
  => 它的**真实**边数 2−1=1 => 它就是终点。
- 终点在 X 侧（自由格数为奇时）：镜像过来，z 放 ~X 侧，所有 X 格（除 s）→z→汇。

**⚠ 这里踩了个坑**：第一版写死「终点必是 ~X」，在奇数格的关卡上模型就错了，
表现是 **L235 直接 FAIL 而 v23 能解**，但 `--verify` 四关零报警照样验不出来 ——
又一次「单起点验证覆盖不到」。代码里 `prop_endcol` 本来就算好了奇偶，照它挂哑点即可。

已定的边先扣掉（rb = b − 该格必用边数），禁用边不进图。于是变成在待定边上找 b-匹配：
**流不可行 => 这个起点直接被证伪**（比连通性强得多的矛盾检测器）。
找到可行流后建残量图跑 Tarjan SCC：两端不同 SCC 的弧取值被锁死 —— **同时产必用和禁用**。

### 效果

| 关 | 判定率（v23） | 判定率（v30） | 禁用（v23） | 禁用（v30） |
| --- | --- | --- | --- | --- |
| L139 | 39.7% | **50.1%** | 106 | **159** |
| L195 | 43.1% | **55.1%** | 225 | **359** |
| L215 | 48.3% | **53.1%** | 337 | **417** |

**判定率涨 5~12 个点，禁用涨 24~60%** —— 正好补在偏科的那一侧。时间也赢：

| 关 | v23 | v30 |
| --- | --- | --- |
| L195 | 12.1s | 13.6s |
| L215 | 3.14s | 3.24s |
| L235 | 20.6s | 19.7s |
| **L239** | 83.4s | **55.5s** |
| **L249** | 50.4s | **21.9s** |

难关明显变快，简单关基本持平。soundness 三关逐步验过 + 奇偶两类关卡共 55 关全解出。

**为什么它管用（和前面几条负结果的对比）**：v24 桥、v27 互斥、v29 等价推理都是**局部**规则，
而这批盘面局部太规整（最小度 2、连通性好、拐弯贴墙），局部规则抓不到东西。
流 GAC 是**全局**的 —— 它一次性回答「在度约束这个松弛下，这条边有没有可能被用」，
不依赖任何局部特征。调研里说的「专门在你现有传播变瞎的场合起作用」就是这个意思。

## v31：连通性加强 —— 0 条推论。三次失败连起来是一条规律

流解出的子图 H 是若干路径和圈的并，真解是一条覆盖全盘的路径，所以对 H 的每个连通块 C，
跨出去的边必须 ≥ 2 条（含端点则 ≥ 1）。据此可推：跨出的待定边 0 条 => 矛盾；1 条 => 那条必用
且 C 必含端点（顺带定位终点）；两个这样的块 => 矛盾。

实测 **0 条**。原因：H 的每个连通块跨出去的待定边**远不止 2 条**，约束离紧还差得远。

### ⭐ 一条规律：这批盘面的信息在「度」里，不在「连通性」里

| 规则类型 | 版本 | 结果 |
| --- | --- | --- |
| 桥（1-边割） | v24 | 0 条 |
| 割点（关节点） | v8 | 几乎不出现，还慢 3~4 倍 |
| 互斥 / 等价推理（局部） | v27 / v29 | 0 条 |
| subtour（连通块跨边） | v31 | 0 条 |
| **度约束流 GAC** | **v30** | **判定率 +5~12 点，难关快 30~55%** |

生成器把盘面造得**连通性极好**（最小度恒为 2、2 边连通、无桥无割点、88% 的拐弯贴墙），
所以一切基于**割 / 连通 / 局部形状**的推理全部落空；而**度数结构**里有大量信息。

**这条规律直接决定下一步该往哪找**：继续在「度 / 匹配」这一族里加强松弛，
而不是再去找割、桥、局部定石。也说明原计划第三条（5×5 局部模式表）该降优先级 ——
它同样是局部规则，而且 sound 性还没证明。

## ⭐⭐ v32：全局端点过滤 —— 直接砍掉起点彩票

思路一句话：**起点必然是路径两个端点之一**。而流模型里那个哑点 z，它那一个单位挑中谁，
谁就是终点 —— 我们本来就在算残量图 SCC，**之前只看了网格边，从没看过哑点的弧**。
Régin 过滤对哑弧同样成立：`z→c` 若两端不同 SCC 且当前无流量 => **c 不可能是端点**。

再进一步：把起点也放开，两端都用哑点表示（自由格两色计数 c0/c1，|c0−c1|>1 直接无解；
相等则两端点异色，各挂一个 1 单位的哑点；差 1 则两端点同为多数色，那侧哑点带 2 单位），
开局跑**一次**全局流即可。

### 效果

| 关 | 自由格 | 起点候选 | 压缩 |
| --- | --- | --- | --- |
| L97 | 854 | **4** | 214× |
| L215 | 3702 | **54** | 69× |
| L235 | 4451 | **59** | 75× |
| L249 | — | — | — |

| 关 | v30 | v32 | 倍数 |
| --- | --- | --- | --- |
| L215 | 7.32s | **0.85s** | 8.6× |
| L235 | 38.0s | **2.26s** | 17× |
| L249 | 52.3s | **1.93s** | 27× |

**踩坑**：`FDUM2` 那侧遍历的是**反向弧**，其 `fcap` 记的是**已用流量**而不是剩余容量，
判据写反 => 当前解里真正的端点被标成「不可能」=> L13/L97 直接 FAIL。
修正后不但 sound，过滤反而更强（L215 从 50.2% 变 1.5%）。
**教训**：残量图上「这条弧用没用」的判据，正向弧和反向弧是相反的，写的时候要分别验。

### ⭐ 瓶颈被精确定位了

扫 L201~L271 共 36 关：**30 关被砍到 0.2~3.1%，6 关完全无效**（L217/223/237/239/257/271）。

**而过滤不动的那 6 关，恰恰就是一直卡住的慢关**：

| 关 | 过滤 | 耗时 |
| --- | --- | --- |
| L215 / L235 / L249 | 1~2% | **0.85s / 2.26s / 1.93s** |
| L223 / L239 / L257 / L271 | **100%** | 38~94s / 83~127s / 90~126s / 207s |

所以要修正 v32 的说法：**它提速的是中等难度的关**（压到 1~2 秒），真正的难关几乎没动
（L239 127→114s）。**这 17% 的关卡现在就是整个瓶颈，攻下它们等于攻下全部。**

### v33：全局阶段滚不动点 —— 迭代救不了

全局流除了过滤哑弧，也过滤**网格边**，而这些推论**不依赖起点假设**（只假设「存在哈密顿路径」），
所以全局有效、可以固定下来再跑一轮。实测：

    L215：第1轮固定 2459 条边（占 5215 的 47%！），起点 3702→54；第2轮 0 条，已到不动点
    L139/L195：第1轮固定 **0** 条，端点候选 100%

**L215 在完全不假定起点的情况下就定死了 47% 的边，而 L139/L195 一条都定不出来。**
它们的残量图整个是一个 SCC —— 度约束层面自由度太大，存在海量个不同的 2-因子，
交替环把所有格子连成一片。**这不是 bug，是这些实例在度约束松弛下就是不受约束的**，迭代救不了。

## v34：分治（块对联合假设）—— 负结果，但它改掉了下一步的方向

思路：单端点的支撑性 Régin/SCC 已经完备（所以难关上过滤为 0），但**两个端点是联合约束的** ——
单独看每格都可能，组合起来未必。于是把格子按行带分成 K 块，对每一对块 (i,j) 假设
「两端点分别落在这两块」跑一次流，不可行就杀掉这一对；某块与所有块的配对都不可行 => 整块淘汰。

| 块数 | 块对 | 可行 | 整块淘汰 |
| --- | --- | --- | --- |
| 12 | 144 | 72（死一半） | **0 格** |
| 32 | 1024 | 426 | **0 格** |
| 64 | 4096 | 1580（死 62%） | **0 格** |

**联合约束确实很紧（六成块对不可行），但每块都至少还有一个可行搭档**，按块淘汰不掉格子。
而且这个信息被 per-start 的流**包含**了 —— 固定起点后端点候选只剩 0.9~2.4%，比块级细得多。

（踩坑：`filter_endpoints` 会覆写 `estate`/`end_ok`，受限运行的结果**不是全局有效的**，
必须存下来再还原。第一版没还原，最后留下的是「两端点都在最后一块」那次的结果，
打出来 6~7% 的漂亮数字全是假的。**凡是「带假设跑一遍」的函数，都要检查它有没有污染全局状态。**）

### ⭐ 真正有用的发现：慢关的瓶颈不是起点数量，是筛选的代价

    L223: probe: 3892/3992 起点被证伪，剩 100

**即使在「全局过滤完全失效」的关上，per-start 探针照样证伪掉 97.5% 的起点。**
所以那些慢关的瓶颈**不在起点数量**（已经只剩 100 个），而在**为了筛掉这 3892 个所付的代价**
—— 每个都要跑一次完整的 `propagate_strong`（含一次 max-flow）。

**下一步方向因此改变**：不是继续找更强的过滤规则，而是**让筛选本身变便宜** ——
分层：先用最便宜的规则刷掉大部分起点，只对幸存者上流和 probing。
（v23 的自适应分层是按「预算」分的，这里要按「规则代价」分。）

## v35：按规则代价分层 —— 瓶颈关直接快 2 倍

上一条诊断说「慢关的瓶颈不在起点数量，而在筛掉它们所付的代价」。照着改：
`propagate_strong` 原来的顺序是 **传播 → probing → 流 → probing**，
而 **probing 是最贵的一环**（几千次 `try_edge`，每次 O(N) 快照/恢复），
`do_flow` 只是一次 max-flow 却同样能证伪。按代价重排：

    第 1 层 propagate(s)              最便宜
    第 2 层 do_flow(s)                一次 max-flow，强证伪
    第 3 层 do_probing(probe_rounds)  最贵，只有活下来的起点才付
    第 4 层 do_flow(s)                probing 产出的新边喂回流

| 关 | v32 | **v35** |
| --- | --- | --- |
| L223 | 26.6s | **15.3s** |
| L239 | 48.5s | **23.3s** |

**正好 2 倍，而且就在瓶颈关上。** 56 关全部解出，L139/L215 逐步 soundness 零报警。

**顺带修了一个从 v33 起就存在的诊断路径 bug**：`g_estate` 的分配写在 `--verify` 代码块之后，
而 `propagate()` 要从它 memcpy => 空指针段错误。也就是说 **v33/v34 我一直没能真正跑 soundness 验证**，
只有「56 关全解出」这一个证据。分配提前后补验通过。
**教训：诊断路径挂了会静默地让你以为「验过了」——它不出错才叫验过，不出声不算。**

## ⭐ 查清了：过滤失效不是关卡的性质，是**我们模型**的性质

之前说「那 17% 的关卡在度约束松弛下本身就不受约束」—— **这个结论是错的**。

先排除粗结构：过滤失效组（L217/223/237/239/257/271）和过滤有效组（L215/219/235/245/249/263）
在**每一个粗指标上都一样** —— 墙密度 66~68%、deg2≈30%/deg3≈56%/deg4≈13%、2×2 空块 30~33%、
永远单连通。挑不出任何差别。

然后扫 L201~L271 全部 36 关，把自由格数的奇偶和过滤率对上：

| | 关数 | 过滤失败 |
| --- | --- | --- |
| **自由格数为奇** | 13 | **0 关**（全部 0.2~1.9%） |
| **自由格数为偶** | 23 | **6 关**（26%） |

**奇数关 13/13 全部成功；失败的 6 关全是偶数关。**

### 原因：**不是**哑点的个数（一分钟实验直接证伪了我的第一个猜想）

- **奇数格**：两端点同色，模型里只挂**一个**哑点（容量 2，连多数色的所有格）
- **偶数格**：两端点异色，挂**两个**哑点（各容量 1，各连一种颜色的所有格）

我最初的猜想是：每个哑点都是**连向该色全部格子的 hub**，hub 在残量图里制造大片强连通；
偶数关有两个 hub，连通性翻倍导致 SCC 糊成一块。

**v36 直接把这个猜想证伪了。** 改成「一个穿透哑点」（左格→D_in→D_out→右格，中间容量 1，
语义完全等价、hub 减半、两端还天然耦合在同一个单位上）：

| 关 | v35（两个哑点） | v36（一个穿透哑点） |
| --- | --- | --- |
| L217/223/237/239/257/271 | 100% | **100%**（一关都没救回来） |
| L215 | 1.5% | **25.5%**（反而变差） |

不但没救回失败的关，还把本来好的 L215 弄差了 —— 穿透结构反而制造了**额外**的残量连通
（左格→D→右格 相当于给图加了一批捷径）。

**所以：奇偶相关性（奇数关 13/13 全成功）是硬事实，但机制不是 hub 个数，还没查清。**
下一步该做的是**直接观察**而不是猜：在一个失败关和一个成功关上，
把残量图的 SCC 大小分布打出来对比，看是「一个巨块」还是「多块但哑点在巨块里」，
再决定往哪修。

### 下一步的三个候选（按我的把握排）

0. **先观察再猜**：打印失败关/成功关的残量图 SCC 分布（块数、最大块占比、哑点落在哪块）。
   v36 的教训是不看数据直接改模型会白干。
1. **削 hub 的扇出**：哑点不连「该色所有格」，而是连一个**已经被别的规则缩小过的候选集**。
   per-start 的流实测端点候选只剩 0.9~2.4%，说明信息是有的，只是全局阶段没利用。
   可以先跑一轮廉价规则拿到粗候选集，再用它约束哑点。
2. **把路径建成「过虚拟点 ∞ 的哈密顿圈」**：所有真实格度数一律为 2，模型统一，
   不再有奇偶两套代码路径。要小心它会破坏二部性（∞ 同时连两种颜色），
   得确认 max-flow 的分层结构还成立。
3. **两个哑点之间加耦合约束**：现在它们各自独立（各自饱和），
   但真实约束是「这两个端点是同一条路径的两头」。这个耦合在纯流模型里怎么表达是开放问题。

（v34 的块对联合假设算是 3 的一个粗粒度近似：确实杀掉 62% 的块对，
但每块都还有可行搭档，按块淘汰不掉格子。要在**格**的粒度上做才有用。）

## 今晚（2026-08-21 通宵）的账

**分数：221 → 237**，零 FAIL。其中 16 关来自一个参数值，0 关来自新算法 ——
这本身就是个结论：**这一版的瓶颈已经不在"缺规则"，而在"起点彩票的方差"**。

正结果：
- PROBE=8000（+16 关，见上）
- v23 自适应分层（同分但尾部 8 倍，最慢关余量更大）
- 一条**无条件成立**的新约束：相邻两格不能背对背互相进入（真解上违反 0 次）
- 一条 sound 但暂时用不上的规则：**桥 ⟹ 必须直行**（初始图上 0 条桥，但边被禁用后会冒出来）

负结果（都有解释，不只是数字）：
- v21 无差别分层、v22 预算 portfolio：多花的钱摊在不需要的关上
- 起点打分信号（传播强度）：只有 2/7 的关有区分度
- 有向层单独跑、跨链偏序、瓶颈分区的四种用法：见前面各节

诊断（这些比分数值钱）：
- **规则够强，缺的是种子**：喂真解 25% 的 in 方向能推出 69%、50% 推出 93%
- **种子的分布比数量重要**：连续前缀（DFS 能给的）+0.0pt，随机散布 +45.9pt
  —— 这条同时解释了「在搜索里做传播」为什么只在很浅的地方划算
- **纯结构约束刻画不了 Coil**：5×5 空盘 8648 条哈密顿路径，加互斥后还剩 4048，
  而 Coil 解只有 32。时序是本质的，绕不过去

下一步（按把握排）：
1. **2-level probing**（同时假设两个 in 方向）—— 单层已经收敛在 12%，双层是自然的下一档，
   Tron 的 "bounded brute force" 多半就是这个量级。要先把 python 原型 C 化并做增量传播
   （trail 撤销），全量重算的原型跑不动。
2. **桥规则做成动态的** —— 在传播的不动点迭代里重算桥（边被禁用后会出现），
   或者在搜索内部用。这是唯一一条"把时序条件转成连通性条件"的 sound 规则。
3. **有界窗口内含时序的枚举** —— `catalog.py` 的思路，但窗口不按瓶颈切而是以格子为中心。
   这是最接近 Tron 描述的形态，也是代价最不确定的一条。

## ⭐ v38/v39：分层计时把 v35 的排序打脸了 —— 链定向排错了位置

v35 按「规则代价」排序（传播 → 流 → probing），代价是**估计**的：probing 要几千次 `try_edge`，
流只有一次 max-flow。但真实的时间分布和证伪贡献从没量过。v38 加了分层计时，L223 一个 shard：

| 层 | 时间占比 | 证伪掉的起点 |
| --- | --- | --- |
| 传播 | 0.7% | 0 |
| 流1 | 6.5% | 0 |
| **probing** | **62.1%** | **0** |
| 流2 | 3.8% | 0 |
| **probe2** | **26.6%** | **0** |
| **链定向** | **0.3%** | **976（98%）** |

**probing 吃掉 89% 的时间、一个起点都没证伪；链定向只花 0.3% 就证伪了 98%。**

排错位置的原因也清楚：链定向原本是「产出定向信息」的收尾步骤，没人拿它当**证伪器**看。
但它其实是这堆规则里唯一带**时序**的那个 —— 链是路径的连续段，链上拐弯点的前方格若落在
本链更晚的位置就矛盾；而传播/流/probing 全都只在**结构**层面打转（度约束、不成环），
结构层面刻画不了 Coil 这件事早就测过了（5×5 空盘加互斥后还剩 4048 个结构解对 32 个 Coil 解）。

v39 把它提到 probing 前面（JOBS=4 口径）：

| 关 | v35 | **v39** |
| --- | --- | --- |
| L215 | 0.15s | 0.14s |
| **L223** | **42.5s** | **11.8s** |
| L235 | 0.52s | 0.52s |
| L239 | 67.3s | 66.9s |
| L249 | 2.08s | 2.09s |
| L257 | 123.8s | 126.9s |

**L223 快 3.6 倍**，其余持平（L257 略差 2%）。

**排序原则要改**：不是「按规则代价从低到高」，而是**「按单位代价的证伪量从高到低」**。
一条又便宜又强的规则被排在最后，等于让 98% 的起点先付了最贵的那笔钱。
这条对以后每次加新规则都适用 —— **加完必须量它的「证伪量 / 代价」，再决定插在第几层**。

## 全局过滤失效的机制：查清了一半

按 notes 上一轮定的「先观察再猜」，v37 把残量图的 SCC 分布直接打了出来：

| 关 | 含格 SCC 块数 | 最大块占比 |
| --- | --- | --- |
| L215 / L219 / L235 / L249（过滤有效） | 1796 ~ 2357 块 | **2.3~3.4%** |
| L217 / L223 / L239 / L257（过滤失效） | **1 块** | **100%** |

**断崖式，没有中间态。** 所以不是「哑点恰好落在最大块」（那种是可修的），
而是**整个格子图在残量图上强连通** —— 交替环遍布全图。v36 那个「两个 hub」的猜想到此彻底出局，
连「hub 落在哪块」都不是问题，因为**只有一块**。

两条后续实验：

- **一条边打不碎它**：逐条固定未定边（必用/禁用各试一次，试了 30 条边 60 次假设），
  L223 的端点候选恒为 3992，一次都没动。**所以不是「只差一点点种子」**，
  全局层面的单边 probing 这条路也堵死了。
- **连通性确实是缺失的约束，但它区分不了这两组**：把可行流还原成 2-因子数连通块 ——
  L215 有 **146** 块、L223 有 **142** 块、L257 有 **207** 块，而哈密顿路径要求 **1** 块。
  松弛解散成一百多个碎圈，可见连通性缺得很厉害；但**成功组和失败组的块数一模一样**，
  所以它解释不了差异。而且流模型本身装不下 subtour 约束（这正是 TSP 要用 cutting plane 的原因）。

**结论：全局流对那 6 关是无能为力了**，不用再往这个方向修模型。差异的机制仍未查清，
但已经排除了：哑点个数、哑点落在哪块、种子不足、连通块数量。

## 下一个待查：L239 的起点为什么杀不掉

v38 的计时顺带暴露了另一件事 —— 同样是「全局过滤失效」的关，两者差别极大：

    L223 shard0：998 次调用，链定向证伪 976（98%）
    L239 shard2：1144 次调用，**总共只证伪 2 个**

L239 上**所有规则加起来几乎杀不动任何起点**。这跟「过滤失效」不是同一回事
（L223 也过滤失效，但起点照样被杀掉 98%）。下一步该查的是这个。

## ⭐ v40：probing 是纯浪费 —— 流早就把它覆盖了，白烧了 10 个版本

顺着 v38 的计时往下查，发现 probing 在**所有**瓶颈关上都是「89% 的时间、0 个证伪」。
它唯一的实际产出是「更多必用边」，而那件事现在被 `do_flow` 的 Régin/SCC 过滤覆盖了 ——
probing 是 v20 时代加的，那会儿还没有流（v30 才有）。**加了新规则之后，没人回头量过旧规则。**

关掉之后（JOBS=4 口径，16 关全测）：

| 关 | v39（带 probing） | **v40（去掉）** |
| --- | --- | --- |
| L195 | 20.6s | **7.7s** |
| **L239** | **59.0s** | **13.3s**（4.4 倍） |
| L257 | 113.1s | **60.5s** |
| L263 | 41.3s | 33.3s |
| L249 | 1.46s | 1.11s |
| L223 | 11.8s | 13.5s（唯一略差） |
| L139/L165/L201/L213/L219/L231/L245/L251 | — | 全部略快 |

**16 关里 15 关更快，只有 L223 差 14%。**

**教训（比这次提速值钱）：加了新规则之后，要回头量旧规则还剩多少边际贡献。**
这个仓库的规则是一层层叠上去的，从来没做过反向检查 —— probing 白烧了 89% 的时间，
烧了整整 10 个版本（v20 到 v39）。`PROBEROUNDS>0` 可以把它开回来做对照。

配合 v39 那条（排序要按「单位代价的证伪量」而不是「代价」），可以合成一条例行动作：

> **每加一条规则，量三个数：它的耗时占比、它证伪掉多少、以及关掉它之后总时间怎么变。**
> 前两个决定它排第几层，第三个决定它该不该留在里面。

## 三个瓶颈关的病灶其实各不相同

同样是「全局过滤失效」的关，v38 的计时把它们区分开了：

| 关 | propagate_strong 的证伪 | DFS 探针之后的存活率 | 病灶 |
| --- | --- | --- | --- |
| L223 | 998 次调用证伪 **976（98%）** | 19~25 / 998 | 已经好了，v39 的链定向前置解决 |
| L257 | 1319 次调用证伪 **6（0.5%）** | 442~475 / 1319（**存活 35%**） | 规则杀不动，全靠 DFS 探针硬扛 |
| L239 | 1144 次调用证伪 **2** | — | 同 L257 |

**L223 那种（规则能杀 98%）已经不是问题了；L239/L257 那种（规则只能杀 0.5%，
起点靠 DFS 探针一个个硬试）才是现在真正的瓶颈。**
注意这跟「全局过滤失效」不是一回事 —— L223 也过滤失效，但它的起点照样被规则杀光。
下一步要查的是：L239/L257 的起点为什么连链定向都杀不动。

## 同口径对照：v35 → v40 是 +20 关

之前的成绩都是 28 核跑的，这轮为了不占满机器改用 **JOBS=6**，所以重跑了对照：

| 版本 | JOBS=6 / 30 秒口径 |
| --- | --- |
| v35（这轮之前的最好版本） | **235** |
| **v40**（v39 链定向前置 + 去掉 probing） | **255** |

**+20 关，全部来自「把已有规则重新排序 + 删掉一条冗余规则」，没有加任何新规则。**

## PROBE 的最优值**第二次**翻转

去掉 probing 之后又扫了一遍探针预算：

| 关 | P=2000 | P=8000（v20 时代定的默认） |
| --- | --- | --- |
| L239 | **15.8s** | 23.9s |
| L257 | **54.9s** | 73.9s |
| L195 | **4.9s** | 9.5s |
| 中小关 9 关 | 全部持平（L231/L223 略差，绝对值都很小） | |

历史轨迹：**v13 时代 2000 最优 → v20 加了 probing 后 8000 最优（+16 关）→ v40 去掉 probing，
又变回 2000。** 每次底层一动，这个常数的最优点就跟着搬家。

**同一个坑踩第二次了，写成规矩：凡是「最优常数」，底层改动之后必须重扫一遍。**
这个仓库里现在还有一批没复查过的常数：`PROBE2`、`TIER2`、`PROPDEPTH`、`probe_rounds`、
`MAX_CELLS`/`MAX_NODES`（catalog）——它们各自的最优值都是在不同版本上定的。

## L239/L257 的链为什么杀不动起点：不是链太短

L223 的链定向证伪 98%，L257 只有 0.2%。先怀疑是必用边少、链太短（`chain_dir_ok` 要求链长 ≥3
才有内部拐点可查）。v41 量了一下，**猜错了**：

| 关 | 平均必用边 | 链数 | 长度≥3 的链 | 最长链 |
| --- | --- | --- | --- | --- |
| L223（链定向证伪 98%） | 2394 | 531.7 | 461.2 | 58 |
| L239（证伪 0.1%） | 2588 | 615.6 | 533.4 | 44 |
| L257（证伪 0.2%） | **3088** | **671.2** | **592.2** | **73** |

**L257 的链更多、更长、必用边更多**，杀伤力却差两个数量级。所以差别不在链的**规模**，
而在链的**几何** —— 链定向要靠「拐弯点的前方格恰好落在本链上、而且位置更晚」才能否掉一个方向，
这取决于链自己蜿蜒到什么程度、有没有擦到自己。下一步要量的是这个（每条链有多少个可判定的拐点），
而不是再猜。

## 全规则消融矩阵：当前配置基本就是最优组合

v40 靠「重排 + 删冗余」拿了 20 关，自然要问其他规则是不是也冗余。逐个关掉量（JOBS=3/4）：

| 规则 | 关掉后（代表关） | 结论 |
| --- | --- | --- |
| 流（`FLOW=0`） | L195 5.5→26.0s，L223 16.8→68.5s | **最重要的规则**，独扛 4~5 倍 |
| 链定向（`CHAIN=0`） | L223 +28%，L239 +25%，L195 反而 −13% | 保留。它证伪的起点 DFS 探针也能杀，价值是「便宜」不是「独家」 |
| 搜索内传播（`PROPDEPTH=0/8`） | ±10%，噪声级 | 边际≈0。v18 时代的东西又一条被时代淘汰，但不碍事就先留着 |
| **自适应第二层（`TIER2=999` 关掉）** | **L239 13.9→63.1s，L257 47.5→204.2s** | **v23 的机制在新底座上依然关键，4 倍** |

加上已有的两条（probing 已删、PROBE 已回调 2000），**propagate_strong 的每一环都过了一遍
边际贡献检查**。当前的组合：传播 → 流 → 链定向 → （按需）大预算二层 → 无限搜。

并行利用率也查了：L257 全程 6/6 shard 满载，无提前退出 —— 工作窃取没有空间。

## 现在的瓶颈画像（JOBS=6，30 秒口径 = 255）

卡在 **L257**（~40 秒）。它的特征：起点证伪率只有 65%（L223 是 98%+），存活的 442 个起点
全靠 DFS 探针硬扛；链定向在它身上几乎无效（证伪 0.2%），且已证实**不是**链太短
（它的链比 L223 更多更长），是链的几何——拐弯点的前方格没落回本链。

三个没做完的方向：
1. **量链的「可判定拐点」密度**（L223 vs L257），把几何差异钉死 —— 然后看有没有办法
   把「前方格在别的链上」的情形也用起来（之前跨链偏序失败是在旧底座上，
   现在必用边多了近一倍，值得重测）。
2. **端点对称性**：起点和终点在无向层完全对称，而我们只枚举起点。若终点候选被流压得很小
   （L215 那类），从终点侧枚举可能快得多 —— 每关挑「候选少的那一侧」开搜。
3. 600 秒全量口径重跑（上次 v32 是 318，现在底座又快了不少）。

## 任务 1 结案：跨链偏序在新底座上也是负结果，死因换了一个

v42 把跨链时序偏序 + 环检测 + 不动点完整做了（旧底座上失败是因为跨链引用为 0，
新底座必用边 46~59%、每次调用有 80~100 个跨链引用，值得重测）。结果打平
（L223 16.1s vs 16.2s，L257 65.3s vs 62.1s），插桩一看：

    L223: 偏序边 0.3 条/次, 环杀 0, 方向排除 37
    L239: 偏序边 0.0 条/次, 环杀 0, 方向排除 0
    L257: 偏序边 0.0 条/次, 环杀 0, 方向排除 0

**死因和旧底座不同**：旧底座是「没有跨链引用」；新底座引用有了，但偏序边的前提是
「链已定向」，而 L239/L257 上几乎所有链**两个方向都可行**（这正是 v41 量出的「双否为 0」
的另一面）。没有定向的链，就没有可提交的偏序边，环检测无从谈起。

链定向这个家族到此摸清了：它在「链会自己擦到自己」的关上是大杀器（L223 证伪 98%），
在链两个方向都通的关上整族失效（L239/L257）。**这不是实现问题，是这些盘面的链几何**。

## 任务 2 结案：终点候选剪枝 —— 候选很小（~40 格）但当剪枝用不划算

先纠正一个设想：「从终点侧枚举」不可行 —— Coil 解不是时间对称的（早测过），反向搜索的
滑行规则变成「转弯时**出发方向的反侧**必须是墙或未访问」，意味着可以在滑行中途任意停，
分支因子爆炸。放弃。

改做 sound 的变体：per-start 的流在 Régin/SCC 过滤时顺带算出「谁还可能当终点」，
实测**恰恰在瓶颈关上候选极小**：

| 关 | 自由格 | per-start 终点候选（平均） |
| --- | --- | --- |
| L223 | 3992 | **32.0（0.8%）** |
| L239 | 4578 | **40.4（0.9%）** |
| L257 | 5278 | **47.8（0.9%）** |

v43 把它接进 DFS：维护「未访问的终点候选数」，清零即剪枝（路径的终点必然在未访问格里，
而终点必在候选集里）。踩了一个真坑：第二/三层复用 est_save 时 tcand 没跟着换，
用了**别的起点**的候选集，误杀真解（4 关 FAIL）。修法是候选集随起点一起缓存（tc_save），
加 tcand_for 防串台。修完 sound，但**全面略慢**（L257 10.6→13.7s，其余 +5~15%）：
40 个候选散布全盘，搜索通常先死于别的剪枝，轮不到「候选耗尽」触发；而每起点多一份
N 字节的缓存拷贝是实打实的。**负结果。**

但这个测量本身有价值：**终点候选平均只有 40 个**这条信息还没有被真正用对地方。
「计数到零才剪」太弱；更强的用法是**方向性**的——比如把这 40 个格子当 DFS 的目标集合
做启发式排序（朝候选区走），或在 reach_local 里顺带检查「至少一个候选可达」。
第二个想法每节点多一次标记查询，值得试但这轮先记下。

## 满核复测的意外收获

给 v43 做对照时发现：**满核（28 核）下 v40 的 L239 只要 2.6 秒、L257 只要 10.6 秒**，
都远在 30 秒线内 —— JOBS=6 口径的 255 分严重低估了满核成绩。正在跑满核完整评测。

## ⭐ v44：死端必须是终点候选 —— tcand 的正确用法找到了

v43 的教训是「计数到零才剪」太弱。换一个角度看同一份信息：**deg≤1 的未访问格只能是路径的
终点**（进得去出不来），而终点必须在 tcand 里 —— 所以**不在候选集里的死端出现即矛盾**。
这比原来的死端规则（「死端最多 1 个」）强了一整个维度：原规则数**数量**，新规则还看**身份**。

实现上与 low_cnt 完全镜像：`ntc_low` = 未访问、deg≤1、且非候选的格子数，mark/unmark O(1)
增量维护，`cheap_ok` 里做同款邻接修正（deg0 邻居效果度 1 仍是死端，不能减免）。
soundness 五关逐步验证零报警（L139/L195/L215/L239/L257）。

| 关 | v40 | **v44** |
| --- | --- | --- |
| L257 | 10.2s | 10.2s |
| **L287** | **23.5s** | **4.0s**（5.8×） |
| L297 | 17.1s | 10.0s |
| L317 | 5.2s | 3.2s |
| **L319** | **>300s** | **271s** |
| L321 | 0.67s | 0.15s |

（中途虚惊：v40 在 L319 的 stderr 里出现 "no solution found"，一度怀疑不 sound；
回看是命令把 solver stderr 收进了计时文件，那只是某个 shard 正常搜穷自己那片的输出，
timeout 杀掉后 tail 抓到了它。**以后判「全体无解」只能看退出码和 stdout，不能看 stderr 片段。**）

## 满核口径修正：v40 = 317（不是 255）

用户释放核后重跑：**v40 满核 = 317 关**（30 秒口径，零 FAIL），JOBS=6 的 255 严重低估。
最慢的关全换血了：L287 23.0s、L297 18.0s、L257 10.1s —— 之前的 L239/L257 危机已经解除。
v44 的完整评测在跑。

### v44 的第一次完整评测在 L39 上 FAIL —— v32 的老坑第三次出现

大关的逐步 soundness 全过，**17×15 的 L39（自由格数为奇）当场 FAIL**。
病根就是仓库里记过的那条：奇数关（终点色 == 起点色）时哑点挂在**汇侧**，
弧是 cell→FDUM，从 `fhead[FDUM]` 遍历到的是**反向弧**，`fcap` 记的是已用流量 ——
「这条弧用没用」的判据和偶数关（源侧，正向弧）**正好相反**。

要命的是：这段代码（`end_cand` 统计）**从 v30 起就是错的**，但它之前只当展示用、
错了没人看出来；v44 把它接进剪枝，错误第一次变得可观测。修完后 L39/L41/L99/L175 全过，
奇数关逐步 soundness 零报警。

**教训（这个坑的第三次）**：残量图上「弧用没用」的判据依赖哑点挂哪一侧，正反向弧相反。
凡是新代码要消费 `FDUM` 弧的信息，第一件事是查当前奇偶下哑点在哪侧。
另一条更普遍的：**「只当展示用」的代码是潜伏的错误**——它不被验证，直到有一天被拿去当依据。

### v44 修复后完整评测：317，同分但尾部大改善

| | v40 | **v44** |
| --- | --- | --- |
| 30 秒口径 | 317 | **317**（同卡在 L319） |
| 最慢关 | L287 23.0s | **L257 10.6s** |
| L287 | 23.0s | **4.1s** |
| L317 | 5.1s | **0.46s** |
| L319（线外） | >300s | 271s |

**分数没动是因为 L319 单独立了一道墙（271 秒）**，其余全线提速。零 FAIL，
奇数关（L99/L175）和大关（L139/L257）逐步 soundness 都是零报警。**当前主版本 v44。**

## 今天两项任务的账（2026-08-21 白天）

任务 1（链几何 → 跨链偏序）：**负结果链条，但每一环都有数据**——
拐点分布三关几乎一样（墙 79~82%）→ 不是分布问题；证伪路径统计发现 L223 的杀伤全来自
「他链双否」1290 次而 L239/L257 为 0 → 差别在「链会不会自己擦到自己」；
跨链偏序完整实现后偏序边 0.0~0.3 条/次 → 前提「链已定向」在瓶颈关不成立。整族关闭。

任务 2（端点对称）：「从终点侧枚举」不可行（Coil 不时间对称，反向滑行可中途停，分支爆炸）；
「终点候选计数」太弱（v43，略慢）；**但第三个形态成了：死端必须是终点候选（v44）**——
L287 5.8 倍、L317 11 倍，满核 317 关且尾部余量翻倍。

外加两个修正：满核口径 v40=317（JOBS=6 的 255 严重低估）；
奇数关反向弧判据从 v30 起就错、v44 第一次消费才暴露（L39 FAIL → 修复）。

**下一个瓶颈非常具体：L319（110×108，271 秒）。** 它是新的「起点彩票之墙」。

## v45：swarm 模式 —— L319 从 271 秒压到 45 秒（6 倍），然后撞到真正的墙

### L319 是一种全新的瓶颈类型

给它跑起点统计，画像和之前所有瓶颈关都不同：**全局过滤工作得极好**
（起点候选 94/8149 = 1.2%，全局定死 4962 条边 = 61%），探针再杀到只剩 11 个幸存者。
**起点彩票已经赢了** —— 但时间线显示：探针阶段 1 秒结束，27 个 shard 因为自己那片
没有幸存者而退出，剩下**一个 shard 单核扛 271 秒**。

### 对策：候选极少时全员同攻（portfolio DFS）

`swarm` 模式：候选总数 ≤ 4×核数时放弃「按下标分片」，每个 shard 处理**全部**候选，
靠**不同的搜索顺序**搏运气 —— 这些核本来就是白扔的，管道协议不变（谁先解出谁赢）。
逐步叠了四层多样性，每层的实测：

| 手段 | L319 |
| --- | --- |
| （v44 基线，单核扛） | 271s |
| + DFS 同分抖动（shard 专属种子，只洗同分的牌） | 124.8s |
| + 浅层轮转（前 3 层每个 shard 强制选不同分支，真正切树） | 84.1s |
| + 起点顺序轮转（11 个幸存者摊给不同 shard 先试） | 82.0s（没用） |
| + **策略梯队**（纯 Warnsdorff / 同分抖动 / 大幅抖动 / 纯哈希序 各 7 个 shard） | **44.7s** |
| 反 Warnsdorff 档 | 141s（负资产，回滚） |
| 大幅抖动档从 7 席加到 14 席 | 45s（无变化） |

两个值得记的观察：
- **起点顺序轮转没用** —— 赢家 shard 固定是同一个（tier 2 的 18 号），说明幸存者排序的
  第 7 位就是真起点，大家很快都会轮到它；瓶颈从头到尾是**真起点自己那棵树**。
- **扩席位没用** —— 更多种子没找到更快的顺序。portfolio 在 45 秒饱和：
  这是「Warnsdorff 系 + 哈希系」顺序空间里最好的运气了。

### 现状与墙

v45 完整评测确认无回归（swarm 只在秒级易关上加几十毫秒）。**L319 稳定 45 秒，30 秒线外**，
分数因此仍卡 317。剩下的 1.5 倍只能靠剪枝/搜索本身，顺序运气榨干了。

下一个有把握的方向（Hippo 的 "path shortcuts"，我们自己的数据也支持）：L319 全局定死 61% 的边，
必用边拼成的链已经覆盖大半盘面 —— **把链收缩成原子单位来搜索**（度 2 链收缩），
搜索深度和分支数都会数量级下降。这是个大工程，但对象已经非常明确。

## ⭐ 一行改动 4 倍：reach_local 的洪水从 DFS 序换成 BFS 序

portfolio 饱和后按老规矩上 profile。gprof（`-fno-inline` + FORCESHARD 单进程复现赢家）
把账算得明明白白：

    reach_local   95.7%   （1.23 亿次调用，每次 ~1.9μs）
    mark/unmark    2.4%
    其余           <2%

**整个搜索的成本就是连通性检查一件事。** 而 reach_local 的病根在洪水的**顺序**：
它要回答「滑行线的几个自由邻居是否连通」，那些源通常就在线两侧、绕线头几步就接上了；
但栈序（DFS）洪水会乱走远路 —— 先一头冲到盘面另一边，把半张图灌满才绕回来接上。
换成队列序（BFS），成功情形只探「最远源的距离」那个半径的邻域，
失败情形（真断开）和原来同价（都要灌满整块）。

    L319: 41s → 10.0s（4 倍），进 30 秒线

**这可能是全仓库性价比最高的一次改动**：改了一行数据结构的使用方式（`--top` 换 `head++`）。
教训跟 v39/v40 一脉相承：**热点要量，不要猜** —— 之前一直以为瓶颈在「树太大」（搜索层面），
实际是「每个节点付的连通性检查太贵」（实现层面）。portfolio 那 6 倍是在给一个
本可以便宜 4 倍的循环买并行。

配套小项：bench 编译参数升到 `-O3 -march=native -funroll-loops`（实测白捡 10~18%）；
`FORCESHARD` 环境变量单进程复现任意 shard 的种子/梯队（profile 专用）。

### BFS 版完整评测：**387**（+70 关）

一行改动的完整战果：**317 → 387**，零 FAIL。尾部依然健康 —— 最慢的过关是 L363 的 11.9 秒，
L319（之前的 271 秒之墙）现在 10.0 秒。**已超过公开代码的最高纪录（ericgopak 332，不限时），
而我们是 30 秒限时口径。** 距离官方榜各家模型的最高分（kimi 2.6 的 264，600 秒口径）
也已经是碾压位置 —— 600 秒口径待重跑，预计远超 400。

新墙：**L389（132×132）**。

### PROBE2=48000 完整评测：**411**（+24 关）

387 → **411**，零 FAIL，最慢过关 L403 的 13.9 秒。今天一天的曲线：
**317（早上）→ 387（BFS）→ 411（PROBE2）**，两个改动各一行。
搜索路线的老天花板估算（L350~450）已经踩进去了——而且是 30 秒口径。

新墙：**L413（140×140）**。

## 全量口径（奇+偶，600 秒/关）：**412**

用户提醒偶数关一直没在快速口径里 —— 补了 `coilbench/evaluate_all.py`（直接吃 `levels_all/`
的全部 1207 关，不需要解密偶数包），跑了 600 秒/关的官方口径全量：

**412 关，零 FAIL**（停在 L413 吃满 600 秒）。历史对照：上一次全量是 v32 的 **318**；
官方 results.md 各家模型的最高分是 **264**（kimi 2.6）。**偶数关全程零 FAIL** ——
奇偶两套代码路径（哑点两侧、奇偶种子）都被全面踩过了。

注：这一轮跑的时候 solver 还是 PROBE2=24000 的旧二进制（部署没跟上源码，教训 +1：
**跑长评测之前先确认 solver 二进制和最新源码一致**）。已用 PROBE2=48000 的新二进制
从 L413 续跑。

## 多源 BFS：负结果

reach_local 的下一步优化尝试 —— 所有源各带标签同时扩、相遇即并（小并查集），
理论上典型情形半径减半。soundness 六关全过，但实测**更慢**：

| 关 | 单源 BFS | 多源 BFS |
| --- | --- | --- |
| L319 | 10.5s | 16.5s |
| L383 | 9.3s | 16.8s |
| L403 | 14.2s | 21.8s |

原因：单源 BFS 的早退已经很快，而多源版每条边要做「标签读取 + find + 可能 union」，
每邻居的常数开销超过了省下的探索面积。**内层循环的每一条指令都很贵，
「理论上更少的访问」不敌「每次访问更贵」。** 已回滚到单源版。

## 两个口径的最终定格与 L413 的画像

- **30 秒/关（奇数关口径）：411**
- **600 秒/关（奇+偶全量，官方口径）：412** —— 新二进制（PROBE2=48000）重试 L413 仍吃满 600 秒。

**L413（140×140，13157 自由格）成为两个口径共同的墙**，而它的画像和 L389 完全相反：

    全局阶段：固定 9358 条边（71%！），起点候选 19 / 13157（0.1%）
    探针后幸存 5 个 —— swarm 28 核、28 种顺序、5 棵树，600 秒不够

**起点彩票在它身上已经被彻底解决了**（19 个候选！），墙是纯粹的单树搜索深度。
和 L319（94 候选/45 秒）同类但深一个量级 —— 这类关上 portfolio 的顺序运气已经榨干，
唯一能改变结局的是**让树本身变小**：

1. **度 2 链收缩**（Hippo 的 path shortcuts）：71% 的边已被全局定死，链覆盖了大半盘面，
   把链当原子单位搜索，深度和分支数都数量级下降。这是下一个大工程，对象前所未有地明确 ——
   L413 就是它的验收标准。
2. 分支点的强传播（v18 只在 D=2 划算是 BFS 之前的结论，常数规矩说了要重扫）。

## 今天的总账（2026-08-22）

| 口径 | 起点 | 终点 |
| --- | --- | --- |
| 30s 奇数关 | 317 | **411** |
| 600s 全量 | 318（v32 历史） | **412** |

四个改动：BFS 一行（+70）、-O3（~15%）、PROBE2=48000（+24）、swarm（L319 类 6 倍）。
三个负结果：多源 BFS、反 Warnsdorff 档、超订阅。零 FAIL 贯穿全程，偶数关全面验证。
官方模型纪录 264、公开代码纪录 332，均已大幅超过。

### v48 完整评测：**445**（+34），零 FAIL

30 秒口径：**411 → 445**。最慢过关 L383 26.6s（唯一的深传播受损关，仍在线内），
次慢 L389 11.5s（自适应正确降档）。今天全天曲线：**317 → 387 → 411 → 445**。
新墙：**L447（152×151）**。600 秒全量口径用 v48 从 L413 续跑中。

### PROBE2=32000 完整评测：**471**（+26）

L447 拿下后一路推到 **L471**，零 FAIL。最慢过关 L383 25.0s、L447 24.0s、L467 21.3s。
今天全天：**317 → 387 → 411 → 445 → 471**。

两个口径的墙分化得很清楚：
- **30s 奇数口径（471）的墙**：传播强类的大盘（L473，160×160）—— 参数和常数还有边际空间；
- **600s 全量口径（413）的墙**：L414（141×140），**传播零信息类的放大版**
  （全局 0 条边、13440 起点全候选、探针存活 57%）—— 现有工具全部无效，
  这一类是真正的算法边界，v30 时代查清的「残量图一整块 SCC」问题至今无解。

### SWARMX 4→6 完整评测：**473**；两个口径的墙统一为「传播零信息类」

L473 因 138 个候选卡在 swarm 旧阈值（112）外面而没开 portfolio——参数扫描全部无效后
查出来的。阈值改成可配（`SWARMX`，默认 6）后 87s → 12.5s，完整评测 **473**，零 FAIL。

至此两个口径的墙**统一了**：
- 30s 奇数口径（473）：墙是 **L475**（162×160）—— 全局 0 条边、17192 候选，传播零信息类
- 600s 全量口径（413）：墙是 **L414**（141×140）—— 同类

**传播零信息类**（自由格数偶、残量图一整块 SCC、全局过滤/传播/链定向/probing 全部无效）
现在是唯一的边界。这一类的病根 v30~v37 时代查得很透：度约束松弛下这些实例真的自由
（固定一条边都打不碎巨型 SCC）。搜索层面它表现为「几千个起点都活着、每棵树都不小」。

今天全天（2026-08-22 深夜）：**317 → 387 → 411 → 445 → 471 → 473**（30s 口径，+156）；
600s 全量口径 318 → 413。四个大改动：BFS 一行、全深度分支传播（千倍那刀）、
PROBE2/SWARMX 重扫。八个负结果全部入档。

### L414 探针预算重扫：大预算无效，但默认参数 656.78s 解出——墙在 600s 线附近抖动

传播零信息类最后一个便宜想法：探针存活率 57% 意味着 probing 大多在烧时间，
那反过来加大预算能不能把存活率打下去？三档重扫（各 700s 上限）：

| PROBE | 结果 |
|---|---|
| 2000（默认） | **656.78s 解出** |
| 20000 | 未解出（DYN 证伪率 19%，不达标） |
| 100000 | 未解出（DYN 证伪率 20%） |

两个结论：
1. **大预算负结果**：这一类的起点大多真的活着，探针烧不死，加预算纯亏。
2. **L414 不是硬墙，是 600s 线附近的抽奖**：默认参数能在 656.78s 解出，另一次
   605s 超时未中。差距只有 10% 量级——换句话说传播零信息类的「不可解」
   其实是「刚好比预算贵一点」，方差实验（3 次重复）在跑。

如果重复实验有一次落进 600s 内，600s 全量口径就能从 413 续推；
就算全部 miss，10% 的差距也意味着任何一个小常数改进都可能翻盘——
这比「需要全新算法」的预估乐观得多。

### L414 认识修正：不是抽奖，是**确定性的 ~656s**；瓶颈画像重画

三次重复（605s 上限）全部 miss，加上更早一次 605s miss 和一次 656.78s 解出——
五个数据点完全自洽：每个分片的工作是**确定性的**（切片固定、顺序固定、无随机源），
解总是在 ~656s 到来，605s 的「miss」只是切在它前面。要过 600s 线，
需要的是**确定性的 ~10% 提速**，不是运气。

顺带修正两个误读：
1. 分片日志「probe: 13214/13440 起点被证伪，剩 226」的分母是全局 13440，但
   「剩 226」是**本分片 480 条切片**的幸存数——全局幸存 ≈ 230×28 ≈ **6400 个（48%）**，
   不是 230 个。swarm 不开是对的（阈值判的还是探针前的 ns=13440，但就算判探针后
   也是 6400，照样不够格），SWARMX 加大的假设作废。
2. 时间分布：全局阶段 1s（不是负担）、第一层探针 8s、第二层到 27s、
   剩下 578s 全在第三层无限搜——每分片 ~230 棵树按 sc 序磕，600s 大概只磕得动头几棵。
   赢的那次是某个分片列表前排的一棵可解树，磕了 ~630s。

在跑的确定性提速实验（各 700s）：PROPDEPTH=0 / PROPDEPTH=1（零信息类分支传播
证伪率只有 19~21%，接近 DYNMIN=15 的门线，干脆关掉换节点速度）、NORANK=1
（sc 排序换个顺序，可能把可解树换到更前排）、TIER2=101（跳过第二层省 19s，
代价是第三层列表多 20% 死树）。

### 定点实验大反转：赢家树单核 5~22s 就能解，656s 全烧在它前面的死树上

给 v48 加了实验旋钮 `STARTXY=x,y`（versions/v48-start.c，把候选过滤到指定起点，
ns=1 自动触发 swarm portfolio）。用 637s 那次解的起点 (112,115) 定点攻击：

- 28 分片 portfolio 齐攻：**5s**（vs 基线 637~656s，130 倍）
- 单核单策略（FORCESHARD 各档，120s 上限）：**七档全中** —— tier0 纯 Warnsdorff 5s、
  tier3 哈希序 6s、tier2 大抖动 10~12s、tier1 小抖动 10~14s、最慢 22s

结论彻底反转：这棵树是软柿子，**连基线同款策略单核都只要 5s**。656s 的账在于：
赢家起点排在其所属分片候选表的第 k 位，前面 k−1 棵**死树每棵烧掉几分钟的无限搜**。
传播零信息类的病根不是「树难搜」，而是**死树垄断时间**——第三层无限搜是
为「搜穷即永久剔除」设计的投资，在这一类上每棵死树的搜穷成本太高，投资collapse。

对策（v49 方向）：**零信息类条件下的限额扫荡第三层**。判据 gfix_total==0；
第三层改为多轮递增预算（如 2M -> 16M -> 128M -> 无限），dfs 返回 0（搜穷）永久剔除、
-1（预算尽）留给下一轮。软柿子树在低预算轮就会被收割，死树最多烧一轮预算。
v15 纯迭代加深的教训（重搜浪费）靠三点缓解：类条件触发、指数递增、永久剔除保留。

逐树日志基线（TREELOG=1）在跑，等死树耗时分布和赢家位置 k 落地后定预算常数。
另：两个工程教训——nohup 后台进程必须 launcher `sleep 2` 逗留否则启动竞态即死；
监视器 tail 一律用 `-F`（按名字跟随），否则 rm+重建后跟死 inode。

### 逐树日志基线 + SC2 排序实验：排序思路证伪，账本重写

TREELOG 基线（655s 解出）逐树数据：
- 赢家 (112,115) 在 shard4 第 **#37 位**，自己只要 **4.2M 节点（~7s）**
- 前面 37 棵死树共 374M 节点（~620s）；**死树全部能搜穷**（0.5M~30M，均值 ~10M ≈ 17s）
- 节点速率 ~600K/s；全系统 655s 内 28 分片共搜穷 598 棵树，只有 1 棵 r=1
- 模型：可解起点密度 ~1/600，本质是 28 分片赛跑，谁的候选表前排踩到可解树谁赢

SC2 实验（tier2 的 32K 深探针 best_rem 回填 sc 重排）：**700s 未解出，比基线还差**。
深探针 best_rem 挑出来的是「探得深的巨树」——shard4 前 3 名变成 40M/170M/167M
节点的怪物，700s 只磕完 3 棵。深探针深度 ≈ 树大 ≈ 搜穷贵，和「可解」不相关。
教训：**best_rem 是「能探多深」信号，不是「是否可解」信号，也不是「搜穷多便宜」信号**。

账本结论:赢家 4.2M vs 死树均值 10M 只差 2.4 倍，排序和限额扫荡的理论上限都在
2 倍上下（限额扫荡估算 ~500s+，不够过线）。剩的大杠杆是**把死树搜穷变便宜**：
PROPDEPTH=999999（零信息类从没试过全深度分支传播，探针阶段 DYN 证伪率 19~21%）在测。

### 🔥 PROPDEPTH=999999：零信息类被全深度分支传播击穿——L414 655s→30s，L475 也解出（200s）

排序（SC2）和限额扫荡的理论上限都不够之后，试了从没在这一类上开过的全深度分支传播：

| 关 | v48 基线 | PROPDEPTH=999999 |
|---|---|---|
| L414（600s 口径的墙） | 655s | **30s（22 倍）** |
| L475（30s 口径的墙） | 从未解出 | **200s** |

逐树日志揭示账本被彻底改写：死树搜穷成本 **~10M 节点 → ~15K 节点（600 倍）**，
赢家树 4.2M → 2 万节点，而且换了个更早排到的起点（shard1 第 1 位，(112,116)）。
30s 里大头是探针阶段的 27s，第三层只花 ~3s。

**为什么 v48 的自适应规则漏掉它**：初值规则用「全局定死边比例 >=10%」当开关，
零信息类 gfix=0 直接被判成浅层。但 estate 密度只是传播威力的**一个**来源——
零信息类里传播吃的是残余区域自身的窄缩（DYN 证伪率实测 19~21%），全局定不下边
不代表分支点没肉。L389 型真正无肉的（证伪率 <15%）有 DYNMIN 自测兜底。

v50-deepzero：初值恒深层，降档全权交给自测。一行改动。
教训（第 N 次）：**代理判据（gfix 比例）不如直接测目标量（证伪率）**；
以及「从没在这类上试过的旋钮」比「新算法」便宜一万倍，先扫旋钮。

### v50 完整评测（600s 全量口径）：**413 → 500**（+87），零 FAIL

v50-deepzero 从 L414 续跑：87 关全过，最慢 L475 203s、L458 104s，大多数秒级。
新墙 **L501（170×169）**：又是传播零信息类（gfix 0、19232 候选）——深传播把这一类
的墙从 414 推到 501，类内战争重新变成常数战争（深传播下的探针预算还没重扫过，
「底层一动重扫常数」第 N 次排上日程）。30s 奇数口径回归在跑。

今天两个口径累计：30s 奇数 317 → 473；600s 全量 318 → **500**。

### 30s 奇数口径 v50 回归：L379 超时（30.03s）——但不是 v50 的锅

复测（空闲机器各 2 次）：v48 32/39s，v50 38/39s——**两版在 L379 上等价**
（它 gfix 34%，v48 的规则同样选深层）。这关本来就骑在 30s 线上，v48 拿 473 那次
是抽到了好签或机器更凉。v50 的抽查（383/389/447/467）都在线内，从 381 续跑
30s 口径确认后面没有藏真回归。骑线关（377 也 26s）的方差管理后面再说；
先把深传播底座的探针常数重扫做了——它可能顺手把这些骑线关也拉下来。

### 孤儿分片事故 + v51 三段式传播深度：全部抽查绿，L414 再快到 24s

**基础设施事故**：evaluate.py 超时只杀求解器父进程，fork 出的 28 个分片全成孤儿
烧满全机（load 47）。传染链：600s 评测的 L501 超时留下孤儿 → 30s 口径全程被污染 →
L379「超时」（干净复测 19/20s 稳过）→ 我的 A/B 也被污染。修复：子进程
`prctl(PR_SET_PDEATHSIG, SIGKILL)` + getppid 竞态关窗，已验证（杀父后子清零）。
教训（第三次）：**任何反常测量先查机器**；这次孤儿是 evaluate 的超时路径系统性制造的。

**v50 的真回归**：L381（gfix=0、DYN 证伪率 32%）深传播亏 2.4×（干净 A/B：v48 6~7s
vs v50 15~17s）——证伪率不是充分判据，树本来就小的关深层每节点成本回不了本。
真判据是**浅层探针的第一层存活率**（= 树大不大）：
L381 27~33%、L389 30~37%（深层亏）vs L414 57~62%（深层 22 倍）。

**v51-survdeep 三段式**：gfix>=10% 上深层（强类）；否则浅层探针，第一层存活率
>=45%（SURVDEEP）升深层（零信息大树类）；DYNMIN 自测降档兜底。
抽查：L381 6s、L389 11s（双双复原 v48 水平）、L414 **24s**（比 v50 的 30s 还快——
浅层第一层便宜，第二三层才升深）、L379 18s、L383 24s、L319 3s、L39 0s，全绿。

### v52-reprobe：升档时第 1.5 层深层重探——三类关全部拿到各自最优

v51 的 L475 洞：它的浅层存活率 40% < 45% 阈值，根本没升档，退回 v48 的「解不出」。
类分布：L381 27~33%｜L389 30~37%｜**L475 40%**｜L414 57%。两个修正：

1. **SURVDEEP 默认 45 → 38**（切在 L389 的 37 和 L475 的 40 之间；就算 L389 型
   偶尔越线，代价也只是 v50 时代的 20s，线内）。
2. **第 1.5 层深层重探**（v52 核心）：升档后对第一层幸存者用同样 PROBE 预算深层
   重探——深层证伪 ⊇ 浅层证伪、estate 缓存与 prop_depth 无关，所以重探后的杀集
   与排序**精确等于 v50 全程深层的第一层**，但成本只花在幸存者上（≈60%），
   未升档的关一分钱不付。v51 的浅层 sc 在大树类上质量差（L475 400s+ 未解出）
   就是缺了这一步。

v52（SURVDEEP=38）六关抽查：L414 34s｜L381 7s｜L389 10s｜L379 20s｜L383 25s｜
**L475 212s**（复原 v50 水平）。三段式判据 + 重探 = 每类关都走自己的最优路径。

### v52 完整评测（30s 奇数口径）：**473 恢复确认**，零意外失败

干净机器 + v52：一路推到 L475（212s > 30s）才停——孤儿污染时代的 L379/L381
假超时全部消失。30s 口径的墙如今是 L475 本尊（零信息类 162×160，40% 存活率档），
它在 600s 口径下已经能解，30s 内还差 7 倍。600s 全量口径 v52 从 414 续跑中
（v50 拿了 500，看 v52 的重探排序运气）。

### v52 完整评测（600s 全量口径）：**500 确认**（同 v50，墙 L501），零意外失败

87 关全过（最慢 209.6s），v52 的三段式在全量上没丢 v50 的任何战果。
双口径官方数字：30s 奇数 **473**（墙 L475/212s）、600s 全量 **500**（墙 L501）。
今天累计：30s 317→473（+156）、600s 318→**500**（+182）。

下一层：深传播底座的探针常数重扫（PROBE/PROBE2 是浅层时代的甜点，
「底层一动重扫常数」第 5 次）。L414 扫五档在跑，重点怀疑第二层 32K 深探多余
（深传播下第三层死树 1.5 万节点就死，比第二层预算还便宜）。

### 深传播底座的探针常数重扫（L414 测试台）：曲线噪声大，PROBE~1000 领先

| 旋钮 | L414 时间 |
|---|---|
| 基线（PROBE=2000/PROBE2=32000） | 34s |
| TIER2=101（跳过第二层） | 33s |
| PROBE2=8000 / 64000 | 34s / 34s |
| PROBE=250 / 500 / **1000** / 1500 / 4000 | 29s / 38s / **25s** / 30s / 46s |

两个发现：
1. **PROBE2 在升档类上完全不敏感**（8K~64K 持平）——1.5 层深层重探已经把杀集
   做完了，第二层只是白转一圈。TIER2 跳过也不亏（33s）。
2. **PROBE 曲线噪声 ±30%**：预算改变 sc 排序 = 换抽奖签，单次数字不可信。
   1000 领先（25s）但 250 也 29s，500 反而 38s。要定默认值得做重复实验。

先探更值钱的：L501（600s 口径的墙）900s 探底跑量（默认 + PROBE=1000），
看它是 L414 型的「差一口气」还是真悬崖。

### L501 诊断 + PROPDEPTH 中间档扫描：全深度在大盘撞 O(N) 成本墙，16 层就是平台

L501（170×169）900s 两档全 miss 后 TREELOG 揭示病根：**深传播的每次调用 O(N)
扫全盘（29K 格），大盘上节点速率崩到 ~2.3K/s**——树便宜了 50 倍（216K 节点）
但每节点慢 200 倍，每分片 300s 只磕完 ~2.6 棵树。深度全开在 141×140 是 22 倍
的神药，在 170×169 是毒药。

PROPDEPTH 中间档（L414 测试台，此前只有 2 和 ∞ 两个极端）：

| PROPDEPTH | 2 | 4 | 8 | **16** | 64 | 256 | ∞ |
|---|---|---|---|---|---|---|---|
| L414 | 656s | 80s | 76s | **28s** | 31s | 31s | 34s |

**16 个分支决策就拿到全部剪枝价值**，再深只付成本不涨肉。这也解释了为什么
深传播能赢：值钱的是「路径前缀早期的区域窄缩证伪」，不是全路径的持续传播。
L501 用 PD=16/32 探底中——大盘的每条路径有几万个分支点，限深把 O(N) 调用次数
从几万砍到 16，节点速率应该能回到正常量级。

### L501 六核限深诊断 + v53-sweep16 组装

PD=16 下 L501 的逐树数据（JOBS=6、1200s）：完成 32 棵，**中位 ~12-25 万节点（便宜）
但尾部怪物 3.5 亿节点**——重尾分布，无限搜的怪物垄断队列问题在高一个层级重演。
节点速率恢复到 ~150K/s/核（全深是 2.3K/s，浅层 600K/s）。

v53-sweep16 = v52 + 两刀：
1. 升档目标 999999 → **16**（PDPROMO 可调；gfix 强类保持全深不动）
2. 升档分片的第三层 → **多轮限额扫荡**（SWEEP=2M ×8 递增，末轮无限；r==0 永久剔除）
   ——正是 v49 当年设计的场景，现在数据（中位便宜+重尾+可解树便宜）终于教科书匹配。

打游戏模式：全部实验限 JOBS=6，数字只看相对。

### v54-incprop：增量式分支点传播——**每次调用 ~76 倍提速**（verify 微基准 2062ms→27ms）

三个支柱：
1. **estate2 底座化**：不再从零全盘重建，memcpy 自起点缓存的 estate。关键洞察：
   终点色 col[p]^(rem&1) 沿路径**不变**（每步 len 格的颜色翻转与 remaining 奇偶抵消），
   所以起点时刻的 estate 沿整条路径永远有效，还比从零重推更强（含 flow/链定向结论）。
2. **末滑行播种**：工作队列只播「刚滑过的线的自由邻居 + 当前头」，级联只扩散到
   被扰动区域。老滑行的影响由祖先调用覆盖；漏的跨调用组合结论单向弱化（不误杀）。
3. **DSU 基座 dsu0**：estate 必用边的并查集每起点建一次，每调用 memcpy 复制；
   estate 重载处 dsu0_valid=0 惰性重建。

可靠性(soundness)：--verify 真解 7091 步零报警。
代价：证伪率 33%→23%（丢了跨滑行组合结论），但同 wall 时间内调用量 ×2.6。
六核 A/B：L389 43→21s、L381 28→17s、L319/39 持平、L414 131→231s（estate 底座
改变证伪集合→排序洗牌，抽签重抽，非机制问题）。

下一步：混合播种（每 K 个分支决策做一次全量播种恢复跨滑行结论，其余走便宜路径）；
传播成本崩塌后 PDPROMO=16 帽、SURVDEEP 门槛的经济学全变，都要重扫。

### L501 三臂满核实验：全灭——增量传播不是 L501 的钥匙（用户判断正确）

| 臂 | 结果 |
|---|---|
| FULLEVERY=4 PDPROMO=16 | 900s 未解出 |
| FULLEVERY=4 PDPROMO=999999（全深度混播） | 900s 未解出 |
| FULLEVERY=0 PDPROMO=999999（全深度纯增量） | 900s 未解出 |

结论：传播成本崩塌（76 倍）没有换来 L501 的树塌缩——它的病在「树规模 × 树数量 ×
可解密度 ~1/600」，传播便宜只是让磨树便宜一点，改变不了要磨多少。v54 定位为
**常数优化工具**（L389 减半、L381 -39%，六核口径），不是范畴突破。

FULLEVERY 混播扫描（六核，噪声大）：L414 0:233s/4:158s/8:211s，L389 0:21s/4:27s/8:25s
——混播在 L414 有肉在 L389 略亏，都在抽签噪声内，不定论。

L501 类下一步只剩范畴武器（老清单重新排序）：
1. 瓶颈分区的「零穿越区域必含端点」——唯一有实证能打起点彩票的招（现有分区在
   L501 上整块淘汰 0 格，但端点约束这半边从没实现）
2. 有向进入方向 probing（Tron 路数）——增量传播已就位，成本前提刚好满足

### L501 几何判决：0 个关节点——瓶颈分区对零信息类正式出局

度直方图：deg2 5533（29%）/ deg3 11060（58%）/ deg4 2639（14%），19232 个自由格
**2-连通（关节点为零）**。「单穿越区域必含端点」没有靶子。零信息类至此是
「在所有松弛下都真自由」的完备样本：无固定边、单一 SCC、无链约束、无几何瓶颈。

### v55-classseed（播种按类分家）+ 三版满核对决：没有版本占优，抽签不稳定性成为主导

v55 = v54 + 升档零类分片恒用全量播种（保 v52 证伪语义），其余末滑行播种（保 76 倍）。

| 关 | v52 | v54 | v55 |
|---|---|---|---|
| L414 | 33s | 79s | **27s** |
| L458 | **104s** | 250s miss | 221s |
| L475 | 207s | 300s miss | **128s** |
| L496 | 62s | 72s | 67s |
| L379 | 19s | **2s** | 3s |
| L383 | 24s | **1s** | **66s ✗**（炸 30s 口径） |
| L447 | 24s | **2s** | 5s |
| L467 | 22s | **4s** | — |
| L319 | **3s** | 8s | 11s |

核心教训：**任何传播语义的改动都重洗全部关卡的抽签**——增量传播（v54）在浅层/强类
拿 2~24 倍是结构性的赢，但升档零类的输赢完全是签运。官方数字仍由 v52 把持
（30s 奇数 473 / 600s 全量 500）：v55 的 L383 破 30s 线、v54 的 L458 破 600s 线。
v54/v55 入库为工具版本。v55 打 L501 最后一发在跑。

## 2026-08-23 收盘总结

**官方数字（v52-reprobe，全部验证）**：30s 奇数口径 **473**（墙 L475，~200s 量级）；
600s 全量口径 **500**（墙 L501）。两天累计：317/318 → 473/500，
官方模型纪录 264、公开代码纪录 332 均已大幅超越。

**L501（170×169，零信息类放大版）攻坚全记录——5 配置 × 900s 全灭**：
v52 默认、v52+PROBE=1000、v54 三臂（PD16 混播/全深混播/全深纯增量）、v55。
它的画像：19232 候选、0 固定边、0 关节点（2-连通）、树成本重尾
（PD16 下 94% 超 2M 节点）。可解密度与树规模的乘积在这个尺寸越过了 600s 预算。

**今天入库的工具**（都不改官方默认）：
- v54-incprop：增量式分支点传播（estate 底座 + 末滑行播种），每调用 76 倍，
  浅层/强类结构性大胜（L379 19→2s、L383 24→1s、L447 24→2s、L467 22→4s）
- v55-classseed：升档零类全量播种（L414 27s、L475 128s 两个类内最优）
- PDEATHSIG 孤儿修复（基础设施，三版都带）

**判据链的完整版本**（几天的认知浓缩）：
gfix≥10% → 全深传播（强类）；浅探存活率≥38% → 升档+深层重探（零类大树）；
DYNMIN<15% → 降档兜底；传播成本 O(N)/调用在 >160×160 盘面是硬约束，
增量化把它打到 O(扰动区域) 但证伪率 -1/3，两者的净效应因关而异（抽签洗牌）。

**下一步候选**（按预期性价比）：
1. 有向进入方向 probing（Tron 路数第 2 条）——增量传播已就位，前置刚好满足
2. 抽签组合：v52/v54/v55 三版分片混编 portfolio（同一关内三种语义并行赛跑，
   把「洗牌方差」从风险变成资产）——工程量小，值得先试
3. 度 2 链收缩（Hippo 关键招，工程量大、bug 密度高）

### v56-portfolio：三语义分片混编——十关抽查 8/10 拿最优签，两项历史最佳

shard%3 → 三种传播语义（0=v52 空底全播｜1=v54 底座滑播｜2=v55 底座全播），
同一关内三种签同时抽。可靠性(soundness) --verify 零报警。十关抽查：

| 关 | v52 | 三版单独最优 | v56 |
|---|---|---|---|
| L414 | 33s | 27s | **25s（新最优）** |
| L475 | 207s | 128s | **126s（新最优）** |
| L383/L447/L379 | 24/24/19s | 1/2/2s | **1/3/3s** |
| L319 | 3s | — | **2s** |
| L458 | **104s** | 104s | 241s（掷硬币输，线内） |
| L467 | 22s | 4s | 22s（同上） |

portfolio 不是逐关取 max（候选归属分片固定，等于每个候选掷一次三面骰），
但期望上「每关至少 1/3 的候选拿到自己类的好语义」，抽查显示这就够吃掉大部分方差。
30s 奇数口径完整评测认证中（零破口径回归的前提下 v56 可转正）。

### v56 双口径认证通过，转正为官方默认：**473 / 500**，逐关时间大幅收紧

- 30s 奇数口径：473（墙 L475/126s），之前零失败
- 600s 全量口径：500（墙 L501），87 关零失败，最慢 L458 241.7s、次慢 L475 126.3s
- 对比 v52 的走廊时间：L383 24→1s、L447 24→3s、L379 19→3s、L414 33→25s、
  L475 207→126s——同分，但整体余量厚了一大截（30s 口径最慢过关从 ~25s 压到个位数
  为主），对抗机器方差的鲁棒性显著提升

v56-portfolio 自此为官方默认求解器。分数没动（473/500 的墙是 L475@126s 和
L501 两座真墙），但「同分更快更稳」正是下次底层改动的最好底座。

### v57-direntry：有向进入方向 probing——机器有效，目标类 0.0%（负结果）

Tron 路数第 2 条落地：H(c,d)=「从 d 侧进入 c」两案分解（穿过：c 两边同轴+垂直边禁；
停下：{c,c+dd} 必不用），两案传播皆矛盾 ⇒ 禁用 (c,d)。消费端进 estate_ok
（滑行途径格全查）。soundness：L414 真解 7091 步零报警。

| 关 | 禁用率 |
|---|---|
| L413（传播强） | 9.3%（3382/36260） |
| L383（传播强） | 10.2% |
| **L414（零信息）** | **0.0%（0/38402）** |

机器实现正确（强类非零），但零信息类下两案在度约束松弛里双双可行——
**假设粒度再细也补不了松弛太松**，和单边 probing、全局 probing 的 0 一脉相承。
v57 入库不转正（强类本来就快，10% 禁用换不回预处理成本）。

### L501 的覆盖算术（限额扫荡的新解读）

600s 内可覆盖树数 ≈ 28 分片 × (500s × 150K节点/s ÷ SWEEP预算)：
SWEEP=2M → ~1260 棵；500K → ~5000 棵（几乎全部幸存者）；8M → ~300 棵。
v56 默认（2M）覆盖 1260 棵仍 miss ⇒ 要么可解密度 ≪ 1/600，要么可解树 >2M 节点。
三档扫描（250K/1M/8M × 900s）在跑——直接把「密度 × 可解树成本」的联合分布探出来。

### L501 SWEEP 三档全灭：软柿子定律在此尺寸死亡——死亡证明书齐了

| SWEEP | 900s 内覆盖 | 结果 |
|---|---|---|
| 250K | 每分片全部 ~220 棵（全覆盖）+ 半轮 2M | MISS |
| 1M | 全覆盖 + 部分 8M 轮 | MISS |
| 8M | ~300 棵 | MISS |

250K 档全覆盖仍 miss 是决定性的：**整个候选集不存在 ≤25 万节点的可解树**。
L414 的赢家 2 万节点、L501 的可解树 >数百万节点——软柿子定律随盘面尺寸死亡，
这就是指数墙的素颜。全天 L501 攻坚合计 9 种配置 × 900s 全灭。

L501 需要的是 10~100 倍量级的树塌缩，而不是 2 倍的搜索加速。据此对方案 2
（度 2 链收缩）的预期收益重新估值：滑动模型下搜索单元本来就是「滑行」不是「步」，
链收缩的理论收益 1.3~2 倍——**够不着这堵墙**，且原作者警告 bug 密度极高。

## GPU（RTX 5090 D v2）探索：树级并行限额扫荡原型（cuda/sweep.cu）

用户提供 5090 D v2（24GB, sm_120）。WSL 的 nvcc 12.4 最高 sm_90，走 compute_90 PTX
让驱动 JIT 前向兼容——实测可行。原型设计：一线程一树、滑动 DFS 显式栈、统一节点
预算（天然负载均衡）、Warnsdorff 落点度排序、奇偶/死路/搁浅格剪枝（全 sound，
小关能解出验证）。一个可靠性(soundness)坑：搁浅格检查必须豁免「紧邻落点的格子」（可被下一步
直接滑入）——第一版漏了，L39 全树被误证伪。

**实测（初版，全局显存）**：
- 裸吞吐 90~200M 节点/s ≈ CPU 全机（~17M/s）的 5~12 倍
- 但「笨节点」的树大 ~1000 倍：L101（36×36，CPU 秒解）20M 节点/树都磕不动
- 结论：**连通性剪枝（reach_local）是树有限性的来源**，GPU 版必须带上它才有意义；
  逐线程全局显存 BFS 会吃掉大部分裸优势，正确设计是 block-per-tree + 共享显存
  位棋盘（vis/seen 各 3.6KB 位集，DFS 和 BFS 全在片上）+ 协作洪水——还要几天工程量

### L475 SWEEP 四档：**72s @SWEEP=3M**（126→72s，-43%）

| SWEEP | 500K | 1M | 3M | 8M |
|---|---|---|---|---|
| L475 | 102s | 112s | **72s** | 80s |

诊断依据：赢家树 2.17M 节点，3M 预算让它在第 0 轮就被收割。距 30s 还差 2.4 倍。

## 研究地图：这个问题在学术上是什么、跳变想法可能藏在哪（2026-08-23 检索）

**学科定位**：CP（约束编程）× TSP 多面体组合优化 × 网格图组合学的交叉。
没有论文正面处理滑动规则；我们的传播栈 = 一台手写 CP 求解器
（Régin 1994 流+SCC 过滤、failed literal probing、circuit constraint 思想）。

**矿脉 1：TSP 分支切割的高阶割平面**（最正统的跳变候选）
- 我们的流松弛 = 分数 2-匹配；零信息类 = 「2-匹配 + 子回路消除都切不掉」的实例
- 教科书下一层：**comb 不等式 / blossom 割**（齿全为 2 点的 comb 即 blossom），
  对 cycle 多面体 facet-defining。分离算法见 Letchford 平面图 comb 分离
  （lancaster.ac.uk/staff/letchfoa/articles/2000-planar-comb.pdf——平面图上多项式！
  网格图是平面图，这条正好用得上）、Kobeaga 2021 收缩+精确子回路分离
  （arxiv.org/pdf/2004.14574）
- CP 侧对应物：Proof Logging for the Circuit Constraint（Springer 2024）

**矿脉 2：FHCP / 实战哈密顿环求解器**
- FHCP Challenge Set：1001 个「难在结构不在规模」的 HCP 实例（和我们的零信息类
  哲学同源！）——sites.flinders.edu.au/flinders-hamiltonian-cycle-project
- Snakes-and-Ladders Heuristic（SLH，arxiv 1902.10337）：Lin-Kernighan 思想的 HCP 版，
  ≤5000 点实例全胜。**移植警告**：滑动规则破坏任意改接路径类技巧（Pósa 旋转同理），
  但「圈上排序 + 蛇梯分类」的框架可能有变体空间
- 网格图理论：Itai–Papadimitriou–Szwarcfiter（SIAM 1982，矩形网格充要条件+一般网格
  NP 完全）；C-shaped/supergrid 各类特殊形状有线性算法（arxiv 1602.07407 等）——
  对「分治到特殊形状子区域」的思路可能有用

**矿脉 3：AI coding 做算法研究的实践（用户要求调研）**
- AlphaEvolve（DeepMind 2025，arxiv 2506.13131）：LLM 进化整个代码库 + 自动评测器
  反馈闭环；FunSearch 的全面升级。开源复刻：OpenEvolve / CodeEvolve（arxiv 2510.14150）
- Effective Harness Engineering for Algorithm Discovery（arxiv 2605.15221）：
  **每次生成想得更深 > 生成更多个体**；评测函数必须防 hack（模型越强越会钻空子）
- 观察：我们这两天的方法论（每改动带预测、负结果入档、soundness 用真解校验、
  测量前清机器）和这批文献的「evidence-driven + falsifiable edits」高度一致——
  值得补的是他们的「每次编辑显式记录预测影响并在下次评测时对账」

### 🏆 v58 完整评测（30s 奇数口径）：**473 → 499**，两个口径在 L501 会师

PROBE 按类分家（零类 650/SWEEP 3M）让 L475 从 207s 崩到 25.2s，而它身后的
477~499 全是坦途（0.17~7.3s）——L475 是那一带唯一的硬骨头。250 关零失败。

| 口径 | 分数 | 墙 |
|---|---|---|
| 30s 奇数 | **499** | L501 |
| 600s 全量 | **500** | L501 |

两个口径的墙**会师于 L501**——这个项目现在只剩一堵墙。两天累计：
30s 317→499（+182）、600s 318→500（+182）。惊人的对称。

复盘 L475 的坍塌路径（写给未来的自己）：诊断（TREELOG 找到赢家树 2.17M 节点
卡在 2M 预算外）→ SWEEP=3M 让它第 0 轮被收割（126→72s）→ PROBE 细扫发现
650 山谷（26s，两壁 32s+）→ 类分家保住 L473。每一步都是数据牵引，没有一步是猜。

### ⚖️ 割平面矿脉的死刑判决：ground truth 禁边率 0.0%（research/milp_probe.py）

三级实验递进，一级比一级强，全部零收获：
1. 度 LP + 1005 条子回路割（lp_probe.py）：L101 抽样 60 边，固定率 0.0%
2. **整数**可行性探针（milp_probe.py）：度∈[1,2]+边数 n−1+连通的整数解**恰好是
   哈密顿路径**，所以这是 ground truth——「边 e 是否在任何哈密顿路径中」。
   L101 抽样 12 边全部可行（每条边都有真实哈密顿路径经过）。
3. 推论：图层面每条边都真自由 ⇒ **任何合法割平面在数学上不可能固定边**。
   comb/blossom 不用实现了——它们只能割掉分数点，而这里连整数点都够多。

**这就是零信息类的完整死因与唯一出路**：这类盘面的底层图在哈密顿意义上
接近「万能」（怎么走都有路），全部约束力都藏在被所有松弛忽略的**滑动规则**里
（停step只能停在墙/已访问格前）。滑动规则的静态松弛是文献空白——
没有任何论文处理过。下一个跳变想法必须在这里挖：
- 「转弯格的前方必须是墙或更早访问的格」的时序信息如何静态化？
- 首步撞墙约束（已用）是这个家族里最平凡的一员——家族里还有什么？

## 🧬 生成器逆向（用户直觉驱动）：「路径先行」家族实锤

用户直觉：关卡是生成的 ⇒ 生成器必然握有可解性证明 ⇒ 找到生成算法就可能拿到起点先验。

**实锤证据——死端不变量**：任何「先造滑动路径、未访问格变墙」的生成器都满足
「自由图中度数 ≤1 的格子 ≤2 个」（路径中间格度 ≥2，只有两端点可能度 1）。
全量 1207 关验证：**1088 关 0 死端、116 关 1 死端、3 关 2 死端、违反者 0**。
纯随机撒墙+可解筛选会有 ~8% 死端格——彻底排除。结论：**生成器 = 路径先行家族**，
它手里有每一关的解。

已证伪的子假设：纯「空盘滑走+残格变墙」（预言外圈无墙、首滑通边界——实测外圈
~30% 是墙）。所以是「预置墙/自愿停+残格变墙」的变体，首滑指纹不可用
（射线格检验：L475 赢家命中、L414 赢家未中——无普适性，近边界格的短射线主导了 8% 基率）。

**116 个单死端关卡是白送的**：死端必为路径端点 ⇒ 这些关的一个端点已知！
（待接进求解器：tcand 直接锁死。）下一步：批量收割零类赢家起点做统计，
找生成器政策的空间偏置（离边距离/首滑方向/局部墙型 vs 均匀基线）。

### 🎯 小盘逐起点普查（L2~L40 全量 ground truth）：可解起点稀少且空间聚集

用 STARTXY 对 39 关的每个自由格做可解性判定（research/unistart_2_40.log）：

- **12/39 关的可解起点全盘唯一**（L13/16/18/22/23/24/27/29/30/33/34/38）
- 大多数关 1~3 个，密度 ~1%；少数开阔关例外（L25 有 42 个）
- **空间聚集**：L31 的 6 个挤在 2×4 框内、L26/L40 抱团角落、L39 同列隔 2 格；
  大盘印证——L414 的两个已知赢家 (112,115)(112,116) 相邻
- 12 个唯一起点 = 12 个纯净样本（极可能是生成器起点或结构强制起点）

推论：起点彩票的真实结构是「全盘找一个小区域」而不是「全盘找一个格子」。
下一步：用这批标注做可解释特征挖掘（颜色/度数/距墙/局部墙型），若能把可解
起点压进前 5% 排序 = 20 倍起点先验，直接喂 sc。

### 起点特征挖掘：零信号（research/start_features.py）——可解区域是全局性质

209 个 ground truth 可解起点 vs 全体自由格：度数/贴墙/8邻域墙数/距墙距离/首滑长度
全部几乎同分布（唯一弱信号：可解起点的最长首滑略短，3.70 vs 4.57）。
简单评分排序命中率不如随机。**起点先验的局部特征路线正式关账**：
可解起点稀少、聚集，但聚在哪由全局结构决定，局部模式不可辨识。

### 📚 Ice sliding games（arXiv 1507.00559）精读：挡板记账的现成数学找到了

虽然它研究的是可达性（不是哈密顿覆盖），但核心机器可移植：
- **「未支配列」引理**：列内无障碍 ⇒ 机器人无法在该列垂直停步 ⇒ 强制水平穿越
  ⇒ 反向约束所有跨越行——**维度耦合**（列的缺陷强制行的约束）
- 下界用总支配数 γ_t 刻画，上界用可级联的基础模式构造
- 移植到 Coil：无墙列的所有垂直停步必须轨迹垫背，而**时间上第一个**垂直停步
  的垫背必须来自更早的水平穿越（或起点本身在该列）——时序约束变列级记账。
  这是「第三支：挡板供需记账」的正经数学起点，也是零信息类唯一活着的理论方向。

## 🔬 L101 显微镜：唯一起点、收尾口袋理论的诞生

**全盘 ground truth**（894 格逐一 STARTXY 判定，research/l101census.log）：
可解起点**恰好 1 个**——(14,15)。探针幸存 117 个（87% 击杀），假幸存 116 个。

**靶场地图**（research/l101_delta.py）：116 个假幸存者不散布——组成一个**连通团块
把真起点包在里面**（上中部 x∈[10,31] y∈[1,17]），仅几个飞地。

**唯一解解剖**（research/l101_anatomy.py，解存 research/l101sol.txt）：
- 466 步滑行，**91% 靠墙停步**——解是「墙到墙的弹珠」，轨迹垫背仅 9%
- **终点 (11,13) 距起点 (14,15) 仅 4 格**——近似「哈密顿环断一边」的结构
- 访问顺序热力图：路径先冲出中心区、按大区块扫完全盘、最后**回到起点旁的
  「收尾口袋」终结**——而这个口袋恰与假幸存者团块高度重合！

**收尾口袋理论（今日诞生的定理候选）**：
假幸存者骗过探针的机制 = 探针只看开局，而口袋里的格子开局都正常；
真约束是全局的——**起点必须让身后留下一个可最后收尾的口袋**（起点与终点
必须共居同一小区域，环状结构的断点）。这一并解释了两天来所有赢家聚集现象
（L414 两赢家相邻、小盘普查的聚集框）。
验证中：假幸存者搜死时的 best_rem 分布——若都是「差一两百格死」（≈口袋尺寸）
而非「差几格死」，理论坐实，然后把「口袋可收尾性」做成静态检查。

## 🧵 缝合线理论（seam theory）——今天的理论顶点

**判决性证据（解重放，research/ 下可复现）**：
| 关 | 起点 | 终点 | L1 距离 | 盘面自由格 |
|---|---|---|---|---|
| L101 | (14,15) | (11,13) | **5** | 894 |
| L414 | (112,115) | (112,116) | **1** | 13440 |

随机预期距离是几十~百格；联合 p < 1e-5。且 L414 的另一个实测可解起点恰是
(112,116)——终点本身也是可解起点。

**理论**：零信息类盘面存在隐藏的「近似闭环」全覆盖结构。停步依靠关系
（每次停步依靠墙或更早的轨迹）构成有向依赖；把环在任意点断开，多数断点
使依赖成环（时间上矛盾），**只有缝合线处断开才无环可线性化**。推论：
1. 可解起点稀少、聚集成小簇（缝合线是局部的）——两天来全部观察的统一解释
2. 起终点相邻（断环的两端）
3. 求解 = 找缝合线；生成器大概率也是环基构造（v1/v2 随机走法填充率只有
   16~53%，撑不起 70% 的真实填充率——闭环构造天然全覆盖）

**验尸修正（诚实记录）**：L101 的「假幸存者团块」是第一层探针预算的假象
（25/25 抽样死于第二层 32K 探针；小盘上探针≈全量搜索）。大盘的假幸存者是
同一现象在「探针预算 ≪ 树规模」尺度下的表现——研究靶心修正为：
(a) 单起点证伪成本亚线性化，或 (b) 缝合线的正向静态签名。

**下一步排序**：更多终点数据点（每个零类关重放解找终点距离）→ 缝合线
静态检测的理论 → 环基生成器 v3（近环走法/全盘闭环+雕墙）。

### v59-seam（缝合线剪枝）判决：不转正

抽查：L414/458/389/475 与 v58 持平，L381 略慢，L496 67→200s（洗牌受损），
L501 901s 未中（第 11 次）。终点候选半径掩码的剪枝强度不足以改变 L501 的
覆盖算术（缝合线位置没变，只是错树死得稍快）。v58 保持官方默认。
结论与用户判断一致：**主战场是排序不是剪枝**——真缝排名测量启动。

### 真缝排名测量（17 关，research/seam_ranks.log）：sc 排序已把缝排到中位 #4

真缝在所属分片队列（~200+ 项）中的排名分布：**#0 ×6、#1 ×2、#4 ×2、#7、#8、
#14、#16 ×2、#20**（L414/L141 两关的日志行缺失，另计）。随机期望 ~#110——
探针深度信号（sc=best_rem）对缝合线有强相关，这就是这批关秒级~分钟级的原因。

**L501 的病症定位**：SWEEP=3M 下 600s 覆盖每分片前 ~45 名；11 连灭 ⇒ 它的缝
排在 #45 开外——**排名分布的尾部倒霉蛋**（样本最差 #20，它抽到 #60~200）。
四小时全核猎缝已发射（覆盖算术保证 round-0 全扫 <1h、round-1 到 2~3h，必然找到），
目的：拿到 L501 缝的位置/排名/sc 值——尾部病例的完整标本。

**方法论修正（用户指正）**：不应假设我们手里有 Tron 没有的工具——他大概率
自建过生成器和全套实验体系。我们仅有的优势是 100 倍算力和迭代速度；
他的只言片语（0.3% 不确定、recursive patterns）应当藏宝图用。

## 滑动感知进入引擎（research/entry_engine*.py）——v2 首战报告

v1（纯结构）：ban 0%——印证杀伤力全在时序层。
v2（+环模式无终点豁免 +软垫背时序递归 depth2）：
- **L101 ban 40.7%、L414 ban 40.0%，真解零报警（sound）**——零信息类上首次
  出现静态确定力（此前流/SCC/LP/MILP/v57 全部 0.0%）
- 但冷静判读：形状唯一化率 0.0%（Tron 口径 99.7%）；depth=3 与 depth=2 同额；
  形状数分布规整（2/6/12）——当前禁用疑似「沿途无可行停点」型浅层几何废案，
  时序递归的逃生口（硬垫背/穿过/深度耗尽放行）开得太宽没咬到肉
- 环矛盾格 0 个——缝探测暂未显形

待办：解剖 40% 禁用 vs C 版 estate 已有能力；规则族升级方向=冰面论文列记账。
GPU 侧：warp 位棋盘内核由子代理并行开发中（目标 CPU 品质节点 ≥1.7 亿/s）。

### ⚠️ 撤回上一条：v2 的 40% 是会计幻觉，真实静态确定力仍为 0.0%

解剖 12 个抽样禁用：全部是「来路是墙」的几何废案（prev 非自由格）。
总账：3576 个 (格,向) 对中恰有 1034 个来路为墙 = ban 数恰好 1034——
**v2 实际推出的非平凡禁用为零**。「零类首次静态确定力」的说法撤回。

根因：环模式+软垫背「交给迭代」的设计让所有局部图景都有逃生口——
时序债从不到期。连同 v1/v57 的零收获，形成一致图景：**零信息类的局部
静态推理可能本质无力**（局部图景真实地丰富），Tron 的 0.3% 大概率是
传播强类上的成绩。活着的路线收敛为：缝排序的搜索侧信号（sc 已到中位 #4，
问题在尾部）、GPU 吞吐（子代理开发中）、以及「缝约束探针当排序信号」
（对每个候选用终点邻域约束做小预算探针，缝上的应探得更深——待试）。

## 🧵→⚔️ 缝约束扫荡（seam sweep）——缝合线理论的第一件真武器

机制：零类候选逐棵做「小预算(10万节点) + 终点必须落在起点 L1≤R 邻域」的受限搜索。
缝假设下**86.3% 的假候选在几千节点内被搜穷证死**（可靠地证明「它不是缝」——
比无约束搜穷便宜几个量级），撞帽的 13.7% 才是缝候选。

L414 实测（R=12，TIER2=101 跳二层）：
- 1567 棵：r=0 证死 1352｜撞帽 214｜**解出 1（真缝 113,116——13371 节点就解了整关）**
- 同时揭穿 sc 的大失灵：L414 真缝在 sc 排序里排 88.7%/98.1% 的**倒数位置**
  ——「sc 对抗性缝」= 硬关的真面目（305~475 那批缝排 #0~20 所以秒解）
- 端到端：L414 26s 持平，L475 96s（已破关不占优——本武器的价值在 sc 失灵处）

L501 第 12 次（R12/900s）miss：扫荡速率 ~5 棵/s，6200 幸存者 900s 只覆盖 1/3
——又是覆盖算术。R=25 完整扫荡（3600s，猎缝+诊断二合一）在跑。
待办：v60 生产化（零类分片对半：缝扫荡管线 + v58 经典兜底，防远缝翻车）。

## 🚀 GPU v2 内核交付（cuda/sweep2.cu，子代理开发）——209.5M CPU 品质节点/s

三级验收全过，正确性做到**与 CPU 参考实现逐位一致**（oracle16.c 对拍：四组测试
每起点的 (result, nodes) 完全相等），解经 --verify 零报警：
- L101 全盘 5M 预算：exhausted 99.9%（平均 3.3K 节点/树）——树尺寸=CPU 尺寸
- L501 前 2000 起点 2M 预算：13.7s，聚合 **209.5M 节点/s**（CPU 全机的 ~12 倍）
- L501 全盘 19232 起点 100K 预算：**4.3s 证伪 8389 个**

设计：warp-per-tree + 共享显存位棋盘（9.1KB/树）+ run-collapse reach_local
+ B 探针两段洪水（失败洪水均价 7725→1674 格）+ 全局工作队列。
迭代 65.7 → 102.3 → 209.5M/s 三轮。瓶颈：reach 占 66%（失败洪水占 BFS 半数，
信息论下界附近）；occupancy 被 shared 钉在 10 warp/SM。
后续升级点：双向会师洪水、cheap_ok 摊薄、候选缓存、多关流水。

**L501 算术被改写**：GPU 全盘 2M 扫 ≈130s、8M ≈520s——升级扫荡（2M/8M/32M）
与 CPU 缝扫荡（3600s R25）双马竞速中。

### L501 双马竞速验尸：双重墙确诊

GPU 升级扫荡（全 19232 起点无死角）：2M/76s、8M/304s、32M/1195s 三轮 solved=0；
预算 ×16 只多证死 270 个起点（8852→9122），**10110 个起点在 32M 节点下不倒**。
结论：L501 连赢家树都 ≥32M 节点（L414/L475 的赢家 2~4M）——**缝排名差 × 树规模大
双重叠加**，纯排序改良救不了它，需要 GPU 吞吐 × 深传播剪枝的合体
（深传播把树砍 10~100 倍——GPU 内核下一阶段）。
GPU 第四轮 128M（~70min）+ CPU 缝扫荡收尾报告挂通宵模式。

## ⛏️ WLOG 挖矿实验台首采（research/wlog_mine.py，小关全解穷举）

**四条 9/9 全成立的 WLOG 引理候选**（「若有解，必有一个解满足 P」）：
- P3 首滑最短（根部分支砍半+）｜P7 尾滑一步（收尾强约束）
- P5 前三停靠墙｜P9 近缝 ≤8（缝理论下探到小盘全成立）

**天花板地图（每格形态在全部解中一致的比例）**：多解易关 14~18%，
唯一解关 100%。=> **金假设：零信息类难关 = 近唯一解类**（可解起点稀少+
唯一起点关+L101 全盘唯一起点全部吻合）。若真，多项式传播在这类盘上
「原则上」能确定一切——Tron 的 99.7% 只有在这类盘上才可能成立，
而这恰是要打的盘。**多项式路线的可行性从玄学变成可检验命题**。

用户的增长率判定（本节的战略基石）：Tron 能跑 2000×2000 证明其方法在此实例族
上有效多项式；一切指数域的改良（排序/扫荡/GPU 吞吐）都不是最终答案。
L501 双马（GPU 128M 与 CPU 缝扫荡）已按用户指示撤下。
验证中：唯一起点小关的「从唯一起点出发解数」（近唯一解假设的直接检验）。

### 🏛️ 金假设坐实：唯一起点关 = 全局唯一解谜题（11/12 恰好 1 个解，L38 有 2 个）

链条闭合：零信息类难关 =（近）唯一解谜题（数独式设计唯一性）⟹ 每格形态被
完全逼定 ⟹ 完备多项式传播原则上可确定一切；Tron 的 2000×2000 是存在性证明。

**多项式路线研究纲领（下一阶段的主线）**：以 11 个全答案已知的唯一解小谜题
（41~140 格）为测试台，实现推理规则 → 量确定率 vs 100% 天花板 → 迭代。
现有规则的起点：我们的 estate 传播在这些关上的确定率待测（它们多为传播强类
（有死端），可能已有 50~80%；真正的考题是把同样的攀登搬到零类唯一解盘上——
L101 是第一座 894 格的靶山）。

### 📐 多项式路线基线测量：现有传播在唯一解谜题上只确定 9~44% 的边（天花板 100%）

| 关 | L13 | L16 | L18 | L22 | L23 | L24 | L27 | L29 | L30 | L33 | L34 | L38 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 判定率 | 41% | **9.9%** | 11% | 12% | 14% | 15% | 9.2% | 30% | 44% | 24% | 33% | 24% |

这些盘的真实确定度是 100%（解唯一 ⇒ 每条边被逼定）。**九成的确定力还没被
发明**。L16 是理想训练靶（81 边、基线 9.9%、每条未定边答案已知）。
下一阶段主线：对着靶场造规则爬山——每条新规则的产量直接可测。

（当日两口径官方数字：30s 奇数 499、600s 全量 500；L501 双重墙确诊后按用户
战略判断转入多项式路线。）

### 🪜 多项式梯子第一段：L16 9.9% → 22.2%（强传播）→ 37.0%（+3轮边探针）

关键发现：基线测量时 verify 默认走弱版 propagate（STRONG=0）且 PROBEROUNDS
挂在 propagate_strong 里根本没跑——**已有机器就没开满**。开满后（STRONG=1
PROBEROUNDS=3）：L16 37.0%、L27 12.6%（顽固）。梯子真实存在，级距还太短。
已加 PERSTART 判定率普查到 verify 通路（多项式路线的靶场刻度线）。
下一级候选：更多轮数（测中）、二级探针（probe 内嵌 probe——老清单 #10，
靶场上可负担）、探针内用强传播、有向进入假设探针。
文献侧：Slitherlink 社区的推理体系 = 局部度规则 + 边假设传播，与本路线同构
（jonathanolson.net/slitherlink）。

## 🎆 多项式路线打通：累积探针在 L16 上到达 100% 完全确定

**research/ladder.py（一级，知识累积版失败文字探针）：L16 全部 81 边 100% 确定，
239 次传播调用（≈3×边数——线性量级！），全程真解对拍零冲突。**

与 C 内置探针 37% 平台的唯一区别：**知识累积**——每确定一条边立即作为公理
注入后续所有假设（C 版每轮从头、不积累）。这就是 Tron 的「recursive patterns
+ bounded brute force」的真身：迭代 failed-literal + 累积，在唯一解实例上
完全确定，复杂度多项式。

规模化路径已清晰：C 原生实现累积探针（把 FORCEEDGES 循环内置），
配 v54 增量传播降每次调用成本——L501 量级 38K 边 × 若干轮 ≈ 分钟级可期。
全靶场验证跑量中。

### 靶场全验证：4/7 关 100%，L38 98.5% = 其真天花板（双解关，未定边=两解差异处！）

| 关 | L13 | L16 | L30 | L33 | L38(双解) | L27 | L22 |
|---|---|---|---|---|---|---|---|
| 一级累积探针 | 100% | 100% | 100% | 100% | **98.5%=天花板** | 37.9% | 13.2% |

L38 的 4 条未定边恰是两个解不一致的边——**方法已「相对解集完备」**。
L22/L27 抵抗一级 ⇒ 需要二级（假设内嵌探针）——与数独规则阶梯
（唯一数→数对→链）同构：实例难度 = 所需探针深度。L22 二级在跑。
可靠性：七关全程真解对拍零冲突。

### 数独移植第一批：either-way 交集推理——L22 上零增益（负结果）

ladder2.py（交集版）：两分支都活时取确定集交集并入。L22 实测 13.2% 不动——
两支的后果集无公共新项。L22 的抵抗比 either-way 深，真·二级（嵌套探针）在跑。
数独移植清单状态：试数→探针 ✓（已是主力）｜难度分级→探针深度 ✓（已采）｜
either-way→交集 ✗（L22 无效，留作别的关的备件）｜唯一性技巧：可用但不纯粹
（L38 双解会翻车），封存。

### L22 二级判决：13.2%→18.6%（+7 边 / 9.1 万调用）——探针深度不是万能钥匙

嵌套加深在开阔盘收益锐减：**探针力量上限 = 内层传播力量**。开阔盘上内层
传播（时序盲）看不见矛盾，加深只是徒劳枚举。缺的拼图指回滑动时序规则，
但现在有收益计量器（L22 梯子产量）。
实施路径：v42 跨链时序偏序+环检测——当年败于必用边稀疏（一条跨链边都攒不出），
而**探针假设恰好制造稠密必用边**：把 v42 机器装进探针内层 = 时序×探针合体。

### XCHAIN（v42 跨链偏序复活版）：L22 零增益——开阔盘前沿钉死

跨链机器已移植进 propagate_strong（XCHAIN=1，可靠性通过），但 L22 上链仍太稀
（F+假设只有 ~18/129 边，链短且互不引用）。至此 L22 抵抗了四种武器：
一级(13.2%)、二级(18.6%，代价 200×)、either-way 交集(0)、跨链时序(0)。

**当前科学前沿的精确表述**：开阔盘（微缩零信息类）上，探针的内层传播缺一族
「开阔空间也能看见的时序规则」——现有全部规则都要靠墙或已定边当锚点，
开阔处无锚。候选方向（下阶段）：开阔区首穿定理的可执行化（首次进入开阔区
必须直穿——对假设态的开阔区计数）、列/行记账（冰面论文维度耦合的移植）、
或承认此类需要 O(未定边) 深的探针（即实际超多项式，Tron 之谜仍未全解）。

## 本日总账（2026-08-23，两天累计的一天版）
- 官方：30s 奇数 473→**499**、600s 全量 500（会师 L501）
- GPU：209.5M CPU 品质节点/s 内核（12×，逐位对拍）
- 理论：缝合线理论（19/19）→ 缝约束扫荡；唯一解基础（11/12）；
  多项式梯子（4/7 关 100%，L38 至其真天花板）；L22 前沿钉死
- 方法论：ground truth 先行、负结果全入档、每假设配可证伪预言——全程零可靠性事故

## 🗺️ Tron 之谜终解：零信息类只是中段的一条「指数带」，尾巴全是传播高速公路

高层类谱（决定性）：L601 gfix=1.8万/候选49｜L801 3.2万/123｜L1001 4.9万/46｜
L1101 41万/40｜**L1207（1990×1990）gfix=189万/候选66**——四百万格的最终关
在传播下只剩 66 个起点候选！

结论：**传播强类统治尾巴**（大盘的墙结构给传播充足锚点），零信息开阔类
只盘踞中等尺寸带（~L100-550?，边界测量中）。Tron 的「多项式」是强类上的
真实现象；指数带他当年同样靠搜索硬啃——没有失落的魔法。
我们的 499/500 卡在带内最厚处（L501），带的出口之后是高速公路。

L22 前沿终报：六件武器（一级/二级/交集/跨链/割集记账/形状）确定率平台
11.6~18.6%；形状梯子新产出=端点候选 84→34（新信号类型）。开阔小盘的
剩余确定力需要超出本日全部规则族的推理——留作长期研究题。

### 🗺️ 指数带边界实测：L501-503 之后立刻回到高速公路！

L505~525 全部传播强类（候选 42~193）；零类只剩零星孤岛（L531、L555…）；
L537 以后至 L1207 抽样全强。**挡在 499/500 前的只是 L501-503 三关 + 零星孤岛**，
穿过 501 分数即冲 ~525+。战略从「攻克一个类」变为「各个击破有限的孤岛清单」。
（注意孤岛尺寸随关号增长——L531 有 21440 格，比 501 更大：孤岛难度递增，
每座都需要 600s 内的真解能力，硬编码离线解=作弊不可取。）

创世之谜终态：生成器随机产出两类盘（墙锚型 92% 传播可杀｜开阔型 8% 指数带）；
Tron 无失落魔法（强类=传播 99.7%，零类他也硬啃）；剩余问题=有限孤岛清单。

## 🔎「推理看见缝」攻坚记录（圆弧模型 + 松紧解剖）

**圆弧模型**（理论成果）：把解视为闭环+断点，每个软垫背转弯在环上禁止一段
断点弧；合法缝 = 圆周减弧并集。一次解释三件事：缝稀有（弧盖满圆周）、
起终点相邻（末滑撞在起点格上自锁）、收尾口袋（时间跨度最大的依赖钉住缝位）。
**可算法化的特例**：距离 1 的严格环实例（L414/432/474）——先找图哈密顿环
（无滑动约束，易），再做圆弧扫描直接读出缝，纯多项式。留作下一战验证。

**松紧解剖（负结果，关键认识）**：真解的时序依赖普遍很松（slack 最小 5~13），
软垫背仅 9~42%，紧依赖零星散乱——「紧依赖聚缝」假说被否。
**深层结论：缝的稀有性不长在成功解身上，而长在全部失败路径的集体中**——
真解温和从容，无挣扎痕迹可验尸。看见缝必须对反事实空间推理（探针/搜索），
或走环情形的圆弧扫描。小关语料的局部/解剖学路线至此全部穷尽。

## 👑 圆弧扫描验证成功：「推理看见缝」在环情形被证明（research/arc_scan.py）

L414（严格环实例，环长 13440）：
- **正向合法缝仅 8 个**：(113,116)真起点、(112,116)真终点 + 6 个邻居——
  两个已知赢家全在名单，赢家聚集现象被精确复现
- 反向合法缝 0 个（环只能单向走——滑动不对称的直接显形）
- 499 条软垫背约束、纯几何、零搜索、O(n·T)

**定理（环情形）**：给定图哈密顿环的几何，滑动可行的断点集 = 圆周减去
全部软垫背转弯的禁止弧 [j+1..i]（断点两端转弯豁免）——多项式读缝。
谜题变换：找滑动路径（时序地狱）⇒ 找「弧扫描有缝隙」的图环（几何+多项式验证）。

下一步（新战役的起点）：
1. 相位 A：不知解时的图环搜索——图环候选多不多？弧缝隙过滤能否唯一化？
   （若 L414 的图环中只有真环有缝隙，整个问题在环实例上就是「图环枚举+扫描」）
2. 缝区推广：距离 >1 的实例（语料多数），断点变断弧段，扫描公式需推广
3. 若 1 成立：接进求解器 = 零信息类环实例的多项式管线

### 相位 A 初战：朴素 MILP 不收敛（2-因子恒碎 ~140 环，60 轮割平面无效）

与 v30 时代测量一致——图环搜索需要真方法（文献现成：FHCP 的 SLH 启发式）。
败仗中的新点子：**深败回收**——错误起点的深搜索若覆盖 95%+，其残骸≈从错误
断点走的真环；提取→补全→弧扫描读缝→真缝重启。把相位 A 从「从零找环」
变成「从失败捡环」，与现有搜索天然融合。待测：错误起点深搜的最大覆盖率。

（今晚理论弧线完整版：缝不可见 → 圆弧模型 → 环情形读缝定理（L414 实证 8/13440
含双真赢家）→ 相位 A 待解（SLH 或深败回收）→ 缝区推广待做。）

### 深败回收：死刑（错误起点深搜最多盖 74.5%，残骸非近环）

L414 实测（2000 万节点/T3LIMIT）：错误起点 bestrem 3428~6371/13440
（最好只盖 74.5%），真起点对照 bestrem=0。错误的代价在早段就付——
残骸不含环几何，无可回收。相位 A 唯一活路回到正经图环构造（SLH 启发式）。

（今晚「推理看见缝」攻坚终盘：圆弧读缝定理成立（环情形，L414 8/13440 实证）；
相位 A 三案两死一活：朴素 MILP 死、深败回收死、SLH 构造待建——下一场的头号工程。）

## 🔗 LKH 管线首航：相位 A 通了，自检机制如设计工作

- 请出学界正规军 LKH-3（Helsgaun）：L414 编码为 TSPLIB HCP，**300s 找到合法
  哈密顿环**（13440 格，缺陷 0）——相位 A 的专业解法成立
- 找到的环 ≠ 真环（86% 边重合）——图环不唯一
- **圆弧扫描正确否决错环（两方向 0 缝）**——自检如设计工作：错环处处滑动矛盾
- 关键重构：**不需要找到「真环」——任何扫描有缝的环本身就是合法解**。
  管线 = LKH 采样环 → 弧扫描验缝 → 无缝换种子 → 有缝即解
- 剩余唯一问题：滑动可行环在全部图环中的占比（命中率）——14 种子并行采样中
- 土方 SLH 对照：120s 缺陷压到 10/13440 未清零（2-opt 平台效应）——专业工具完胜
（彩票批第三轮 ALLMISS，L501 累计 15 连败——下限证明机持续工作）

### 丰度判决：0/14——随机图环无一滑动可行，修补匠路线转正

14 种子 LKH 采样全部 0 缝（难度守恒：硬度整体搬进了环空间的稀有性）。
管线分支二启动：**滑动修补爬山器**——在环空间做 2-opt 局部搜索，目标函数
= (min_s 弧覆盖数, 总弧质量) 字典序；覆盖 min 降到 0 即得可行环+缝。
增量评估：翻转段内转弯的进入方向反转 ⇒ 约束集 O(len) 更新；
cov 用差分数组 O(T+n) 重建 ⇒ 单步评估 ~2 万操作，万步/秒可爬。
这是「无人区修补匠」岗位——LKH 管图合法，爬山器管滑动合法，
成熟工具与土方法的正式分工。

### 缝区推广的答案（用户问题直接催生）：修补匠搬进路径空间，环假设整个扔掉

关键设计跃迁：爬山器不需要环——**路径几何只有两个走向，每个走向的
「违约数」（软垫背晚于转弯的个数）就是损失，O(T) 可算**；2-opt 翻转在
路径上合法且天然挪动端点（起终点也被搜索）。损失=0 ⇔ 合法滑动解。
- 环情形退化为特例（曾用它验证理论因断点自由度让数学最清晰）
- 每个零类关必然有靶（路径解按定义存在）——不再赌 L501 是否成环
- LKH 角色改为提供高质量初始几何
语料分布备忘：19 零类解中严格环仅 3 个（距离 1），其余 3~21——环管线
覆盖少数派，路径版覆盖全类。小号闭环校准靶：L73（41~139 扫描唯一命中）。

### 掰绳工程首审：2-opt 贪心死刑（用户直觉先于数据）

L73（502 格校准靶）：11.4 亿次尝试、83.7 万步接受，min_cov 从 112 降至 90 后
**绝对平台**（mass 同步冻结）。L414 同症（3938→3438 后蜗牛化）。
地形定性：弯的靠山链式耦合——修一个弯拆三个弯，单步小手术必陷坑，
坑的数目组合级。用户判断在先：「非常多的点去卡」——正确。

下一级候选（按文献成熟度）：
1. **backbite 移动**（格点哈密顿路径采样的标准移动，遍历性有证明，
   统计物理密堆聚合物界的成熟工具）+ 退火——和 2-opt 不同物种，值得一次测量
2. Lin-Kernighan 式变深度复合移动
3. 混合编队：掰到接近可行交回搜索器收尾
（绳子模型白话版已成为项目通用语言：定长贴板不压自己、弯弯要靠山、
缝=从哪头铺、罗盘=悬空弯计数。）

## ⚡ CP-SAT 完整编码：滑动规则首次进入带学习的求解器——L73 7.8s、L22 0.1s

编码（research/cpsat_solve.py）：AddCircuit（图半边，含虚拟格=路径起终点输出）
+ pos[c] 位置变量（弧选中 ⇒ pos 递增）+ **滑动蕴含子句**（进入 c 且不直行且
垫背 b 自由 ⇒ pos[b] < pos[c]——「弯要靠山」的完整逻辑化）。

| 靶 | 结果 |
|---|---|
| L73（502 格，闭环校准靶） | **7.8s OPTIMAL**，滑动自检过，且给出与已知解不同的新解 |
| **L22（六武器全扛的开阔试金石）** | **0.1 秒瞬杀**，直接给出真起点 (8,1) |

背景判决（同日）：几何空间局部搜索全线阵亡——2-opt（L73 11 亿次卡 90）、
backbite 退火（3.2 亿次冻死 111）——用户三连预判全部正确。
差距的本质：CDCL 子句学习让每次失败变成全局共享的永久禁令（「失败会说话」），
盲目局部移动的失败则是哑巴。
文献锚：CP2020 HCP-SAT 编码研究（picat-lang.org/papers/cp20.pdf）、
CP2024 顶点消去编码、CP-SAT Primer。
L414（13440 格）尺度测试进行中——若过，L501 就是下一个。

## CP-SAT 编码换代：朴素位置编码退役，二进制加法器编码上线（2026-08-22）

前沿扫描（朴素编码，即 13440 域 IntVar 位置变量）定位了它的墙：
- L101（n=894，零信息类、894 起点仅 1 可解的钉子户）：**OPTIMAL 13.8s，SLIDE-VALID**。土法探针梯队打一天的关，通用求解器 14 秒。
- L141（n≈1100）：OPTIMAL 20.7s，SLIDE-VALID。
- L305（滑动子句 13848 条）：120s UNKNOWN。
- L414（n=13440）：600s UNKNOWN，16GB 内存。
结论：朴素编码的墙在几千格档，L501（19232 格）无望，退役。

朴素+缝合线约束的 L414 二发因 WSL 重启丢失，无结果（不补跑，编码已换代）。

换代：research/cpsat2.py，CP2020（picat-lang.org/papers/cp20.pdf）胜者编码移植进 CP-SAT：
- AddCircuit 保留（连通/度传播是 CP-SAT 强项）
- 位置变量 = B=ceil(log2 n) 个布尔位/格（L414: 14 位），弃大域 IntVar
- succ(u)=bits(u)+1 涟漪进位加法器每格一份、弧间共享；弧选中 => bits(v)==succ(u)（每位 2 子句）
- 滑动子句 => 一侧 MSB 优先比较器 bits(垫背)<bits(转弯格)，等位链按无序格对共享
- 缝合线约束保留（起终点 L1<=25，19/19 实证）；SEAMCON/SEAMR2 环境变量开关
- 模型体积 O(n log n)：L73 总子句 10.2 万（朴素为 n 个 502 域 IntVar 反化线性约束）
L73 自检：OPTIMAL 24.6s SLIDE-VALID（比朴素 7.8s 慢——小关上朴素的强传播占优，符合文献预期；
二进制编码的收益在模型体积，赌的是大关）。扩展测试 L305/L414 后台进行中。

## L414 判决 + 注入弹药普查 + F 信号发现（2026-08-23）

二进制加法器编码扩展测试判决：
- L305（n=7328，221万子句）：300s UNKNOWN
- L414（n=13440，442万子句，含缝约束）：627s UNKNOWN
开阔大关两种编码全灭。体积从 16GB 压到 ~2GB 换不来推理力——CP-SAT 输的不是内存是传播。
L501 裸跑（19232 格，SEAMCON=0 保可靠）在途。

L501 靶子解剖：170x169，自由 19232，墙 9498（33.1%），撒盐式均匀散布（非迷宫非走廊）。

PROBEDUMP 普查模式（v58 新增，不需已知解）：逐起点 propagate(s)，矛盾=可靠证伪；
幸存者 estate 交集 = 全局边事实（真起点必幸存 => 交集 ⊆ 真起点事实 => 对一切解成立，零赌博）。
- L501 纯传播：19204 幸存 / 28 证伪，交集必用 7 禁用 0 —— 注入弹药近乎空仓。
  这就是 L501 难的本质：传播在它身上近盲。STRONG 版（流层+终点过滤）普查在途。
- L531（同类带墙岛屿关，v58 可解）：21399 幸存，交集 9 条；但每起点传播定边 ~8100 条（20%），
  弹药不在交集在条件事实。

F(s) 信号（新假设）：F = 起点 s 假设下传播定出的边数。
L531 现场解出真起点 (105,128)：F=8104，升序排位 859/21399 = 前 4.0%（分布 med=8143 max=13234）。
方向反直觉：**真起点 F 异常低**（真起点最「松」，错起点过约束）。
单样本不作数 => 80 个缓存解逐关验证（/tmp/frank_sweep.py）在途。
若普遍成立：免费起点排序器，直接治 L501 病根之一（赢家起点排位深）。

工程教训：MSYS->WSL 边界会剥一层反斜杠，C 源补丁里的 \n 要用占位符+chr(92) 拼；
pkill -f 的模式会匹配自己所在 shell 的命令行（用 [x] 括号技巧自保）。

## F 信号验证 + STARTORD 武器化 + L501 试射（2026-08-23）

F 信号 81 关验证总结：升序百分位中位 16.8%（随机=50%），<=10% 占 31%，<=20% 占 52%。
分布双峰：25 关在前 10%，同时 10 关在后 10%（最差 L411=100%）。纯升序覆盖好的一半。
几何特征面板（距墙 BFS、5x5 密度、度数、首滑 min/max）全平（中位 43-67%）——只有 F 有货。

缝定律扩展到带墙类：L531 现场解 起点(105,128) 终点(106,126) L1=3。
证据链：零信息类 19/19 + 带墙类 1/1 全是近环。生成器「造环剪缝」画像再加一码。

STRONG 普查白跑：L501 证伪 41 vs 纯传播 28，交集照样 必用7/禁用0。弃。

STARTORD 武器（v58 新增）：PROBEDUMP 普查输出直喂（OK 按 F 升序重排起点，REF 可靠剔除）。
L531 接线测试：F 序就位（21399 起点，剔 41），180s 内解出（且从另一真起点 (105,125) 解出——
多真起点实锤）。

L501 试射（在途）：STARTORD=F升序 x PDPROMO=999999 SEEDMODE=2 x 600s。
预算账：F 序期望真起点前 ~17% ≈ 3300 名，tier1 扫 ~2min，晋升深挖 ~0.5min，600s 装得下。
此前 15 连败病根 = 赢家起点排位深（度数序埋在万名外），F 序治的就是这个。
早期日志再证 L501 传播全盲：全局固定 0 边，19232/19232 起点全候选。

## 第16击尸检修正 + 缝指纹阴性 + 两段流水线（2026-08-23）

第16击（F序×三连击×600s）EXIT=124 脱靶。尸检两个修正：
1. **此前"剩130"是每分片数字**：19204/28≈686 起点/分片，tier2 后每分片剩 ~130，
   全局决赛圈 ≈ 3600 个候选，不是 130。
2. **L501 的真病根是漏斗太贵，不是赢家埋太深**：tier1(2000节点)在别的关证伪 95%+，
   在 L501 只有 ~45%；~1 万起点涌进昂贵的中层（PD=999999 深传播重探），600 秒全烧在
   证伪错误起点上，决赛圈没开打。15 连败的钱都花在这。

缝指纹挖矿（81 解）：**全平，阴性**。起/终点的贴墙数、5x5 密度、板边距中位 42-53%，
无聚集——"缝贴墙"假设死刑，缝在几何上无指纹。缝 L1 高置信重确认：中位 7，
十分位 1/4/5/9/15，max=41（样本 81，比旧界 21 宽）。

白捡：v58 早有 NORANK 开关（跳过探针得分重排）→ FDEEP 不用写，
STARTORD+NORANK 组合即"F 序统治深挖"。

新开关：FUNNELDUMP（tier2 后每分片落盘决赛名单）、STARTONLY（STARTORD 文件外的起点全剔）。
两段流水线实验（在途）：A 段 900s 跑漏斗存 ~3600 决赛者；B 段 STARTONLY 只喂决赛圈纯深挖 600s。
B 段若命中 => 问题收敛为"漏斗加速"单一工程题（如 tier1 预算加大提前穷尽、中层降价）。

朋友摘要对表（打到 L362 的 Codex 项目）：可逆DFS+分区、奇偶、割点、Dinic匹配剪枝、
UNSAT缓存（我们实测 0.03% 判死）全部重合；他的"下一步"（强制边传播/失败探针/CUDA）
是我们的现在与过去。他独有：Beam（不完备，弃权）、跨轮持久前沿（等效无限累计时间，
规则姿势不同）。我们独有：类分家、缝定律、F 信号、普查交集。

## B 段判决 + 板相全同 + 针密度实验（2026-08-23）

两段流水线 B 段（3129 决赛者、F 序、NORANK、600s 纯深挖）：EXIT=124 脱靶。
与 16 击尸检合并定案：**L501 漏斗和决赛圈两头都超预算——增长率问题，非调度问题**。
（技术花絮：B 段首发吃了空名单——决赛名单是等待器的活，被掐 A 段时连锁跳过，已重发。）
校准长跑（同配置 3600s、24 核）在途，量差距倍数。

死因统计仪表（DEATHSTAT）装好：五个死亡出口计数 + 周期打印
（首版挂分片收尾打不出来——PDEATHSIG 直接 SIGKILL 没有收尾机会；改 dfs 每 8M 节点周期吐）。

滑长理论生灭一小时：猜想"L501 撒盐墙 => 滑短 => 树深 => 爆炸"，实测八关全同枪毙：
L414/474/531/555/505/501/502/503 墙占全部 33%±1、平均滑长全部 2.65-2.75。
**可解关与不可解关在板面统计上完全不可区分**（旧地图"只有 501-503 有墙"是错的，全员撒盐板）。
连 gfix 都分不开：L531 交集 9 条 vs L501 7 条，都近零，但 L531 四分钟解、L501 3600s+ 不解。

现头号嫌疑：**真起点密度**（针尖关假说）。旁证：L531 两次解出用了不同真起点
（105,128 / 105,125），L101 是 894 挑 1。若 L501 是 19232 挑 1 而 L531 是一抓一把，
"离谱"全解释——是生成器在关口调了针密度，不是板子长相特殊。
定量实验（在途）：L531 决赛圈 788（漏斗存活率 14.7%，与 L501 的 16.3% 相近——
漏斗行为也一样！差异只可能在决赛圈的可解密度）；两边各抽 200 决赛者 x 5s 短试射数出解率。

## 针的真相 + 夜间作战（2026-08-23 深夜）

密度实验判决：L531 和 L501 各抽 200 决赛者 x 5s 全部 0 命中——**针在哪个关都极稀有**，
"L531 针多所以容易"猜想枪毙。
但真起点校准爆出全项目最重要的数字之一：**L531 两个已知真起点在 5 秒单核内全部解出**
（纯 Warnsdorff、无 swarm）。推翻"赢家树 >=32M 节点"旧诊断——**站对起点，树小得可怜**。
B 段脱靶真凶随之落案：tier3 的"每起点无限搜穷"让队首的错误起点（树搜不穷）堵死队伍，
针即便排第 5 也永远轮不到——经典 head-of-line blocking，调度病不是算力病。

第 17 击（在途）：全体 3129 决赛者 x 5s 单核 x 28 并行 ≈ 560s。
若 L501 的针也是小树，此击直接命中；注意与世代扫描有过抢核，脱靶需干净重跑再定论。

夜间阶梯（night_ladder.py，自动，命中即停）：预算 x 策略网格
30s/id0 -> 5s/id{5,12,26}（swarm 抖动/大抖动/纯哈希序三档树形）-> 120s/id0 -> 30s/id{5,12,26} -> 300s/id0。
理据：树形多样性可能比预算更值钱（不同 ident 的树形状完全不同，小树可能藏在别的树形里）。

榜单事实（用户）：全世界过 500 的没几个人 => 501 是筛掉全世界的闸门，质变非量变；
过的人靠结构不靠算力（用户判断，与我们数据互洽）。
用户假说升级：**501 是新生成器算法的起点**（批次边界+针密度骤降）。
世代扫描（1207 关便宜信号找 501 处的统计台阶）在途。

命中后的解剖清单（morning TODO）：针 vs 3128 陪跑者逐特征对比（F/几何/局部形状，
在决赛圈内部比！）、解回放验近环、量真树形、看缝位置、对生成器假说；
然后做 600s 合规化（找能在预算内走到针的排序/配置）。

## 夜间战报 + 效率审计 + 18 击（2026-08-24 晨）

夜间阶梯 6.5 轮全脱靶：30s/W、5sx{抖,大抖,哈希}、120s/W、30sx抖、30sx大抖(中断)。
17 击（3129x5s）也 0 命中。**但排除力度被两笔浪费掺水**：
1. 启动税：每次单起点测试独立进程，~13s 开局成本（5s 档 72% 浪费）。
2. 鞭尸：起点搜穷=永久证死，但各轮互不知情重复搜。
3. 最重要：STARTONLY 单起点进程 100% 存活率触发升档 PD=16（每节点贵百倍），
   "5 秒"实际只搜几百万节点——夜间的节点数口径全部高估。

考古发现：v53 的"多轮限额扫荡"(SWEEP=3M 起 x8/轮、穷尽剔除、末轮无限)早就是
head-of-line blocking 的药，注释还留着关键数字：**L501@PD16 树成本重尾：
中位 15 万节点、尾部 3.5 亿**。B 段死因补正：升档 PD16 后每节点成本暴涨，
600s 连第 0 轮(3129x3M)都没扫完。TREELOG 开关现成（每起点节点数+判决遥测）。

世代扫描定论（1207 关）：**501 不是新生成器时代**——全图 122 个 gfix=0 针尖关
（n>=3000 的 86 个）散布在富矿海洋里；497-499/502-505 全是富矿关（候选率 0%），
过了 501 白送 8 关到 510。496 是 501 的孪生针尖关且曾被 600s 打穿（罗塞塔），
531/555 是更大的已解针尖关。**L501 = 我们第一根没掷中的针，不是质变**。
用户榜单观察：全世界过 500 的没几个 => 这根针筛掉了全世界。

18 击（在途，3600s）：单进程批量、3129 决赛者、NORANK+F 序、SWEEP=1M 限额扫荡
（1M->8M->64M->无限，穷尽永久剔除）、TREELOG 全遥测。产出：穷尽曲线+怪物名单+可能命中。
后续预案：怪物核 <=168 人时 swarm 模式自动开启 => 28 种树形群殴（多样性总攻）。
L496 罗塞塔解剖并行中（夜里那发死于 nohup 忘 sleep 的老陷阱，晨间重启）。

## 缝球刀验尸 + 死因经济学 + PD64 大发现（2026-08-24）

缝球剪枝（SEAMR：tcand 与起点 L1<=50 球求交）A/B 判决：死。
L305 2s=2s、L414 25s=25s、L474 68s vs 104s（反而慢）。
死因剖面（DEATHSTAT 挂 emit）给出铁证：**liveend=0**——"终点候选耗尽"这个死亡通道
在真实搜索里根本不触发（live_end 只在候选被"踩掉"时减，跑远的分支不踩球内格子，永远砍不着）。
教训：挂载点比刀刃重要，20 行的直觉刀要先量死因再磨。

死因经济学（L414 赢家分片）：estate 禁边 53%、局部连通 33%、几何 7%、动态传播 6%；
赢家树仅 13371 节点。传播才是主刽子手。

FULLREACH（传播窗口外的分支点做全局连通）A/B：也死。穷尽 4/28 输给裸跑 7/28，
样本树两臂节点数一模一样——深层没有漏网的断连分支，局部检查早就抓干净了。

**PD64 大发现**：死因指向传播 => 测传播深度窗口。28 个 1M+ 节点"怪物"起点 x 30s 单核：
- PD16（默认升档）：穷尽 7/28
- **PD64：穷尽 28/28（全灭！）**
- PD999999：穷尽 27/28
怪物不是怪物——是 16 层窗口盖不住几千步路径。窗口拧到 64，砍树回本点完美命中。
（PDPROMO 旋钮 v51 就有，"最优常数底层一动就搬家"的老坑第 N 次应验——
16 是在中盘关调的，L501 尺度下早该重扫。）

19 击（在途）：全池 3129 决赛者 x PDPROMO=64 x SWEEP=50M 限额扫荡 x NORANK F 序，
预算 5400s（估算 ~1h 扫完全池）。逻辑二选一：命中，或全池穷尽（=上游可靠性 bug 暴露）。

## L501 攻陷（2026-08-24，第 19 击）

**解验证全绿**：19232/19232 全覆盖、滑动合法回放通过、10117 步、
起点 (55,157) 终点 (48,155)、缝 L1=9（近环定律 83/83）。
配置：全池 3129 决赛者 x PDPROMO=64 x SWEEP=50M x NORANK F 序，命中于第 ~1995 个处理位
（shard5 #97），墙钟 ~40 分钟。解已入 solutions/501.sol（缓存，不上传）。

针的解剖（两个信仰被处决）：
1. 赢家树 2361492 节点 @PD64 ——"3.5 亿节点怪物"是 16 层窗口吹出来的幻象。
2. **F 序排位 2739/3129（倒数 12%）**——L501 属 F 双峰的坏尾巴，F 升序一直在从错误
   的一端挖。命中靠的是 PD64 让全员变便宜，不是 F 指路。F 信号对 L501 记大过。
全池判决分布（至命中时）：~98.8% 穷尽 r=0，15 个 50M 顶格硬核。

胜利公式复盘：死因经济学（传播 53% 主刽子手）=> 传播深度窗口 A/B（PD16:7/28,
**PD64:28/28**, PDinf:27/28）=> 全池扫射。三步全是测量驱动，零彩票。
用户两次方向盘：叫停蛮力磨池（"要想办法剪枝了"）+ 认定结构性缺陷——都对。

在途：合规化验证（无预热单进程 PDPROMO=64 x 600s 标准预算）——指标涨分的那一发。
待办：PD 常数按尺寸自适应化（16 是中盘调的，L501 尺度要 64——"底层一动重扫常数"第 N+1 课）、
502/503（富矿关，应当白送）、两指标全量重测、510/523/531 下一批针尖关。

## 合规化冲线实录（2026-08-24 深夜）

合规尝试 1-5 全超时，每发揪出一个真 bug（这段调试本身就是教材）：
- 二验：FRANK=3 奇偶对冲——针的分片抽到升序方向，对冲背运（针全池降序 391 名，射程内却在错方向）。
- 三验：加相位计时。账本：漏斗 147s + 决赛圈 ~20s/树，射程 = 每分片 ~22 名。
- 四验（FRANK=1 全降序）：仍脱靶 => 尸检发现**跑内 F ≠ 普查 F**（采样点在强传播/probing
  改写 estate 之后，两套 F 名次对不上，针实排分片第 2 却被排到射程外）。
- 五验（F 采样点挪到纯传播后）：仍脱靶 => 又揪出两个一致性 bug：tier1.5/tier2 幸存者
  压缩循环搬 starts/sc/est_save 但**不搬 fsv**（F 值错位）；且 tier1.5/tier2 之后
  各有一个按 sc 的旧重排会**整体覆盖 FRANK 排序**。全部修复。
- **六验（FRANK=1 + PDPROMO=64）：EXIT=0 全链命中！** 针于 shard13 降序第 32 名、
  t=634s 被扫到，nodes=2361492，解与 19 击逐位一致。距 600s 红线差 34s（6%）。
- 七验（在途）：SWEEP=2600000（针树 2.36M+10% 余量，顶格树省 13%）+ TIER2=101（跳
  二层漏斗，tier3 提前 ~40s）冲线。

诚实声明（用户问"是不是照着答案调的排名"）：**FRANK=1 降序方向就是照 L501 的针调的**，
81 关实证的好方向是升序（L501 属坏尾）。用户裁决：先过关，普适性债后还。
普适化正解已设计好：FRANK=4 双端交替扫（每分片 F 排序后头尾轮流处理，2 倍常数
换掉方向赌博，对 F 双峰两侧都保证 2 倍内命中），待实现+全量回归。

## L501 合规攻陷（2026-08-24 深夜，七验冲线）

**七验 EXIT=0，t=492s < 600s 红线，余量 108s。合规成立。**
配方：FRANK=1 + PDPROMO=64 + SWEEP=2600000 + TIER2=101。
TIER2=101 是冲线关键：漏斗 149s -> 27s（跳二层，幸存者 100->271/分片、针名次 32->76，
但 tier3 提前 122s 开工，净赚 142s）。解 = (55,157) 10117 步，与 19 击逐位一致。
自 v58 认证 499/500 以来，503 天的墙（关卡意义上）正式破了。

下一步：502-510 连环试射（富矿平原收割 + 下一根针 L510 摸底）；FRANK=4 双端扫 + 全量回归。

## 新边疆：501-510 十连破（2026-08-24 深夜）

合规配方（FRANK=1 PDPROMO=64 SWEEP=2.6M TIER2=101）连环试射：
502(1s) 503(0s) 504(2s) 505(1s) 506(1s) 507(12s) 508(5s) 509(0s) —— 富矿平原如预测秒杀。
**L510（下一根针尖，n=19872，gfix=0）：124 秒攻破！** 配方零调参轰穿新针，
"FRANK=1 是 501 特调"的过拟合疑虑大幅缩水（在 510 上 F 降序同样在射程内）。
成绩线：499/500 -> 至少 510。511-535 连扫在途（下批针尖 523/531 在射程内）。

## 35 连破：新边疆推至 535（2026-08-25 凌晨）

511-535 连扫全 HIT 零脱靶。**成绩线 500 -> 至少 535。**
针尖关全部零调参攻破：510(124s) 523(33s) 531(34s)。L531 对照：旧配置 ~4 分钟 -> 34s，7 倍。
百秒榜（工程优化靶,用户点名）：L514(152s,非针尖却最贵!) L510(124s)；
次级样本 523/529/531(33-34s)。
慢关优化思路：TREELOG 遥测拆三数（漏斗秒数/针名次/每树秒数）,定位是漏斗税、排序名次还是树价。
536-570 连扫在途（针尖 546/555/569/570 在射程）。

## 项目命名（2026-08-25，用户）

未来写成文章的话，中文标题定为：**《面向叹息之墙的冲击》**
（叹息之墙 = L501：全世界过 500 的没几个人，排队至此齐齐叹气折返的地方。
副标题候选：或——论一个常数如何挡住全世界。）

## 叹息之墙 2.0：L570（2026-08-25）

536-569 连扫全 HIT（针尖 546/555/569 分别 -/-/71s 攻破）。**L570 MISS 660s——新墙。**
成绩线定格：**501-569 共 69 连破，官方线 500 -> 569。**
L570：针尖关（gfix=0），n=24848，路径 ~18000 步——比 501 大 29%。
攻坚开工：PROBEDUMP 普查在途（单核陪跑）；571+ 侦察连推探墙后地形；
候选刀：PD 再升档（128？路径更长窗口要更大——PD 尺度律的直接预言）、SWEEP 上限放宽、
F 名次反推。工具箱全部现成，预期无需 19 击。

## L570 解剖：与 501 完全不同的墙（2026-08-25）

PD 阶梯（64/128/256 x 16 样本）：三档零差异——570 树的决策深度不超 64 层，窗口早已等效
无限，**PD 不是这堵墙的钥匙**（尺度律在此饱和，攻坚工具箱第一次扑空）。
随机抽 8 个普查幸存者全被强漏斗当场证伪——真决赛圈远小于普查幸存集。
600s 全仪表账本：漏斗仅 39s（证伪 24598/24848）；5920 棵树处理，5882 棵干净穷尽
（平均 ~1s/棵）；**38 棵怪物被 SWEEP=2.6M 顶格**，针几乎必在其中；第二轮加价扫
（8x 预算）没能在 600s 内啃完怪物 => MISS。
定性：**570 = 38 棵树的预算调度问题**（501 = 万人决赛圈的排序问题）。薄墙。
管线在跑：A 段 SWEEP=25M（首轮装下大针）-> B 段 38 怪物定点无限（兜底必中）
-> C 段合规验证 -> D 段 582+ 连推。

## L570 攻陷 + 合规（2026-08-25 午后，管线自动完成）

**A 段 SWEEP=25M 一发入魂；C 段合规验证 600s 通过（570-LEGAL-YES）。**
诊断到攻陷全程 4 发实验（PD 阶梯排除 -> 抽样发现强漏斗 -> 全仪表账本定性 38 棵怪物 ->
SWEEP 放大命中），无一浪费。对照 501 的 19 击：工具箱成熟的速度差。
成绩线：569 -> 581+（D 段 582+ 连推中）。
待办：配置回归矩阵（501/510/523/570 x {SWEEP2.6M, SWEEP25M, 2.6M+SWEEPMUL10}），
找单一全通配置——25M 可能伤 501 的怪物轮经济（legal7 依赖 2.6M 快扫）。
候选优解：SWEEP=2.6M SWEEPMUL=10（首轮保经济、次轮 26M 装大针）。

## 成绩线 602，下一堵墙 L603（2026-08-25）

D 段连推 582-602 全 HIT（582 针尖 91s、593 慢关 368s 入档、590/598 针尖静默通过）。
**L603 MISS——第三堵墙**（针尖名单预测命中，n=27548）。
成绩线：24 小时 500 -> 602（+102）。慢关档案追加：L593(368s)。

## L603 = 第三种墙：厚墙（2026-08-25 晚）

账本：漏斗 23s 证伪 27546/27548——**全盘只剩 2 个候选起点**（L101 型万里挑一的放大版，
前两堵墙的漏斗从来没这么狠过）。但幸存者的树是**上亿节点**级
（shard26 一棵 113912339 节点 ~570s 才穷尽 r=0）。600s 啃不完 => MISS。
三墙三性：501=排序问题（万人决赛圈）、570=调度问题（38 棵怪物）、**603=树价问题
（2 个候选、亿级树）**。工具箱对前两种有效，对厚墙的解法要么等穷尽（过夜），
要么需要新的树内加速（诽谤学习的用武之地？）。
过夜作业：2 候选 x 28 树形 swarm x 8h 无限预算。若全部穷尽仍无解 => 可靠性 bug 警报
（真针被漏斗误杀），那是另一场战役。
在线打榜：注册流程已通（验证码已破、账密已入 1Password），卡在站点要求匿名先解 10 关
+ 网络超时，搁置待网络好转。

## L603 厚墙再解剖：密室现象（2026-08-25 夜）

账本重读修正："剩 2"是每分片，全局幸存 ~56 个，**全部聚在右上角 (180-191, 13-32) 一小片**
——缝理论活标本（生成器剪缝区）。幸存者两群：便宜群（~950 节点）+ 怪物群。
**密室现象**：怪物群所有树节点数几乎逐位相同（113912328/113912371/113912339…±200/1.139亿）
——不同起点钻进同一间 1.14 亿节点密室，重复劳动 56 次。
=> 603 不是 bug 也不是远墙：全池扫完需 ~900-1200s，600s 差半口气。
=> 厚墙类的结构性药方（记账待做）：**密室穷尽结果跨起点复用**（诽谤学习近亲）——
   一次穷尽、全员免搜，成本 /候选数。
温柔攻坚在途：12 核 nice x NOSWEEP x 3900s，预期中途命中。

## L603 攻陷（2026-08-25 夜，温柔版 33 分钟）

12 核 nice NOSWEEP：t=1958s 命中，起点 (190,23)（两群交界处），
**赢家树 780851254 节点（7.8 亿）**——是密室树（1.14 亿）的 7 倍，厚墙实至名归。
合规化差距：这棵树 28 核全力也要 ~850s+，600s 红线塞不下 => 603 的合规需要
树内加速（跨起点密室复用 / 诽谤学习）或更狠的候选排序——第一堵"知道针在哪
也啃不完"的墙。成绩线暂稳 602；603 解已入缓存。

## L603 合规攻陷：自适应门反噬案（2026-08-25 深夜收官）

三发合规定位真凶的全过程（每发排除一个嫌疑）：
1. SWARMX=0 发：树仍 1.14 亿、节点数逐位同前——洗清 swarm 抖动。
2. TIER2 开门发：日志曝出"第一层存活 0/983 = 0%，第二层 跳过"——**真凶=自适应门反噬**。
   tier1.5(SURVDEEP>=38%)/tier2(TIER2>=30%) 的启动条件是"存活率够高"（为探针杀不动的关设计），
   603 存活率 0.2% 两层全跳，幸存者带着浅层 estate 进决赛 => 树 1.14 亿。
   单起点 A/B 恒 100% 存活率两层全开 => 同一棵树 11.9 万节点。门在万里挑一关上装反了方向。
3. **SURVDEEP=0 TIER2=0 强制开门发：EXIT=0，t=27s！** 针树 780851254 -> 1284285（600 倍），
   红线内余量 573s。配方：NORANK PDPROMO=64 NOSWEEP SURVDEEP=0 TIER2=0。

教训（重大，进配方手册）：**自适应门槛在尺度极端处会反向**。正确的门应看"幸存者数量"
而非"存活率"——幸存者极少时深探针每人成本趋零、收益 600 倍，无条件开。
成绩线：602 -> **603+**（明天从 604 连推）。今日总账：500 -> 603，+103 关/24h，三堵墙三种病
（排序/调度/门反噬）全破。

## 密室复用：测量判死（2026-08-26）

按纪律先测后建：CHAMBERSTAT 仪表挂 reach 死亡现场（抽样全量 BFS 量死区+隔断）。
L603 密室树 15 万样本判决：**93% 是局部口袋死**（全局连通，无密室可学）；
7% 真隔断的隔断集也大（多数 9+ 边界、平均死区 870 格）——nogood 模式太大太特异，
复用率注定低。**密室复用/诽谤学习 v1 证据不支持，搁置**（500 行机器省下）。
厚墙正解维持：深探针强开（SURVDEEP=0 TIER2=0）。测量纪律第 N 次值回票价。

在线战线：xbfool 老账号确认存在（用户："这个肯定是我的"），密码重置哨兵值守中
（站点间歇性 0 字节响应）；PuissantPower 备胎待邮箱激活。

## 成绩线 663，第四堵墙 L664（2026-08-26）

604-663 双配方连推 60 连破全 HIT-A（603 式深探配方通杀，B 配方一次没用上；
针尖 617/621/628/629/630/634/662 全数零调参攻破，慢关 619(383s)/662(310s) 入档）。
**L664 MISS 双配方——第四堵墙**（针尖名单预测命中，n=33486）。账本发在途。
在线战线批示（用户）：疑似我们把站点打伤（爬虫卡 442 每秒重试 = 无意 DoS，
真凶 = 长路径 GET 撞 414 上限，该按官方 JS 换 POST）——**在线全面停火**，
重启需带三件套：POST 提交、指数退避、限速。

## 第四堵墙 L664：漏斗税爆炸（2026-08-26）

账本：漏斗 **287s**（501/570=39s，603=23s，本关暴涨 7 倍！）证伪 33427/33486；
剩 313s 进决赛，59 决赛者、树便宜（几十万节点），但预算烧光扫不完。
诊断：n=33486 尺度下"全员深探针(SURVDEEP=0 全开)"的漏斗成本平方涨——
603 药方(无脑强开深探)在此过头。**这是门反噬的反面：603 该开没开，664 开太多。**
正解方向：深探针只对便宜推理的幸存者开（分层：先便宜筛一轮，缩圈后再对幸存者升深探），
即"门看幸存者数量"教训的正向实现。
实验：先跑默认门(SURVDEEP=38 让它自适应) vs 强开，比漏斗时间。

## SAC（singleton arc consistency）测量判死于全局层（2026-08-26）

按纪律先测后建：SACPROBE 模式（逐条未定边假设"必走"→传播→查矛盾）。
修了假阳性 bug（全局阶段后 dsu 脏，须用必用边重建）后：
- L305/L73：**SAC 新增禁边 0**。SACDIAG 证传播真在跑（每假设级联 2-10 条边），
  但从不矛盾——0 是真结果非空转 bug。
判决：**全局层 SAC 对 Coil 无效**。根因同早年 LP/流判死：空盘信息量太低、约束图太稀疏，
单边假设链不到矛盾（又一次印证"零信息类"）。SAC 唯一可能有用处是固定起点后，
但那本质=我们深探针已在做的事（假设起点+深传播），只是粒度起点级而非边级。
=> **我们其实已在用 SAC 思想（起点级 singleton lookahead）**，边级版无额外收益。
用户裁决生成器考古无劲（1000 道有限题，造轮子不划算），一并搁置。
通用武器库到此基本见底：SAC/LP/CDCL/编码路线全测过，剩下增量只能靠领域知识（缝指纹等）。

## 性能优化：trail 撤销测量判死（2026-08-26）

用户点名位图化/v54 76倍。查证：**v54 增量传播(propagate_dyn)早已并入 v58**（1485行），
已服役。剖析(gcc -O1 -pg，避免 -O2 全内联)：**传播函数吃 95.7% 时间**(2.4亿次调用)，
其余全零头。
发现"v54 只优化了一半"：propagate_dyn 每调用仍全量 memcpy 两块(estate2/dsu2 各 4N 字节)。
上 SAT 经典 trail 撤销(只记改动、只撤改动)优化 estate2 那块，--verify 可靠性零报警。
**但固定工作量 A/B：TRAIL 0/1 = 6703 vs 6666ms，提速≈0。**
判决：**memcpy 不是真瓶颈**。95% 时间在传播的实际 GAC 计算(端点过滤/并查集/逐边判定)，
是算法工作非固定开销，省拷贝类工程招数无效。trail 已撤(可靠但无用)。
方向钉死：**要提速只能动算法(让传播少做无用功)，位图化/增量拷贝路已堵死**。
（性能优化两条工程路——v54增量已在役、trail无效——通用工程手段基本见底，
同 SAC 结论：增量只剩算法/领域侧。）

## 参数自适应转正：三条尺度线统一 n=18000（2026-08-26）

四堵墙的手动调参经验固化成公式，全部挂在同一条 n=total_free>=18000 阈值上：
- **PD**：小盘 16(平台) / 大盘 64(501+实证，路径长需深窗口)
- **SWEEP0**：小盘 3M(收割L475的2.17M) / 大盘 (n-18000)*5000+3M(570→18M,664→80M标定)
- **FRANK 方向**：小盘升序(L475升序31s) / 大盘降序(501-663系统性坏尾，60关全实证)
  新增 FRANK=4 自适应方向模式，设为默认。

安全性：PD/FRANK 只对零信息升档类生效，富矿关(L500型)不受影响；阈值 18000 恰好
落在 L475(17175,保持)与 L496/501(18826/19232,升档)之间，不碰已通过的中盘。

**验证（纯裸默认，零环境变量，满核）**：
- L475: 31s HIT（历史 25s，F升序小差异，非回归）
- **L501: 564s HIT**（当年 19 击才破，现零参数自动降服！）
- L570: HIT（自适应 SWEEP 有效）
诊断工程教训：并行验证误用 JOBS=7 饿核致假 MISS；FRANK=1 全局降序过拟合 L501
拖慢中盘(475: 108s)，改自适应方向解决。
成果：**新墙不再需要手动调参，v58 裸默认自带三条尺度线自动选档**。
（L664 仍 MISS——第四堵墙需"决赛者更少"，非参数问题。）

## L664 深挖：早剪的机制找到了，但卡在"可复用计算"（2026-08-24）

用户方向：让最大子树早剪 / 复用计算，而非加预算。
死亡深度直方图（中位死树 47万叶）：**峰值在 depth~96 决策**，depth<36 累计仅18%——
**深死主导，非浅死**。窄深树（分支因子~1.15，绝大多数强制单步）。
=> 致命决策在浅处(~20层)就犯，但要到~96层才被发现。

思路A(终点分量检查)：**空转**——终点候选太散，任何连通分量都含终点，一个节点没剪反慢1倍。
度数-1强制端点判据：**早已实现用满**(cheap_ok的low_cnt<=1 + ntc_low)。
=> 现有廉价/中档剪枝对深death够不着。

**关键发现——更深前瞻能剪树，但被每节点成本抵消**：
怪物树(149,6)传播深度A/B：PD64=374万节点/43s，**PD128=170万节点(2.2倍剪枝!)/44s**，
256/512平台=170万。节点减半但墙钟不变——深传播每节点贵一倍正好抵消。
=> doom确实能在浅处发现(机制成立)，但深传播每节点从头重算前瞻，白付成本。

**真正的硬靶子=可复用计算**：窄深树沿强制链每步盘面只少一格、前瞻高度重叠却全重算。
把连通性检查(reach_local/reach_ok，死亡主因)做成**带回滚的递减连通性**增量维护，
节点减半才能真变时间减半。这是664合规的明确工程路径(有难度：删除下的连通性需link-cut或离线技术)。
(trail撤销之前无效是因为memcpy非瓶颈；这次目标是GAC/连通的实算复用，不同的东西。)

## L664 可复用计算靶子锁定：增量并查集（2026-08-24）

怪物树(149,6) PD128 精确 profile：
- propagate_dyn 64.6%（深传播本体）
- **dfind2 11%——并查集find被调54亿次**（10万次propagate_dyn，每次~5.3万find）
- ppush2 4%（24亿次）
合计~80%是传播机器，流仅7%。

诊断：propagate_dyn每分支点从dsu0**重建并查集**(10万次重建)，沿树重叠的连通计算全重算。
**可复用计算的确切靶子=带回滚的增量并查集**：记union、回溯撤销，替代每分支点重建。
（estate2 trail单独无效是因省memcpy非瓶颈；并查集find是真瓶颈，增量省的是54亿find背后重复工作。）
配合PD128节点减半(374万→170万)，节点与每节点成本同时降，才把时间真正砍下来。

奇偶/连通/度数廉价不变量全已用满(cheap_ok第256行奇偶、reach_local连通、ntc_low度数)，
深doom只有PD128级传播逮得住——所以必须让深传播变便宜，不能靠加廉价判据。
这是664合规的明确路径(难度：带路径压缩的并查集回滚需谨慎，或用union-by-rank无压缩+trail)。
下一步候选：实现增量并查集(dsu2 trail)，--verify验可靠性，怪物树A/B看PD128能否时间也减半。

## v59实验：增量传播——候选共享被golden判死(2026-08-24)

用户建议开新版本(v58不动)+简单观测测正确性。建v59-incprop2 + golden基准(3死树确定性节点数)。
第一步可回滚并查集(按size合并/无压缩/union trail)：golden逐位复现，DSU改动零行为偏差✓。
第二步"基座+候选增量"(算一次节点基座、候选间复用、trail回滚)：**golden抓到假证伪(unsound)**。
根因(有价值)：候选共享不成立——基座假定当前节点p是起点、滑行线自由；但每候选把p滑到不同落点c
(c才是新起点)、访问掉滑行线，起点身份变+被访问格推导被非单调推翻=>假矛盾。
=> 可复用的不是"候选间共享"，是"同一起点沿已提交路径单调累积"(单起点/可靠)=更大的dfs重构。
测量纪律再次值回票价：不可靠优化被golden当场拦下，没混进核心。
v59保留INCPROP开关(默认0=v58行为，已验证golden原样)，路径持久化版留作下一步。

## v59增量传播：撞上根本障碍——传播是"节点相对"的(2026-08-24)

用户洞察"回滚可在某级别做/不从头算"→实现INCPROP=2路径持久化+分支点检查点。
golden对拍+逐次VALIDATE精确定位：深度3处假证伪(增量=死,从头=活)。
**根本障碍(非bug)**：从头算每节点是全新子问题"从当前蛇头出发遍历剩余区"，头被当**起点**(欠一条出边)。
头随路径下移：父头c降到子后c变**内部格**(进出各一边)，c的推导从"起点待遇"变"内部待遇"→变了。
=> 持久累积从根不成立：**每节点推导是"相对当前头"的，非父推导的增量**。头一动起点相关推导失效。
结论：「不从头算」撞真墙——传播天生**节点相对**(头即起点)，无简单可累积状态。
复用需分离"头相关/头无关"推导=难得多的分解，非trail累积可做。
资产：golden测试台+可回滚并查集(按size/无压缩,正确但慢30%)可留作未来。
v59恢复INCPROP=0默认(=v58,golden逐位)。v58生产版全程未动。
诚实结论：增量传播这条路目前无净收益，且诊断出根本障碍。剪枝提速需换思路(分解式复用/别的结构)。

## 夜间自主(2026-08-24 01:xx)：银行流水线 + 空间排序判死

用户睡前布置两招：1.拿664再往后推 2.找其他思路。
招1：夜间银行流水线(nightbank.sh)启动，664-800逐关：先试600s合规，不行离线长跑(2400s)兜底，
校验合法才存缓存，遇硬墙不停车继续铺。产出：更多缓存解 + 完整墙地图(LEGAL/OFFLINE/HARD分类)。
招2探索：
- 赢家起点空间分布(490+关缓存解)：x中位0.52/y中位0.43/到边中位0.17=**均匀分布**，
  空间排序先验平(36%贴边=均匀值,非信号)。第N次印证：几何全平，只有F(传播派生)有区分度。
- 待试(需腾核)：按tier2探针节点数排序finalist(小树先搜)——若真针树小则更快够到，
  免费(仅重排)。是"reach needle faster"的合法路，非加预算。

## 割点剪枝(v60-artic)：进行中(2026-08-24 深夜)

用户方向：找优雅剪枝提前杀深树(非排序非预算)。且指出PD更深有天花板(实测PD128到平台=170万地板,
再深传播看不见剩下的doom)。
新思路——**割点剪枝**(结构性,正交于传播)：哈密顿路径去任一顶点最多分2块(去内部=2段/去端点=1段)。
故剩余自由区若有顶点去掉后分>=3块 => 无哈密顿路径 => 死。连通性检查抓不到(区域仍连通),
但结构注定无解。迭代Tarjan O(区域)算割点。
v60实现+挂dfs每节点(gate ARTIC)。死树(149,6)A/B进行中：ARTIC=0=3745484(对golden)；
ARTIC=1慢(每节点O(3万)贵,符预期)——待看剪多少。若剪破170万地板则值得(即使每节点贵可优化频率:
只分支点/每K节点/浅层)。
风险同PD128：每节点成本可能抵消。但它抓传播看不见的结构doom,正交,有独立价值。

## 夜间自主总结（2026-08-24，给早上看）

**招1（拿664再推）进行中**：银行流水线 nightbank.sh 正在离线啃664（合规600s已过、离线2400s中）。
预期：664离线解出入缓存(约1650 finalist全扫需~2400s,针在其中必命中)，然后自动铺665-800，
每关分类LEGAL/OFFLINE/HARD。早上看 /tmp/nightbank.log 拿全部战果+新墙地图。

**招2（找优雅剪枝提前杀深树）——诚实结论：这一晚的所有思路全部判死**：
1. 更深传播PD128：有效(3.7M->1.7M节点)但每节点成本正好抵消；且170万是地板(再深传播看不见)。
2. 增量传播(不从头算)：撞根本障碍——传播是"节点相对"的(每节点把当前头当起点解子问题)，
   头随路径下移则起点相关推导失效，不可简单累积。golden+对拍精确定位(深度3假证伪)。
3. 割点剪枝(去顶点分>=3块=无哈密顿路径)：几乎不触发(0.14%节点)、慢16倍。深doom不是粗连通分裂。
4. 终点分量检查：空转(终点候选太散)。奇偶：已完整(隐含终点色)。几何/空间排序：全平。

**根本诊断**：664深doom(~96层决策深死)抗一切廉价/优雅剪枝——传播能剪到170万地板，
其下是真NP硬核，需实际搜索。要突破得换范式(学习式/双向meet-in-middle/头相关与头无关推导的分解)，
非增量剪枝的小改能成。

**资产**：golden测试台(3死树确定性节点指纹)、可回滚并查集(v59)、割点Tarjan(v60)、
死因深度直方图工具——都在，未来可复用。v58生产版全程未动，仍是自适应转正的663能打版本。

**给用户的判断**：664这堵墙用当前范式"600s合规"暂时攻不下(离线能解、能上榜)。
成绩线663是扎实的。下一步真想推664合规，是一个需要新范式的研究项，不是一晚能成——
但离线银行能让榜上分数继续涨(打榜不限时)。

## 夜间战果：成绩线663->694，L695首个HARD墙（2026-08-24晨）

银行流水线整晚：664 OFFLINE(2432s离线解出,超600s墙,已缓存验证起点(90,11)全覆盖)；
**665-694 全部LEGAL**(30关全600s内,最慢687=271s,富矿平原延续,自适应求解器通吃)；
**L695 = 第一个HARD墙**(离线2400s都没解出,比664更硬)。
成绩线一夜 663->694(+31,解全缓存)。缓存解总数583。
L695待办：专门更长离线攻坚(可能只是更大需更多时间,或真更难)。流水线继续696+。
配置验证：自适应默认(PD/SWEEP/FRANK按n自动)在665-694上全合规,自适应转正在实战中站稳。


## ⭐ L664 诊断被推翻：不是「深 doom / NP 硬核」，是**调度**（2026-08-24）

之前的结论是「664 深 doom(~96 层决策深死)抗一切廉价剪枝，其下是真 NP 硬核，需换范式」。
**这个结论是错的**，而且错在一个没做的测量上。

我们**早就知道 664 的赢家起点是 (90,11)**（离线解出来的）。加一个 `PINSTART="x,y"` 开关只跑它：

| | |
| --- | --- |
| 只跑赢家起点 (90,11) | **10 秒解出，树 291,193 节点** |
| 全量跑（33486 个起点） | **2432 秒** |

**赢家的树只有 29 万节点、10 秒。** 所以 664 一点都不"硬"——
之前把**错误起点的怪物树**（如 (149,6)，374 万节点）当成了"这关难"，
但那些树是**要被证伪的**，不是答案。**问题 100% 在调度：我们困在错误起点里，走不到赢家。**

### 病灶：SWEEP 第一轮预算被 n 放大到近乎无限

v53 特意加过「第三层多轮限额扫荡（怪物不再垄断队列）」，机制是对的。但预算是自适应的：

    sweep0 = (total_free - 18000) * 5000 + 3000000     // 注释：「664→~78M 实测标定」

L664 的 n=33486 代进去 = **80,400,000 节点**，**是赢家所需的 276 倍** ——
等于第一轮就给每个起点近乎无限的额度，**防怪物垄断的机制在大盘上完全失效**。

那句「664→~78M 实测标定」**标错了对象**：标的是怪物树/决赛者总量，不是赢家树。
**这是个很典型的坑 —— 参数按错误的样本标定，而且注释把这个错误固化成了「实测」。**

**影响面**：这条公式对**所有 n > 18000 的关卡**生效，也就是 L570 往上的全部大盘。

### v63 的修法

第一轮回到 3M 起步，放大交给本来就有的倍增阶梯（SWEEPMUL=8，4 轮 = 3M/24M/192M/1.5G）。
大盘赢家真需要 18M 的话第二轮就接住，**代价只是多跑一轮，而不是把整关烧掉**。

算术：漏斗 287s（满优先级）+ 59 决赛者 × 3M 封顶（≈35s）/ 32 片 ≈ 70s = **357s < 600s**。

### ⚠ 两次实验设计失误（记下来别再犯）

1. **JOBS=8 测漏斗**：漏斗要筛 33486 个起点，8 核时每片 4186 个而不是 1046 个，
   420 秒连漏斗都没走完 —— 欠配的实验什么也证明不了。
2. **`nice -n 19` + 没开 TREELOG**：nice 让漏斗从 287s 变成 470~513s（差一倍），
   而「0 条 T3 记录」其实只是**没开日志开关**，不代表没进第三层。
   **在有人用机器的环境里测性能，必须先确认优先级和日志开关，否则读出来的都是噪声。**

### 下一步

- **回归验证**：这条公式影响 L570 往上全部大盘，修完必须 L570 / L603 / L664 各跑一次，
  不能只看 664 好了就上线 —— 那几关现在是能过的，别修坏。
- **L695 值得用同样的方法验**：先离线找出它的赢家起点，再 PINSTART 量赢家树。
  如果也是几十万节点，那它同样不是"更硬"，而是同一个调度病。


## L664 攻陷：2432s → 706s，并暴露出真正的瓶颈（2026-08-24）

`SWEEP=400000`（按赢家树 291k 节点定标）一发命中，**解通过官方校验器**，起点 (88,11)
（和离线那次的 (90,11) 不同 —— 这关至少有两个可行起点）。

三个预算的对照，能看出那个坑有多深：

| | 第一轮预算 | 相对赢家树(291k) | 结果 |
| --- | --- | --- | --- |
| 原公式 (n-18000)*5000+3M | 80,400,000 | **276×** | 2432s |
| 我第一次改 | 3,000,000 | 10× | 仍超时 |
| **按赢家树定标** | **400,000** | **1.4×** | **706s ✅** |

我第一次改也差一个数量级，原因是**把「剩 59」读成了全局**——那是**每片**的数字。
（这次 FUNNELDUMP 实测幸存者总数 501。）

### ⭐ 瓶颈整个转移了：漏斗的**负载不均**

    漏斗结束 t=697s → 总耗时 706s        第三层只用了 9 秒
    各分片漏斗耗时：最快 113s  中位 124s  **最慢 697s**

**6 倍离散度。** 第三层修好之后，99% 的时间全在漏斗，而漏斗本身不贵 ——
典型分片 124 秒就筛完了，**是一片卡到 697 秒把所有人拖住**。

**用户问了一个关键问题：卡住时其他分片不能先往下跑吗？**
答案分两层：
1. **它们确实没在等** —— 28 个 fork 出来的子进程各跑各的，谁先解出谁写管道、父进程杀掉其余。
2. **但它们跑完就没活干、直接退出了。** 起点是**静态切分**（`i % 28 == 我`），
   快的片 124 秒把自己那份做完就退出，**核空着**；而答案若落在卡住那片的名单里，
   就只能等那一个核磨到 697 秒。**27 个核白闲 570 秒。**

所以问题不是"等"，是**活分死了、匀不过去**。

### v65：漏斗动态取号（work stealing）

`mmap(MAP_SHARED)` 一个原子计数器，子进程 `__sync_fetch_and_add` 自己领号，队列全局共享。
估算：总工作量 ≈ 4000 核·秒 / 28 ≈ **143 秒**。

**注意笔记里之前那条判断要订正**：「工作窃取——实测 28 核全程满载，预计收益不大」
在这关上不成立 —— 核确实满载过，但**只满载了前 124 秒**。
教训：「CPU 利用率高」不等于「没有负载不均」，要看的是**尾部**（最慢分片 vs 中位）。


## ⭐⭐ L664 合规攻陷：590 秒（2026-08-24）—— 官方成绩线 663 → 694

`v66 + SWEEP=400000 + STEAL=1`：**590 秒，解通过官方校验器**。
L664 是唯一挡路的钉子（665-694 本来全是 LEGAL），**它一通，官方 600 秒口径直接推到 694**。

从 2432 秒到 590 秒，两处修：

| | 改动 | 效果 |
| --- | --- | --- |
| 第三层 | SWEEP 第一轮 80.4M → **400k**（按赢家树 291k 定标） | 找到赢家只要 9 秒 |
| 漏斗 | 静态分片 → **动态取号**（mmap 共享原子计数器） | 27 个核不再白闲 |

### ⚠ v65 的教训：一个「让程序变快」的 bug 差点被当成胜利

v65 第一版漏了 `mine_total` 的实时计数（动态取号事先算不出「我这片有几个」，
我关掉了预计算却忘了在循环里补）。后果是分母为 0 => 存活率算成 0% =>
**第二层整个跳过、深传播不升档**。

而日志看上去是**大胜**：漏斗从 697 秒降到 111 秒，且 `最快111/中位111/最慢111` 完全均衡。

**但那是假快 —— 它不是变高效了，是少干了活。** 证据在幸存者数：
每片从 59 个暴涨到 **564~768**（全局约 1.9 万决赛者），第三层根本扫不完，832 秒没解出。

**教训：判断「筛选阶段」的改动，要看的是工作完成度（幸存者数），不是墙钟。**
一个同时「变快」和「变弱」的改动，墙钟只会显示前者。

### 还剩的一段：动态取号匀不了「单个超长任务」

    漏斗: 最快 136  中位 149  **最慢 579** 秒

27 个核在 149 秒把队列抽干退出，**但抓到怪物起点的那个核仍要在那一个起点上磨 579 秒**。
（这正是用户问的「卡住时别人能不能先跑」的下半段答案：能跑，但**一个超长任务本身没法拆**。）
本次答案恰好落在那个慢核的名单里，所以总时间被拖到 590 秒 —— **过线靠运气，不稳。**

**下一步（明确且安全）**：给漏斗里**单个起点**设上限，超了就当「没证伪」放行。
放行只会让它进第三层多占点时间，**不会漏解** —— 证伪失败 = 保留候选，永远 sound。
预期 579s → ~150s，总时间 ~160s。


## v67 单起点上限：负结果（2026-08-24）

想法：动态取号匀不了「一个超长任务」（L664 有一个核在单个起点上磨 579 秒），
那就给单起点设墙钟上限，超时中断传播、**当作「没证伪」放行**。
方向是绝对 sound 的（证伪失败 = 保留候选，少推导只会少剪不会剪错），实现也没问题
（扎在 prun_queue 的不动点循环里，每 1024 次迭代才读一次时钟；六关正确性验过）。

**但实测三档全是负结果：**

| STARTCAP | 总耗时 | 最慢漏斗 |
| --- | --- | --- |
| 无（v66） | **590s** | 579s |
| 20 秒 | 632s | 601s |
| 60 秒 | 651s | 596s |

**原因：上限省下的时间被下游加倍吃回去。** 逻辑链是
上限触发 → 证伪变弱 → 幸存者暴涨（每片 59 → 300+）→ 1.5 层深层重探和第二层的活成倍增加。

### ⭐ 这跟 v65 的 bug 是同一类错误的两面

- **v65**：漏了 mine_total → 第二层被跳过 → 看着快了一倍，实则**少干活**，下游扫不完
- **v67**：上限放行 → 看着单起点被掐住了，实则**把活推给了下游**

**共同点：漏斗的「快」和「强」是一对 trade-off，只看墙钟必然误判。**
判据必须是「幸存者数 + 总时间」一起看 —— 单看任何一个都会得出相反的结论。

### 还发现上限扎得不全（下一步的正确方向）

**1.5 层深层重探和第二层是在主漏斗循环之后跑的**，用本片自己的幸存者名单，
**既不参与动态取号、也不受单起点上限管** —— 那 596~601 秒多半在那里。

所以正确的下一步不是继续调上限的数值，而是**把动态取号扩展到 1.5 层和第二层**。
（本轮没做：机器要还给用户。）

**当前生产版本 = v66**（SWEEP=400000 + STEAL=1，L664 合规 590 秒）。

---

## v68：起点间共享传播（基座缓存）—— **可靠、19 倍省工、但墙钟不动**（负结果，但它指出了真瓶颈）

### 出发点：一处真实的 O(n²)

用户否掉了"扩大算力"的方向（"我就说算法这层面再优化，别总想着扩大算力，没意义"），
这是对的——指数曲线上 1000 倍算力只买到 40 关，而本轮真正见效的两处（SWEEP 标定、动态取号）
都是**逻辑错误**而非算力不足。

从算法层重看漏斗，发现 `propagate(s)` 里有一处明显冗余：

```c
for (int c = 0; c < N; ++c) if (g0[c]) ppush(c);   // 把全盘 33486 格推进队列
prun_queue();                                      // 跑到不动点 —— 每个起点从头来一遍
```

而 `pprocess` 对 **s 以外的每一格**，规则只依赖 `prop_endcol`：

```c
if (c == prop_start)             lo = hi = 1;
else if (col[c] == prop_endcol) { lo = 1; hi = 2; }
else                             lo = hi = 2;
```

`prop_endcol` 由 `col[s]` 决定 —— **只有 2 种取值**。也就是说这份全盘不动点，
16743 个同色起点算的是同一个东西，却各算了一遍：O(n) 的活干了 n 遍。

### 踩的坑：基座的假前提（**这是推理错，不是实现错**）

初版直接"按颜色算 2 份基座，每个起点只推 s 这一格"——**L351 当场把解丢了（输出 0 字节）**，
幸存者 10 → 6，真起点在被多杀的 4 个里面。

原因：基座对 `col[c] != endcol` 的格子断言 `lo = hi = 2`（绝无可能是端点）。
但**起点自己就是这个颜色**（自由格数为偶时 `endcol = col[s] ^ 1`）。
所以基座等于断言"起点也有 2 条边"——对 s 而言是假前提。而传播是**单调**的，
推论一旦落进 `estate` 就撤不回来，事后再设 `prop_start = s` 已经晚了。

⚠️ 教训：**"基座用更弱的假设，所以推论对所有起点都成立"这个论证是错的。**
基座用的不是更弱的假设，是一个**不同的、在某一格上更强**的假设。

### 修法：假前提的作用点是可判定的

这个假前提唯一能产生推论的途径，是 `pprocess` 里那两条规则：

```c
if (used == hi && avail > 0)          -> 在格 c 上禁边
else if (used + avail == lo && ...)   -> 在格 c 上定边
```

两条都**只动格 c 自己的边**。所以污染必然起源于 s 的那 4 条边：

> **基座没碰过 s 的边 => 基座对 s 可靠。** 碰过就退回全盘传播。

（s 的边也可能是被邻居的合法规则定的，那样会白退一次——保守，但可靠。）

### 实测：机制完美生效，墙钟纹丝不动

L451，BASECACHE=0 vs 1：

| | 传播工作量(pprocess) | 幸存者 | 基座命中 | 墙钟 |
|---|---|---|---|---|
| BC=0 | shard1: 317,960 | 17 | — | 4.2s |
| BC=1 | shard1: **16,740** | 18 | **100%**（22/22, 27/27, …） | 4.2s |

- **19 倍**省工，且 BC=1 的工作量**与起点数无关**（各分片都 ~16k = 基座建一次的成本），
  正是 O(n²)→O(n) 该有的形状；
- 可靠性修复后解一致（L301/351/401/451/551/601 全对）；
- **墙钟完全没变。** L351 曾测到 1729ms vs 241ms，重复三次后确认是噪声（两边都 ~230ms）。

### 为什么没用：这一层根本不是瓶颈（分层计时）

加了分层计时器才看清钱花在哪：

```
L551 shard1: prop 0.01s | flow 0.13s | chain 0.01s | probe 0.00s
L601 shard1: prop 0.03s | flow 0.41s | chain 0.02s | probe 0.00s
```

**max-flow 占漏斗 ~90%，我优化掉的传播层只占 ~7%。** 再往里拆 `do_flow`：

```
L601 shard1 flow 0.41s = 建网络 0.04s / Dinic增广 0.27s / 残量SCC 0.05s / 其余(subtour) 0.05s
```

**Dinic 增广是真正的大头（~65%）。**

### 结论与下一步

1. v68 **保留**（可靠、无副作用、大盘上省的绝对时间随起点数线性增长），但它**不是"大提高"**；
2. `do_flow` 犯的是**一模一样**的病：每个起点从头建整张 N+3 点网络 + 从零跑 max-flow，
   而它依赖 s 的地方只有 `col[s]`（2 种）、`c == s` 这一格、和 estate（v68 之后每个起点也只局部不同）；
3. 所以正确的下一步是**增广的热启动**：按颜色算一份基座流，每个起点只改 s 那条弧的容量（2→1），
   再**修复**流（退掉 1 单位、重增广）——O(1) 条增广路 vs 现在的 O(N) 条；
4. ⚠️ 但先别动手：L551/L601 上 `probe 0.00s(0)`——探针**根本没跑**。
   L664 因存活率高会升 PD=64 并开探针，成分可能完全不同。**必须先拿到 L664 的分层剖面。**

### 方法论

这一轮最值钱的东西不是 v68，是**分层计时器**。
之前所有"哪层贵"的判断都是读代码猜的（我一开始笃定是传播层，还算了个"O(n²)"的账），
一量就发现差了一个数量级。**优化前先量分层，别读代码猜。**

### L664 分层剖面（决定性数据）

```
shard 6:  starts=1534 keep=769  basehit=980/1534  | prop 2.35s | flow 61.42s | chain 1.51s | probe 0.00s
shard 17: starts=1562 keep=751  basehit=990/1562  | prop 2.47s | flow 64.03s | chain 1.56s | probe 0.00s
shard 16: starts=1896 keep=1038 basehit=1224/1896 | prop 2.96s | flow 77.69s | chain 1.90s | probe 0.00s
```

**flow 占第一层 92~94%，传播占 3.5%，chain 占 2.3%，探针第一层压根没跑（probe 0 次）。**
小关（L551/L601）上 flow 占 ~90%，大关上更极端。**漏斗 == max-flow。**

（基座命中率在 L664 只有 ~64%，退回率 36%——但 prop 总共才 2.35s，无所谓。）

### v69 计划：max-flow 热启动（证据充分，且结构极其有利）

关键观察：**每个起点的流网络，容量相对"基座网络"只会变小，不会变大。**

- 弧集：per-start 的 estate ⊇ 基座 estate（定的边只多不少）=> 待定边的格-格弧只减不增；
- 源/汇弧容量 `rb = b - nfix`：nfix 只增 => rb 只减；
- 格 s：`b` 从 2 变 1 => 减；哑点到 s 的弧被去掉 => 减。

于是可以用**基座的弧结构 + 每起点只下调容量**，`fcap` 一次 memcpy（~536KB，53µs，
相对现在每起点 40ms 可忽略）。只降不升是增量最大流最好办的情形：**只需退掉超额的流，不需要新增容量**。

每起点流程：
1. memcpy 基座的 `fcap`（基座跑完 max-flow 后的残量）+ 保留 `cap0` 以便反推每条弧的流量；
2. 对每条容量下调的弧，若其流量 > 新容量，沿"载流子图"DFS 退掉超额单位；
3. 直接复用**现有的** Dinic 主循环跑到最大 —— 从任何合法流出发跑到最大都得到最大流，
   **可靠性由构造保证**；
4. `flow != needL` 仍然照旧当证伪。

预期：现在从零增广要找 needL ≈ 16743 单位，热启动只需重路由 ~1 单位。
按 `建网络 0.04 / 增广 0.27 / SCC 0.05` 的比例，flow 61s 里约 46s 可省 => 第一层 66s → ~20s。

⚠️ 唯一的风险点在第 2 步造出**非法流**。必须加 debug 校验（流守恒 + 容量上界），
并在抽样起点上与"从零算"的最大流值逐个对拍——这个位置出 bug 正好会产生
本轮已经栽过两次的"假强/假快"。

---

## v69：max-flow 热启动 —— **墙钟真动了**（L601 -20%，L551 -14%）

### 做法

按起点色建一份**超集**基座网络（用 `g_estate`、`b` 一律取 2、哑点弧全带上），跑一次 max-flow 存下残量。
每个起点：`fcap` 一次 memcpy + 一遍 O(E) 把容量调低 + 退掉超额的流 + 复用原有 Dinic 主循环跑到最大。

三个让它成立的结构性事实：

1. **容量只降不升。** per-start estate ⊇ g_estate => `nfix` 只增 => `rb = b - nfix` 只减；
   格 s 的 `b` 从 2 变 1；哑点到 s 的弧被去掉。只降不升是增量最大流最好办的情形——
   只需退掉超额的流，不需要处理新增容量。
2. **图是 4 层 DAG**（SRC → X色格 → ~X色格 → SNK，哑点插在其中一层），**增广路长度恒为 3**。
   所以退 1 个单位的流不用搜路径：从弧的两端各走一跳就到 FSRC/FSNK，改 3 条弧完事。
3. **可靠性由构造保证**：从任何合法流出发把 Dinic 跑到最大，得到的就是最大流。
   不需要新的正确性论证，只需要保证第 2 步造出的流是合法的。

⚠️ 基座网络必须从 **`g_estate`** 建，**不能**从 v68 的传播基座 `base_est` 建 ——
后者在格 s 上带假前提，可能"定"了 s 的边，弧集就不再是每起点弧集的超集了。

### 实测

| | L551 | L601 |
|---|---|---|
| 冷（WARMFLOW=0） | 21396ms | 68489ms |
| 热（WARMFLOW=1） | **18313ms** | **54944ms** |

flow 层内部（L551 shard3）：

```
冷: build 0.03s / augment 0.19s / SCC 0.03s | flow证伪 1
热: build 0.04s / augment 0.00s / SCC 0.02s | flow证伪 1  退流 553  热增广 74
```

**增广 0.19s → 0.00s**：整个分片只需要 74 个单位的重增广，而从零是每个起点都推 needL ≈ 上千个单位。

**证伪力没丢**：`flow证伪` 数逐分片相等，L101/201/301/351/401/551/601 解逐字节相同。

### 安全网

`fcancel_unit` 的贪心退流如果没走到 FSRC/FSNK 就 `break`，流就非法了 —— 而这正是本轮已经栽过两次的
「静默假结果」形态（v65 假快、v30 假强）。所以加了 `warm_broken` 计数并打印告警。
实测 L301/L401/L551 **走断 0 次**，与流守恒的论证一致
（退掉一条出弧后该点必然多出 1 单位入流，所以一定找得到载流入弧）。

### 与 v68 的关系

v68 单独看是负结果（省 19 倍传播工作量但墙钟不动），但它**是 v69 的前提**：
v68 让每个起点的 estate 只在局部与基座不同，v69 的"容量只降不升"才有意义。
两者合起来才是"起点之间共享计算"这一个想法的完整形态。

### L664 实测（与冷跑同为 JOBS=20，可直接对比）

| | 冷（v68 基准） | 热（v69） |
|---|---|---|
| 分片 | shard 6: starts=1534 | shard 11: starts=1444 |
| flow 总时 | 61.42s | **15.82s** |
| 每起点 flow | **40ms** | **11ms**（3.6 倍） |
| 第一层存活率 | 769/1534 = 50.1% | 696/1444 = 48.2% |
| 第一层总时 | ~66s | **~20s** |

**存活率没升（甚至略降）=> 证伪力没丢，是真快不是假快。**

flow 层内部成分被彻底改写：

```
build 6.18s / augment 0.00s / residual-SCC 4.90s   (退流 17,845,809 单位, 热增广 979,710)
```

**增广没了。** 现在当家的是"每起点一遍 O(E) 算容量"（6.18s）和"每起点一遍残量 SCC"（4.90s）。

### v70 候选：把 O(E) 也干掉

`build 6.18s` = 97788 条正向弧 × 1444 起点 ≈ 1.41 亿次循环。但真正**变了容量**的弧很少 ——
只有"estate 相对 g_estate 有变化的格"所关联的弧，加上格 s 自己。v68 已经保证了这个变化集是局部的。

改法：propagate 时记一份 dirty 格清单，热启动只遍历 dirty 格的弧
（`cell_arc[c]` 存源/汇弧下标，`fmap[c*4+d]` 存格-格弧下标）；
needL/needR 用"基座值 + dirty 格的增量"算。`fcap` 的 memcpy 保留（782KB/起点，~0.1s 总计，可忽略），
它顺带把上一个起点改过的弧全部复位。

预期 build 6.18s → ~0.5s，flow 15.8s → ~6s。

残量 SCC 的 4.90s 更难：增量 SCC 是出了名的麻烦，先不碰。

### v70：双流基座（`base_est` 派生）—— 退流量降 4~6 倍，但在 L664 上不是杠杆

v69 的流基座从 `g_estate` 建，而每起点的 estate 比它强得多，于是每起点要退掉巨量的流
（L664：17,845,809 / 1444 起点 = **每起点退 1.2 万个单位**）。

改法：建**两套**基座。`base_ok` 的起点（v68 传播基座命中）用从 `base_est` 派生的那套 ——
此时 estate 是从 `base_est` memcpy 出来的，弧集必然是它的超集，可靠性成立；
`!base_ok` 的起点退回 `g_estate` 那套。

实测（同解、`flow证伪` 相同）：

| | 退流单位 | 热增广 |
|---|---|---|
| L551 单基座 → 双基座 | 720 → **163** | 99 → 50 |
| L601 单基座 → 双基座 | 309 → **48** | 45 → 8 |

**但在 L664 上这不是杠杆。** 拆 `build 6.18s`：memcpy ~0.5s + O(E) 扫描 ~4.8s + 退流 ~0.9s。
双基座只动得了那 0.9s。

### 下一步：dirty 格清单（真正的杠杆）

那遍 O(E) 是 97788 条弧 × 1443 起点 ≈ 1.41 亿次迭代，3.3ms/起点、**33ns/次** ——
对这么简单的循环体来说慢得离谱，说明瓶颈是 estate/fcap/acell/kind 几个数组的**随机访存**。

⚠️ 走过一个弯路：以为热点是 `nfix` 的 4 邻居扫描，想在 `set_edge` 里增量维护 `ncnt`。
两个问题：(1) 那个扫描本来就**每格只做一次**（13.4 万次读），和重算 ncnt 等价，根本不是热点；
(2) estate 有 4~5 处 memcpy 存档/回滚（`bk_estate` 探针回溯、二三层的 `est_save` 缓存），
增量计数要在每处同步 —— 正是"静默漂移"最容易藏身的形态。**放弃，不做。**

正解是整遍跳过：propagate 时记 dirty 格清单，热启动只遍历 dirty 格关联的弧
（源/汇弧存 `cell_arc[c]`，格-格弧用 `fmap[c*4+d]`），needL/needR 用基座值加 dirty 增量。
配合 v70 的双基座，dirty 集会很小。

### v69/v70 可靠性坐实 + 一条反直觉的边界

L664 在 JOBS=20 下 **830 秒自己跑完、没出解**（不是撞上限）。先查是不是误杀了真起点：

```
PINSTART=88,11  WARMFLOW=0 -> 9s,  17744 字节
PINSTART=88,11  WARMFLOW=1 -> 12s, 17744 字节（逐字节相同，官方 check 通过 ✅）
```

**真起点没被误杀。** 所以 830 秒无解是 20 分片下的调度/预算问题，不是可靠性问题
（赢家树本来就只要 ~10 秒；这和之前"瓶颈是找到赢家之前的调度浪费"的结论一致）。

⚠️ 注意上面那个反直觉的数：**单起点时热启动反而慢（12s vs 9s）。**
热启动是"一次固定开销（建流基座）换每起点省一次从零增广"，起点少就摊不回来。
已加自动关闭：`ns < 64` 时关掉（PINSTART / --verify 都吃这条），实测 12s -> 10s。

**待办：L664 端到端数字还没有。** 590 秒基准是 JOBS=32 跑的，本轮全程按"留核给用户"
用 JOBS=20 + nice，这个配置下冷跑撞 900s 上限、热跑 830s 跑完无解，两边都测不出端到端。
要拿数字得占满 32 核跑约 10 分钟 —— 需要用户点头。

### ⚠️ 测量陷阱：`timeout` 只杀父进程，fork 出来的子进程会活下来

量 v71 时 DIRTYFLOW=1 的分层数据全线比 DIRTYFLOW=0 慢约 2 倍 —— 连 `prop` 都慢了，
而那个开关**根本不碰传播层**。这就是不可能由代码解释的现象，说明是测量环境的问题。

原因：`timeout 200 ./solver` 到点只 kill 父进程，**20 个 fork 出来的子进程会继续跑**。
于是紧接着启动的第二次测量，开头有 40 个进程挤 32 核，整体 2 倍慢。
第一层的分层数据恰好就在这段重叠期里产生。

规矩：**A/B 之间必须 `pkill -9 -x <exe>` 并 sleep 几秒确认残留为 0。**
判据同样重要：任何"不该变的层也变了"的现象，先怀疑测量环境，不要急着解释成代码效果。

## v71：dirty 格清单 —— 收尾，并把分层计时改成 CPU 时间

### 做法

propagate 载入 estate 后清空 dirty 清单并把 s 标脏；`set_edge` 里把两端标脏。
热启动只重算 dirty 格关联的弧，其余弧靠 memcpy 复位成基座容量即可（它们的 estate 相对基座没变）。

关键简化：`set_edge(c,d,·)` 同时标记 c 和 n，而**相邻两格必然异色**，
所以格-格弧的 X 侧端点一定在 dirty 集里 => 只处理 dirty 集里的 X 色格就覆盖了全部格-格弧。

门闩 `dirty_valid`：二三层有三处 `memcpy(estate, est_save[i], ...)` **绕过 propagate** 恢复 estate，
那时 dirty 清单属于别的起点。所以那三处清掉门闩，热启动看到门闩没立就退回整遍扫描 ——
**用构造保证可靠，而不是靠"我觉得那里不会调 do_flow"。**

### L664 完整的账（CPU 时间，按每起点归一）

| | flow 合计 | build | 增广 | 残量SCC |
|---|---|---|---|---|
| 冷（v66 口径） | **41.82ms** | 1.61ms | **33.50ms** | 3.17ms |
| v69+v70 热启动+双基座 | 11.38ms | 3.92ms | 0.00ms | 3.71ms |
| +v71 dirty 清单 | **9.87ms** | 2.84ms | 0.00ms | 3.53ms |

**flow 层 4.2 倍；漏斗第一层整体 44.9ms/起点 -> 12.8ms/起点（3.5 倍）。**

注意 build 从冷的 1.61ms 涨到 2.84ms —— 热启动的 memcpy + 容量重算比冷建网络还贵，
但它换掉了 33.5ms 的增广，净赚极大。现在**残量 SCC（3.53ms）是 flow 里最大的单项**。

v71 单独的贡献只有 -13%（build -28%）：L664 每起点约 4000 格变了容量，dirty 集本来就大，
整遍跳过省不了太多，而退流那部分 dirty 根本不减。顺带一个意外：热增广 360,622 -> 4,943。

可靠性：DIRTYFLOW=0/1 在 L101/301/351/401/551/601 上解逐字节相同；
钉住真起点 (88,11) 解出 17744 字节并通过官方 check ✅。

### 分层计时改用 `CLOCK_PROCESS_CPUTIME_ID`

墙钟不可用：同一配置两次量到 flow 12.11s 和 21.39s（机器上还有别的活）。
换成进程 CPU 时间后数据立刻稳定，量的也正是我们关心的东西——算法开销本身。
顺带验证了一件事：冷基准的 CPU 时间 41.82ms 和早先墙钟量的 40ms 几乎相同，
**说明之前"3.6 倍"那个结论没有被负载污染**。

### 下一个杠杆

flow 里现在最大的是残量 SCC（3.53ms/起点，占 36%）。它每起点对 N+3 点全图跑一遍 Tarjan。
增量 SCC 是出了名的麻烦，但这里的图有结构（4 层 DAG 的残量图），也许有便宜的做法 —— 未验证。
