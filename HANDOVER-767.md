# HANDOVER：767 可靠性危机（2026-08-26，所有计算已停，待重新思考）

> ⚡ **2026-08-26 晚间破案（后续会话）：凶手 = dfs 递归栈溢出，本文其余部分是历史档案。**
> dfs 每滑行递归一帧，44k 盘的解 ≈ 2.3 万层（SOLWALK 实测），v77 的帧 × 8MB 默认栈 = 必炸。
> **只有走向深处（=接近解）的搜索才崩，浅层证伪安然返回** —— SIGSEGV 被 midpin 记"无"、
> 被分片漏斗当"这片搜穷了"（管道 EOF 不验尸）。本文的"确证事实" #3/#4 及"19/19 穷尽"由此全部作废；
> 传播层反而无罪（SOLWALK 8 真值关全谓词零违例）。
> 定谳实验：阳性对照 30/30 全崩（rc=139，ASan 指认 dfs 栈溢出）；同二进制同配置只改
> `ulimit -s unlimited` → 762 钉真起点 380s 出解过官方 check。
> 修复：setrlimit 入 main + 分片父进程 waitpid 验尸（崩过打 TAINTED 退 3）+ postrust 双保险。
> 详见 notes.md「🧨 信任重建」节；脚手架：tools/postrust.sh（阳性对照）+ SOLWALK=<解文件>（搜索段 REFSOL）。

> 状态：**未解决**。用户叫停全部计算，要求重新思考。本文是完整交接。

## 一句话案情

本地 767（= 线上 Level 765，盘面逐字节匹配，**必有解**）被我们的求解器
**全部 44516 个起点证伪** —— 可解盘 + 全灭 = 筛选/搜索链里有一条规则在这块盘上不可靠，
而它躲过了 499 关的 REFSOL 全量检查和 7 个直接邻居（761~766、768）的逐条回放（全零违例）。

## 已确证的事实（每条有实验背书）

1. **盘面为真且正常**：与线上内容匹配；单连通；度数直方图 {2:13229, 3:25366, 4:5921}
   与已解的 768 几乎逐位一致。病在代码不在盘。
2. **基础传播（基石）杀 123 个，全部合法**：度 2/3 格，死因=异色格双边强制→成环
   （§1.1/1.2/1.3 连锁）。真起点 ∈ 剩余 44393。
3. **逐层消融**（PROBEDUMP STRONG，幸存者数）：
   - subtour / 有向层 / chain-strict / chain-首滑 关掉 → **87 = 基线**（无罪）
   - flow 关掉 → 44393（flow 单独只杀 123-ish，供弹者）
   - chain 关掉 → 44383；**只关"链内部拐点"（CHAINTURN）→ 44375**
   - ⇒ 传播层 99.8% 的击杀 = **flow 的边 × 拐点规则**这对组合
4. **中间配置钉**（FLOW=1 CHAIN=0 DIRLAYER=0，快搜索 5~8s/个）：**87 全灭**
   ⇒ 拐点判决不在搜索段杀人；真起点要么不在 87（传播层被杀），要么 flow 边有毒（搜索段被剪）。
5. **地面真值**：747 的真起点 (12,19) **在**同款探针的名单里 ⇒ 探针不必然误杀。
6. **邻居 REFSOL 全零违例** ⇒ 触发条件极特异（起点/局部结构特异）。

## 已作废的结论（别再捡起来）

- ~~"767 存活率反常 400 倍"~~：对比用了被 pkill 截断的半截 747 名单；
  家里完整强筛给 747 是 ~450（1%），与 767 的 87（0.2%）同量级——**针尖名单是这族常态**。
- ~~"凶手锁定=拐点规则"~~：过早。拐点是**主力击杀者**，但"参与击杀"≠"规则错"，
  三种可能（规则错/实现偏/输入毒）未定谳。
- ~~PROPDEPTH=999999 深钉~~：每节点 O(44k) 全播种，快树 4s→120s+，税爆炸，弃。
- ~~基石钉（FLOW=0 CHAIN=0）~~：无毒但树真实巨大（假候选靠 flow 边才塌），
  40 棵慢树 2400s 上限磨不完，**从未跑完** —— 所以"真起点在 87 且被 flow 毒死"**未排除**。

## 被叫停时正在跑的终审实验（设计有效，可续跑）

**全盘求解器 + `CHAIN=0 DIRLAYER=0 SWEEP=400000 PDEVERY=20 BJ=20 JOBS=26`**（上限 6h）：
- 等价于"44288 个被拐点杀掉的起点 + 87 + 123 全部用带 flow 的快搜索重验"，
  漏斗分层预算让多数起点死在便宜探针里（避免了逐钉脚本 ~40/分钟的 sleep 粒度瓶颈，
  那个要 18 小时）。
- **二叉结局都算破案**：出解 ⇒ 拐点规则传播层误杀定罪、flow 无辜、767 落袋；
  全灭 ⇒ 44516 无一存活于"带 flow 的完整搜索" ⇒ **flow 毒边实锤**（唯一还在场的强层）。
- 续跑命令：
  ```
  CHAIN=0 DIRLAYER=0 SWEEP=400000 PDEVERY=20 BJ=20 JOBS=26 \
    bin/v77b /mnt/d/works/coilbench/levels_all/767
  ```
- 若全灭后要解剖 flow：嫌疑点按历史排序 = FDUM 哑点语义（三次栽过）>
  Régin SCC 判读方向 > needL/needR 配平。767 是偶数盘（44516），走**单哑点**分支（per-start）。

## 重要工件（/tmp 会丢，重要的已在 repo）

- `/tmp/cand_767.txt`：87 候选（OK cell F 格式）；`/tmp/abl_*.txt`：7 路消融名单
  （**flow0/CHAINTURN 两份值得存 repo**，重跑各需 ~50 分钟单核）
- 工具（已入 repo）：`tools/ablate767.sh` `tools/pin_extras.sh` `tools/midpin.sh`
  `tools/mk_startord.py`（STARTORD 只排序不过滤，REF 行才剔除）
- v77b = v77 + 链子规则开关（CHAINTURN/CHAINSTRICT/CHAINFRO）+ PDEV/BJ 仪表

## 本轮方法论教训（比 767 本身值钱）

1. **消融只看幸存者数不够**——它测不出 estate 内容对下游搜索的污染；"层无罪"只对传播层成立。
2. **"证伪"输出有三种真身**：树穷尽（真证明）/ 预算耗尽（无结论）/ 毒 estate 下的穷尽（假证明）。
   日志必须区分 cap-hit 与自然退出。
3. 逐钉脚本的 sleep 轮询粒度会成为吞吐上限（40/分钟 vs 理论 160/分钟）——
   万级重验一律走漏斗（STARTORD/全盘），别写进程池。
4. PDEVERY 的经济学按关卡波动 300 倍（747 命中 99% / 767 命中 2~5%），
   **每关先花 90 秒校准命中率再定密度**。
5. 用户四次直觉纠偏全部命中（盘面特异性/慢得没道理/剪枝该杀得动/PDEVERY=20）。

## 建议的重新思考方向（供决策）

- A. 直接续跑终审（6h 内二叉定谳）——机器成本低，信息价值最高；
- B. 先解剖 flow 的 per-start 单哑点分支（767 偶数盘）：对 87 候选之一手写
  最小 flow 模型对拍（几十行 python 独立实现 Régin，比对边判定）——不依赖任何跑；
- C. 换独立求解器交叉验证 767 的"87 候选"（家里 NESTPROBE 版 / v58 老链条），
  两个独立实现的候选集若不同，差集就是毒的指纹。
